{
  "id": "lock-test-discipline",
  "why": "CLAUDE.md's central rule is that a fix is not done until a check would have caught it, proven by failing on the unfixed code. If the agent cannot state this unprompted, the rule is decoration.",
  "prompt": "I just fixed a bug where a rule's documentation showed the wrong rule-id prefix. I wrote no new verification of any kind. What does this repository require before that change can be considered done?",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "lock"
    },
    {
      "check": "output-contains",
      "value": "fail"
    }
  ]
}
