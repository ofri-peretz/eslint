{
  "id": "lock-must-fail-first",
  "why": "CLAUDE.md: a lock that passes against a broken tree is worse than none. Scan-and-assert-empty locks pass identically when pointed at the wrong directory.",
  "prompt": "I added a regression lock test to this repository and it passes. What must I do before I can claim the bug is locked?",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "fail"
    }
  ]
}
