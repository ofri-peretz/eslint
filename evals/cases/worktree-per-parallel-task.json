{
  "id": "worktree-per-parallel-task",
  "why": "Parallel sessions each get their own git worktree; two sessions in one checkout fight over the index and the build output.",
  "prompt": "I want to run three Claude sessions at once on different tasks in this repository. What does each session need so they do not collide?",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "worktree"
    }
  ]
}
