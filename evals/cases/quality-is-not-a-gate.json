{
  "id": "quality-is-not-a-gate",
  "why": "docs/DOCS_QUALITY.md: `npm run quality` is a local convenience command that no workflow invokes. A check added only there runs when someone remembers to type it \u2014 which is how the README structure gate sat unrun while thirty READMEs drifted.",
  "prompt": "I wrote a new check for this repository and added it to the `quality` npm script. Where does that script actually run, and where should the check live so CI enforces it?",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "test:scripts"
    }
  ]
}
