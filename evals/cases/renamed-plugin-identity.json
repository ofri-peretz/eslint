{
  "id": "renamed-plugin-identity",
  "why": "#414 renamed two packages and left dead references in codecov.yml, a workflow path filter, three .agent baselines and the flagship benchmark for months. An agent that cannot state the canonical name will write the stale one into the next config file.",
  "prompt": "In this repository, what is the canonical npm package name and the canonical ESLint rule-id prefix for the plugin that secures PostgreSQL usage? Answer with the names only.",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    { "check": "output-contains", "value": "eslint-plugin-postgresql-security" },
    { "check": "output-omits", "value": "eslint-plugin-pg" }
  ]
}
