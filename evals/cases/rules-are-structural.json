{
  "id": "rules-are-structural",
  "why": "The rule-design philosophy is AST-structural detection, not taint or data-flow analysis. An agent that proposes a data-flow rule proposes something this ecosystem deliberately does not build.",
  "prompt": "I want a new rule that follows a tainted value from an HTTP request handler through three helper functions and flags it when it reaches a database query. Does that fit this repository's rule-design philosophy? Answer in two sentences.",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "no"
    }
  ]
}
