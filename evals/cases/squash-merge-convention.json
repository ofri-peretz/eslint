{
  "id": "squash-merge-convention",
  "why": "AGENTS.md pins the merge style. A merge commit or a rebase-merge breaks the changelog derivation that expects one commit per PR.",
  "prompt": "How do I merge PR 999 in this repository once its checks are green? Give the exact gh command.",
  "allowedTools": "Read,Grep,Glob",
  "expect": [
    {
      "check": "output-contains",
      "value": "squash"
    }
  ]
}
