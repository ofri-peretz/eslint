---
title: prefer-default-export
description: "💼 This rule is enabled in the following configs: recommended, typescript."
---


import { FalseNegativeCTA, WhenNotToUse } from "@/components/RuleComponents";

💼 This rule is enabled in the following configs: `recommended`, `typescript`.
💡 This rule is automatically fixable by the `--fix` CLI option.

{/*  end auto-generated rule header  */}

Prefer a default export if module exports a single name

## Rule Details

This rule aims to prevent issues related to prefer-default-export.

## Options

```json
[
  {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "enum": [
          "always",
          "single",
          "as-needed"
        ],
        "default": "single",
        "description": "When to enforce default exports: always (all), single (single export), as-needed (when beneficial)"
      },
      "ignoreFiles": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "File patterns to ignore"
      },
      "allowNamedExports": {
        "type": "boolean",
        "default": false,
        "description": "Allow named exports without warnings"
      }
    },
    "additionalProperties": false
  }
]
```

## Implementation

- [Source Code](https://github.com/ofri-peretz/eslint/blob/main/packages/eslint-plugin-import-next/src/rules/prefer-default-export.ts)
- [Test Cases](https://github.com/ofri-peretz/eslint/blob/main/packages/eslint-plugin-import-next/src/tests/prefer-default-export.test.ts)

## OWASP

- **Category**: [General Security](https://owasp.org/www-project-top-ten/)

<WhenNotToUse />

## Known False Negatives

<FalseNegativeCTA />

This rule uses pattern-based detection. The following may not be caught:

- **Dynamic patterns** - Runtime-generated code or values
- **Indirect references** - Values passed through multiple variables or functions
- **External data** - Data from APIs, databases, or user input

