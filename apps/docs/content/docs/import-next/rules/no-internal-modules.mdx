---
title: no-internal-modules
description: "💼 This rule is enabled in the following configs: recommended, typescript."
---


import { FalseNegativeCTA, WhenNotToUse } from "@/components/RuleComponents";

💼 This rule is enabled in the following configs: `recommended`, `typescript`.
💡 This rule is automatically fixable by the `--fix` CLI option.

{/*  end auto-generated rule header  */}

Forbid importing the submodules of other modules

## Rule Details

This rule aims to prevent issues related to internal-modules.

## Options

```json
[
  {
    "type": "object",
    "properties": {
      "maxDepth": {
        "type": "number",
        "minimum": 0,
        "default": 1,
        "description": "Maximum allowed depth of module imports (0 = only root, 1 = one level deep)"
      },
      "allow": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Glob patterns for paths that are allowed regardless of depth"
      },
      "forbid": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Glob patterns for paths that are explicitly forbidden"
      },
      "ignorePaths": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Glob patterns for paths to ignore completely"
      },
      "strategy": {
        "type": "string",
        "enum": [
          "error",
          "warn",
          "autofix",
          "suggest"
        ],
        "default": "error",
        "description": "How to handle violations: error (report), warn (report), autofix (fix automatically), suggest (provide suggestions)"
      }
    },
    "additionalProperties": false
  }
]
```

## Implementation

- [Source Code](https://github.com/ofri-peretz/eslint/blob/main/packages/eslint-plugin-import-next/src/rules/no-internal-modules.ts)
- [Test Cases](https://github.com/ofri-peretz/eslint/blob/main/packages/eslint-plugin-import-next/src/tests/no-internal-modules.test.ts)

## OWASP

- **Category**: [General Security](https://owasp.org/www-project-top-ten/)

<WhenNotToUse />

## Known False Negatives

<FalseNegativeCTA />

This rule uses pattern-based detection. The following may not be caught:

- **Dynamic patterns** - Runtime-generated code or values
- **Indirect references** - Values passed through multiple variables or functions
- **External data** - Data from APIs, databases, or user input

