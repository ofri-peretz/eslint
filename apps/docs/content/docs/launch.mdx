---
title: 'Interlace — Static Analysis for Humans + AI Agents'
description: 'The first JavaScript / TypeScript ESLint ecosystem designed for both human reviewers and AI coding agents as primary consumers. CWE-Compatible, MCP-native, federally-mappable.'
icon: Rocket
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { Bot, Layers, Award } from 'lucide-react';

<Callout type="info" title="Install today, npm soon">
  The recipes below use `npx @interlace/*` and GitHub Actions that work as soon
  as the `publish-packages.yml` workflow runs in non-dry-run mode. Until then,
  every command works from a `git clone` of the repo — invoke via
  `node packages/interlace-cli/src/index.mjs <subcommand>`.
</Callout>

**The thesis.** Static-analysis tools for JavaScript / TypeScript have spent 20 years optimizing for the human reviewer (IDE squiggles, PR comments, severity buckets). The agent-as-consumer angle is barely staked. **Interlace owns it** — every rule emits CWE-tagged findings consumable by AI agents (Claude Code, Cursor, Copilot) via MCP, plus humans via VS Code / GHAS / SARIF / your CI.

## The 30-second pitch

<Tabs items={['CLI', 'CI', 'AI Agent']}>
<Tab value="CLI">

```bash
# In your project:
npx @interlace/cli init       # writes a starter eslint.config.mjs
npx @interlace/cli audit      # lint everything with the security suite
```

</Tab>
<Tab value="CI">

```yaml title=".github/workflows/security.yml"
- uses: ofri-peretz/eslint/.github/actions/audit@main
  with:
    plugins: all
    fail-on: error            # SARIF auto-uploaded to Code Scanning Alerts
```

</Tab>
<Tab value="AI Agent">

```json title=".mcp.json"
{
  "mcpServers": {
    "secure-coding": {
      "command": "npx",
      "args": ["@interlace/secure-coding-mcp"]
    }
  }
}
```

</Tab>
</Tabs>

That's the whole product surface.

## Headline numbers

| Bench | Interlace | Strongest competitor | Read |
| :--- | :--- | :--- | :--- |
| **ILB-Arena** (security head-to-head, 18 plugins) | F1 98.8% · rank 1/18 | sonarjs F1 47.5% | We detect 40/40 vulnerable patterns; closest credible competitor catches 14 |
| **ILB-Juliet** (synthetic CWE corpus) | F1 76.5% · 100% recall | sonarjs F1 40% | 13/13 vulnerable patterns across 6 CWEs |
| **ILB-Arena-Quality** (8 plugins) | F1 64.8% · rank 2/8 | unicorn F1 50.8% | Beaten only by jsdoc which over-fires |
| **ILB-Wild** (real OSS, 22 repos, 1.7M LoC) | 7,058 findings · 4.14/kLoC | n/a | Cross-repo coverage |
| **ILB-Determinism** | 100% rule-survival | n/a | Zero finding drift across 4 Node majors × 2 TS compilers × cache states |

<Callout type="info">
  Live numbers regenerate from `benchmark-results/`. If a number above
  disagrees with the JSON, the JSON wins.
</Callout>

## What makes this different

Three legs the static-analysis field has not integrated:

<Cards>
  <Card icon={<Bot />} title="Agent-axis benches" href="#agent-axis-benches">
    Six benches no other tool publishes — determinism, autofix coverage,
    confidence calibration, NL discoverability, adversarial-rewrite
    resilience, LLM-token economy.
  </Card>
  <Card icon={<Layers />} title="Toolchain-matrix coverage" href="#toolchain-matrix-coverage">
    Findings stable across 4 Node majors × 2 TS compilers × 3 ESLint majors ×
    4 parser modes × cold/warm cache. Verified per cell.
  </Card>
  <Card icon={<Award />} title="Credibility seals + open governance" href="#credibility-seals--open-governance">
    MITRE CWE Compatibility (8/8 met), SSDF / ASVS / CAPEC / ISO 25010
    crosswalks, externally-governed corpus, public submission protocol.
  </Card>
</Cards>

### Agent-axis benches

| Bench | What it answers | Why it's new |
| :--- | :--- | :--- |
| **ILB-Determinism** | Same input → same output across N runs and M plugin versions? | Agents loop on findings; non-determinism = infinite-fix cycles. |
| **ILB-Autofix** | What % of rules have a deterministic auto-fix? | Agents need *one* correct fix; ambiguity = hallucination. |
| **ILB-Confidence** | Are stated confidence labels calibrated to empirical precision? | Agents need a routing signal: high-conf → auto-apply, low-conf → escalate. |
| **ILB-Discover** | Given an NL description, can an agent find the right rule? | Agents arrive cold; can't memorize the catalog. |
| **ILB-Evade** | Do rules survive an LLM rewriting code into a semantic equivalent? | Required for the AI-generated-code era. |
| **ILB-LLM-Tokens** | Are per-finding diagnostics cheap to feed back to an LLM? | Token economy matters at scale. |

### Toolchain-matrix coverage

We bench across 5 dimensions independently:

- **Node versions** — 18 / 20 / 22 / 24
- **TypeScript compilers** — `tsc-classic` (5.x) and `tsc-go` (6.x — Project Corsa)
- **ESLint majors** — 8 / 9 / 10
- **Parser modes** — js / js+jsx / ts / ts+jsx
- **Cache state** — cold / warm

**Zero finding drift across the entire matrix.** Every cell verified by `npm run ilb:*-matrix`.

### Credibility seals + open governance

- **MITRE CWE Compatibility** — 8/8 criteria met (submission packet ready)
- **NIST SP 800-218 SSDF** mapped per-rule (`npm run ilb:mappings:report`)
- **OWASP ASVS L1/L2/L3** mapped per-rule
- **MITRE ATT&CK / CAPEC** mapped per-rule
- **ISO/IEC 25010:2023** mapped for quality plugins
- **Pre-registered** — every result envelope carries the methodology commit SHA
- **Externally-governed corpus** (3-steward voting model, non-Interlace majority)
- **Public submission protocol** — any tool can submit a SARIF run; leaderboard auto-rebuilds

## How to consume Interlace

Pick the row that matches you — install commands and what you get back.

| You are a… | Install | Get |
| :--- | :--- | :--- |
| **Developer adding lint to a project** | `npx @interlace/cli init && npx @interlace/cli audit` | One-command setup + audit |
| **CI engineer** | The [GitHub Action](https://github.com/ofri-peretz/eslint/tree/main/.github/actions/audit) | One-line workflow + SARIF → Code Scanning Alerts |
| **Specific-plugin user** | `npm i -D @interlace/eslint-plugin-secure-coding` | Wire into your `eslint.config.mjs` |
| **Full security fleet** | `npm i -D @interlace/eslint-plugin-{secure-coding,browser-security,node-security,crypto,jwt,express-security,lambda-security,mongodb-security,nestjs-security,vercel-ai-security,pg}` | All 11 security plugins |
| **AI agent (Claude Code, Cursor, custom)** | `@interlace/<plugin>-mcp` packages | Typed agent-callable tools (`list_rules`, `audit_file`, `find_rule_violations`, `suggest_fix`) over MCP stdio |
| **SARIF-only emission** | `npm i -D @interlace/eslint-formatter-sarif` | `eslint -f @interlace/eslint-formatter-sarif src/` |
| **Security auditor / RFP responder** | `npm run ilb:mappings:report` | Per-rule crosswalk against SSDF / ASVS / CAPEC / ISO 25010 + CVE provenance |

## Try it now (no install)

```bash
git clone https://github.com/ofri-peretz/eslint
cd eslint && npm install
npx @interlace/cli audit examples/vulnerable-app/src
```

The `examples/vulnerable-app/` directory has one file per flagship rule. You'll see ~10-15 findings, each tagged with its CWE.

## Going deeper

- **[Bench catalog](https://github.com/ofri-peretz/eslint/blob/main/benchmarks/README.md)** — 26 benches + 8 reporters + 4 quality gates
- **[Submission protocol](https://github.com/ofri-peretz/eslint/blob/main/benchmarks/audits/2026-05-09-ilb-submission-protocol.md)** — how a tool vendor lands on the public leaderboard
- **[Agent-onboarding prelude](https://github.com/ofri-peretz/eslint/blob/main/.agent/rules/bench-context.md)** — single-read context for any AI working on the bench

## Acknowledgements

Built on the shoulders of every prior open-source SAST effort — OWASP Benchmark Project (Java), NIST SARD/Juliet, MITRE CWE/CAPEC. Where a methodology existed, we mirrored it; where none did for JavaScript, we say so explicitly.
