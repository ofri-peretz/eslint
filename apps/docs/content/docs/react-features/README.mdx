---
title: Overview
description: "Advanced React patterns, hook usage, and best practices enforcement."
---


<PluginHeader plugin="react-features" />

## Description

This plugin optimizes your React development by enforcing advanced patterns, proper hook usage, and industry best practices. It identifies inefficient rendering, improper state management, and potential memory leaks, ensuring your application runs smoothly. By following these guidelines, you can write more robust and performant React code that aligns with the specialized features of the framework.

<Callout type="info">
  **<RuleCount plugin="react-features" /> specialized rules** — LLM-optimized error messages with CWE, OWASP, and CVSS metadata.
</Callout>

## Installation

```bash
npm install --save-dev eslint-plugin-react-features
```

## Configuration

```js title="eslint.config.js"
import reactfeatures from 'eslint-plugin-react-features';

export default [reactfeatures.configs.recommended];
```

## Available Presets

| Preset | Description |
|--------|-------------|
| `recommended` | Balanced security for most projects |
| `strict` | Maximum enforcement (all rules as errors) |

## Rules

<RulesNavigator plugin="react-features" />

<RelatedArticles plugin="react-features" limit={3} />
