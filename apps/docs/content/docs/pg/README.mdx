---
title: Overview
description: "Security rules for PostgreSQL interaction in Node.js (SQL injection prevention)."
---


<PluginHeader plugin="pg" />

## Description

This plugin secures your Node.js applications by enforcing safe PostgreSQL interactions and preventing SQL injection vulnerabilities. It validates queries to ensure parameterized inputs are used, proactively flagging unsafe patterns before they reach production. By adopting these rules, you can maintain data integrity and protect your database from common exploits.

<Callout type="info">
  **<RuleCount plugin="pg" /> specialized rules** — LLM-optimized error messages with CWE, OWASP, and CVSS metadata.
</Callout>

## Installation

```bash
npm install --save-dev eslint-plugin-pg
```

## Configuration

```js title="eslint.config.js"
import pg from 'eslint-plugin-pg';

export default [pg.configs.recommended];
```

## Available Presets

| Preset | Description |
|--------|-------------|
| `recommended` | Balanced security for most projects |
| `strict` | Maximum enforcement (all rules as errors) |

## Rules

<RulesNavigator plugin="pg" />

<RelatedArticles plugin="pg" limit={3} />
