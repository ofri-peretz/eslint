/**
 * PostCSS Configuration
 * Configured for Tailwind CSS v4 with the @tailwindcss/postcss plugin
 */
const postcssConfig = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default postcssConfig;
