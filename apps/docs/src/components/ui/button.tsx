// Re-export shim — the canonical Button now lives in @interlace/ui/button.
// This shim only exists for `.interlace/` synced files that haven't been
// updated to the new import path.
export { Button, buttonVariants } from '@interlace/ui/button';
export type { ButtonProps } from '@interlace/ui/button';
