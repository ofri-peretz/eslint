{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "npx pnpm@8 install --frozen-lockfile",
    "buildCommand": "npx pnpm@8 build",
    "outputDirectory": ".next",
    "framework": "nextjs",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ."
}