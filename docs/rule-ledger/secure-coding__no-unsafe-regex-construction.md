# `secure-coding/no-unsafe-regex-construction`

**CWE-400** · severity HIGH · 14 catch cases · 35 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: validatePattern, useSafeLibrary, avoidDynamicFlags.
- **`inert-suggestion`** (metadata-contract) — 5 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-400/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-unsafe-regex-construction
Detects unsafe regex construction patterns (user input without escaping, dynamic flags)
CWE-400: Uncontrolled Resource Consumption

Extends detect-non-literal-regexp with pattern analysis

@see https://cwe.mitre.org/data/definitions/400.html
@see https://owasp.org/www-community/attacks/Regular_expression_Denial_of_Service_-_ReDoS

## True positives — what it catches

14 asserted case(s). These are the claims; each is only as good as the case.

- `'const regex = new RegExp(req.query.pattern);'`
- `'const regex = new RegExp("${'a'.repeat(150)}");'`
- `'const regex = new RegExp("^test$");'`
- `'const regex = new RegExp("^test$", flags);'`
- `'const regex = new RegExp("^test$", options.flags);'`
- `'const regex = new RegExp("^test$", getFlags());'`
- `'const r = new RegExp(req.query.q);'`
- `'const r = new RegExp('^${req.params.id}$');'`
- `'const r = new RegExp(await res.text());'`
- `"const r = new RegExp('^' + req.query.q);"`
- `'const r = new RegExp(process.argv[2]);'`
- `'const r = new RegExp(readFileSync(f, "utf8"));'`
- `'function f(p, item) { return new RegExp(p, item.flags); }'`
- `'function f(item) { return new RegExp(undefined, item.flags); }'`

## False-positive guards — what it must NOT flag

21 asserted case(s). A rule with few of these has not been argued with.

- `'const regex = new RegExp(req.query.pattern);'`
- `'const regex = new RegExp("^short$");'`
- `'const regex = new RegExp("${'a'.repeat(150)}");'`
- `'const regex = new RegExp("^test$");'`
- `'const regex = new RegExp("^test$");'`
- `'const regex = new RegExp("^test$", "gi");'`
- `'const regex = new RegExp("^test$", flags);'`
- `'const regex = new RegExp("^test$", options.flags);'`
- `'const regex = new RegExp("^test$", getFlags());'`
- `'const regex = new RegExp(myCustomEscape(userInput));'`
- `'const regex = new RegExp('^static$');'`
- `"const r = new RegExp('a' + ('b' + ('c' + ('d' + ('e' + ('f' + ('g' + ('h' + req.query.q))`
- `'const r = new RegExp(req.query.q);'`
- `'const r = new RegExp('^${req.params.id}$');'`
- `'const r = new RegExp(await res.text());'`
- `"const r = new RegExp('^' + req.query.q);"`
- `'const r = new RegExp(process.argv[2]);'`
- `'const r = new RegExp(readFileSync(f, "utf8"));'`
- `'const regex = escapeRegex(new RegExp(userInput));'`
- `'function f(p, item) { return new RegExp(p, item.flags); }'`
- `'function f(item) { return new RegExp(undefined, item.flags); }'`

## Not covered

No `benchmarks/corpus/CWE-400/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

