# `secure-coding/no-sensitive-data-exposure`

**CWE-532** · severity HIGH · 46 catch cases · 96 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: checkApiResponses — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: redactData, useMasking, removeFromLogs — dead metadata, or a report path that was lost.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-532/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-sensitive-data-exposure
Detects PII/credentials in logs, responses, or error messages
Priority 5: Security with Data Flow Analysis
CWE-532: Information Exposure Through Log Files

@see https://cwe.mitre.org/data/definitions/532.html

## True positives — what it catches

46 asserted case(s). These are the claims; each is only as good as the case.

- `'console.log('password: 123456');'`
- `'console.log(user.password);'`
- `'logger.info(config['apiKey']);'`
- `'console.log('token=${t}');'`
- `'console.log('value is ${apiKey}');'`
- `'console.log('got ${user.password} back');'`
- `'console.log('API token: abc123');'`
- `'console.log('secret key: sk_live_9f2a');'`
- `'console.log('SSN: 123-45-6789');'`
- `'console.log('credit card: 4111111111111111');'`
- `'console.log(userPassword);'`
- `'console.log(apiToken);'`
- `'console.log(secretKey);'`
- `'logger.info('password: ' + password);'`
- `'logger.warn('api_key: xyz');'`
- `'logger.error('token=eyJhbGciOiJIUzI1NiJ9');'`
- `'logger.debug(password);'`
- `'console.warn('secret: ' + apiSecret);'`
- `'console.error('apikey=AKIA1234567890');'`
- `'console.debug(apiKey);'`
- `'console.trace('token: eyJhbGciOi');'`
- `'log('password: 123');'`
- `'customLogger('token: ' + accessToken);'`
- `'throw new Error('password: ' + password);'`
- `'new Error('token=' + refreshToken);'`
- `'throw new Error('secret: ' + clientSecret);'`
- `'throw new Error('password: ' + value);'`
- `'throw new Error('Error: ' + userPassword);'`
- `'throw new Error('Invalid ' + apiToken);'`
- `'throw new Error('Missing ' + secretKey);'`
- `'console.log('user email: test@example.com');'`
- `'logger.info('phone number: 555-0142');'`
- literal carrying a value — `'console.log('password: hunter2');'`
- literal with = separator — `'console.log('api_key=abc123');'`
- concatenated credential in a log call — `'console.log('password: ' + password);'`
- neutral label, sensitive identifier — `'console.log('value is ' + password);'`
- concatenated credential in an error — `'throw new Error('secret: ' + apiSecret);'`
- error literal carrying a value — `'throw new Error('password: hunter2');'`
- github-utils.js:37 — token printed to stdout — `'console.log('Using token from ${source}: ${tokenFromEnv}')'`
- github-utils.js:43 — password printed to stdout — `'console.log('Using password from dev: ${password}')'`
- logDebug is still a logger — `'logDebug('password: hunter2')'`
- log_error is still a logger — `'log_error('password: hunter2')'`
- appLogger is still a logger — `'appLogger('token=eyJhbGciOi')'`
- multi-word label — `'console.log('phone number: 555-0142');'`
- tokenFromEnv holds the token — `'console.log(tokenFromEnv);'`
- apiKeyMessenger is not a descriptor suffix — `'console.log(apiKeyMessenger);'`

## False-positive guards — what it must NOT flag

82 asserted case(s). A rule with few of these has not been argued with.

- `'console.log('User logged in');'`
- `'console.log('Hello world');'`
- `'logger.info('Request received');'`
- `'logger.error('Operation failed');'`
- `'logger.debug('Processing started');'`
- `'throw new Error('Operation failed');'`
- `'new Error('Invalid input');'`
- `'const password = process.env.PASSWORD;'`
- `'processData('password value');'`
- `'fetch('https://api.example.com?token=abc');'`
- `'validator.check('password field');'`
- `'console.log('Password:', password);'`
- `'console.log('password is:', pwd);'`
- `'throw new Error('password error');'`
- `'console.warn("[WARN] Redis server does not require a password, but a password was supplie`
- `'console.log('no password is configured for this connection');'`
- `'console.log(res.statusCode);'`
- `'console.log(config[password]);'`
- `'console.log(res.body.statusCode);'`
- `'console.log('elapsed ${now()}');'`
- `'class A { #count = 0; m() { console.log(this.#count); } }'`
- `'console.log('password: 123456');'`
- `'console.log(user.password);'`
- `'logger.info(config['apiKey']);'`
- `'console.log('token=${t}');'`
- `'console.log('value is ${apiKey}');'`
- `'console.log('got ${user.password} back');'`
- `'console.log('API token: abc123');'`
- `'console.log('secret key: sk_live_9f2a');'`
- `'console.log('SSN: 123-45-6789');'`
- `'console.log('credit card: 4111111111111111');'`
- `'console.log(userPassword);'`
- `'console.log(apiToken);'`
- `'console.log(secretKey);'`
- `'logger.info('password: ' + password);'`
- `'logger.warn('api_key: xyz');'`
- `'logger.error('token=eyJhbGciOiJIUzI1NiJ9');'`
- `'logger.debug(password);'`
- `'console.warn('secret: ' + apiSecret);'`
- `'console.error('apikey=AKIA1234567890');'`
- `'console.debug(apiKey);'`
- `'console.trace('token: eyJhbGciOi');'`
- `'log('password: 123');'`
- `'customLogger('token: ' + accessToken);'`
- `'throw new Error('password: ' + password);'`
- `'new Error('token=' + refreshToken);'`
- `'throw new Error('secret: ' + clientSecret);'`
- `'throw new Error('password: ' + value);'`
- `'throw new Error('Error: ' + userPassword);'`
- `'throw new Error('Invalid ' + apiToken);'`
- `'throw new Error('Missing ' + secretKey);'`
- `'console.log('user email: test@example.com');'`
- `'logger.info('phone number: 555-0142');'`
- `'console['log']('password: 123456');'`
- `'app.logger.info('password: 123456');'`
- `'myThing.log('password: 123456');'`
- `'(getLogger())('password: 123456');'`
- `'throw new Error('code: ' - 1);'`
- `'throw new Error(1 + count);'`
- `'throw new Error('Invalid key format');'`
- `'console.log('Press any key to continue');'`
- password policy quoted back to the user — `'throw new Error('Password must contain at least one letter and one number');'`
- literal carrying a value — `'console.log('password: hunter2');'`
- literal with = separator — `'console.log('api_key=abc123');'`
- concatenated credential in a log call — `'console.log('password: ' + password);'`
- neutral label, sensitive identifier — `'console.log('value is ' + password);'`
- concatenated credential in an error — `'throw new Error('secret: ' + apiSecret);'`
- error literal carrying a value — `'throw new Error('password: hunter2');'`
- completeLogin is not a logger — `'await completeLogin(page, 'https://accounts.shopify.com/lookup', email, password)'`
- a clause between the word and the colon is not a label — `'console.warn('Soft-error fetching password from dev: ${error.message}. Try again.')'`
- two words of gap is prose — `'console.log('token was really found: yes');'`
- a descriptor-suffixed name holds a description — `'throw new Error("accountSid must start with AC" + apiKeyMsg);'`
- prose ending in the word does not label the value — `'throw new Error("Error generating JWT token " + err);'`
- same shape in a log call — `'console.log('Error generating JWT token ' + err);'`
- github-utils.js:37 — token printed to stdout — `'console.log('Using token from ${source}: ${tokenFromEnv}')'`
- github-utils.js:43 — password printed to stdout — `'console.log('Using password from dev: ${password}')'`
- logDebug is still a logger — `'logDebug('password: hunter2')'`
- log_error is still a logger — `'log_error('password: hunter2')'`
- appLogger is still a logger — `'appLogger('token=eyJhbGciOi')'`
- multi-word label — `'console.log('phone number: 555-0142');'`
- tokenFromEnv holds the token — `'console.log(tokenFromEnv);'`
- apiKeyMessenger is not a descriptor suffix — `'console.log(apiKeyMessenger);'`

## Not covered

No `benchmarks/corpus/CWE-532/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

