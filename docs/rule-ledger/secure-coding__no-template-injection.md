# `secure-coding/no-template-injection`

**CWE-94** · severity CRITICAL · 17 catch cases · 48 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-94/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-template-injection
CWE-94: Improper Control of Generation of Code (Code Injection via Templates)

Detects server-side template engine calls where the template argument is
dynamic (not a string literal). An attacker who controls the template
string can execute arbitrary server-side code.

Detection: structural-api + a NAMED untrusted source.
  Handlebars.compile(req.body.tpl)      — fires (request data)
  Handlebars.compile(userTemplate)      — fires (the name states provenance)
  Handlebars.compile('&lt;h1>{{t}}&lt;/h1>')  — silent (string literal)
  Handlebars.compile(content)           — silent (nothing says where it came from)

Covered engines: Handlebars, EJS, Pug/Jade, Mustache, Nunjucks, Swig,
  Dust, doT, and their common aliases.

## True positives — what it catches

17 asserted case(s). These are the claims; each is only as good as the case.

- `'Handlebars.compile(userTemplate)'`
- `'ejs.render(req.body.template, data)'`
- `'Handlebars.compile('Hello ${userPart}')'`
- `'pug.compile("<h1>" + req.query.title + "</h1>")'`
- `'Mustache.render(payload.template, view)'`
- `'nunjucks.renderString(userString, ctx)'`
- request body — `'Handlebars.compile(req.body.template)'`
- request data nested in a template literal — `'Handlebars.compile('<div>${req.query.tpl}</div>')'`
- request data on the right of a concatenation — `'Handlebars.compile("<div>" + req.body.tpl)'`
- awaited request body — `'async function f() { Handlebars.compile(await request.body.tpl); }'`
- process.argv — `'Handlebars.compile(process.argv[2])'`
- a file read — `'Handlebars.compile(fs.readFileSync(p, "utf8"))'`
- a bare reader function — `'Handlebars.compile(readFileSync(p, "utf8"))'`
- a reader reached through a call argument — `'Handlebars.compile(decode(readFile(p)))'`
- userTemplate names its origin — `'Handlebars.compile(userTemplate)'`
- a provenance-named property — `'Handlebars.compile(form.userInput)'`
- snake_case provenance — `'Handlebars.compile(remote_template)'`

## False-positive guards — what it must NOT flag

18 asserted case(s). A rule with few of these has not been argued with.

- `'Handlebars.compile(userTemplate)'`
- `'ejs.render(req.body.template, data)'`
- `'Handlebars.compile('Hello ${userPart}')'`
- `'pug.compile("<h1>" + req.query.title + "</h1>")'`
- `'Mustache.render(payload.template, view)'`
- `'nunjucks.renderString(userString, ctx)'`
- nested past the depth bound — `'Handlebars.compile("a" + ("b" + ("c" + ("d" + ("e" + ("f" + ("g" + userTpl)))))))'`
- request body — `'Handlebars.compile(req.body.template)'`
- request data nested in a template literal — `'Handlebars.compile('<div>${req.query.tpl}</div>')'`
- request data on the right of a concatenation — `'Handlebars.compile("<div>" + req.body.tpl)'`
- awaited request body — `'async function f() { Handlebars.compile(await request.body.tpl); }'`
- process.argv — `'Handlebars.compile(process.argv[2])'`
- a file read — `'Handlebars.compile(fs.readFileSync(p, "utf8"))'`
- a bare reader function — `'Handlebars.compile(readFileSync(p, "utf8"))'`
- a reader reached through a call argument — `'Handlebars.compile(decode(readFile(p)))'`
- userTemplate names its origin — `'Handlebars.compile(userTemplate)'`
- a provenance-named property — `'Handlebars.compile(form.userInput)'`
- snake_case provenance — `'Handlebars.compile(remote_template)'`

## Not covered

No `benchmarks/corpus/CWE-94/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

