# `browser-security/no-insecure-redirects`

**CWE-601** · severity HIGH · 39 catch cases · 81 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: whitelistDomains, validateRedirect, useRelativeUrl.
- **`inert-suggestion`** (metadata-contract) — 6 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-601/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-insecure-redirects
Detects open redirect vulnerabilities
CWE-601: URL Redirection to Untrusted Site ('Open Redirect')

@see https://cwe.mitre.org/data/definitions/601.html
@see https://owasp.org/www-community/vulnerabilities/Unvalidated_Redirects_and_Forwards

## True positives — what it catches

10 asserted case(s). These are the claims; each is only as good as the case.

- `'res.redirect(req.query.url);'`
- `'window.location.assign(window.location.hash.slice(1));'`
- `'location.assign(location.hash + '/callback');'`
- `'location.assign('${document.referrer}/next');'`
- `'location.assign(decodeURIComponent(location.hash.slice(1)));'`
- `'location.assign(flag ? '/home' : location.href);'`
- `'location.assign(fallback || document.URL);'`
- `'res.redirect(request.query.next);'`
- `'res.redirect(req.query['next']);'`
- `'location.assign(document.location.pathname);'`

## False-positive guards — what it must NOT flag

14 asserted case(s). A rule with few of these has not been argued with.

- `'res.redirect(req.query.url);'`
- `'res.redirect(req.query.url);'`
- `'res.redirect(req.query.url);'`
- `'const clean = window.location.href.replace('#', '');'`
- `'window.location.assign(setUrlQueryParams(urlObj, loginHintQueryParam));'`
- `'window.location.assign(window.location.hash.slice(1));'`
- `'location.assign(location.hash + '/callback');'`
- `'location.assign('${document.referrer}/next');'`
- `'location.assign(decodeURIComponent(location.hash.slice(1)));'`
- `'location.assign(flag ? '/home' : location.href);'`
- `'location.assign(fallback || document.URL);'`
- `'res.redirect(request.query.next);'`
- `'res.redirect(req.query['next']);'`
- `'location.assign(document.location.pathname);'`

## Not covered

No `benchmarks/corpus/CWE-601/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

