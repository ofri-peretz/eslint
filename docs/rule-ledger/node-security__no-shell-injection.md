# `node-security/no-shell-injection`

**CWE-78** · severity CRITICAL · 34 catch cases · 50 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-78/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: detect-child-process — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

Shell-execution functions from child_process that run the first arg as a shell command.

## True positives — what it catches

34 asserted case(s). These are the claims; each is only as good as the case.

- `'exec(userInput + " -la");'`
- `'exec(userInput + 'tail');'`
- `'exec('git clone ${userRepo}');'`
- `"execSync('rm -rf ' + path);"`
- `'exec('ls ${dir} | grep ${pattern}');'`
- `"const cmd = 'git'; exec(cmd + ' ' + userInput);"`
- `'child_process.exec('rm -rf ${dir}');'`
- `'execSync('npm install ${packageName}');'`
- `"let dir = '/srv'; execSync('ls ${dir}');"`
- `"const dir = cond ? '/a' : userDir; execSync('ls ${dir}');"`
- `'function installMacos(binTarget) { const filename = basename('${binTarget}.tgz'); execSyn`
- `"const n = 1 - 0; execSync('head -n ${n} f');"`
- `"const cfg = {...base, dir: '/srv'}; execSync('ls ${cfg.dir}');"`
- `"const cfg = {[k]: '/srv', dir: '/srv'}; execSync('ls ${cfg.other}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg.missing}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg[k]}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg.#p}');"`
- `'for (const row of rows) { execSync('ls ${row.d}'); }'`
- `"const rows = [{d: '/a'}, other]; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"const rows = ['/a', , '/b']; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"let rows = [{d: '/a'}]; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"const rows = notAnArray; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `'for (const row in rows) { execSync('ls ${row.d}'); }'`
- `"const rows = [{d: '/a'}]; for (let row of rows) { execSync('ls ${row.d}'); }"`
- `'for (const row of getRows()) { execSync('ls ${row.d}'); }'`
- `"const {dir} = cfg; execSync('ls ${dir}');"`
- `'function run(dir) { execSync('ls ${dir}'); }'`
- `'execSync('ls ${nowhere}');'`
- `'const chain = a; const a = chain; execSync('ls ${chain}');'`
- `'const chain = a; const a = chain; execSync('ls ${chain.d}');'`
- `'const chain = a; const a = chain; for (const row of chain) { execSync('ls ${row.d}'); }'`
- `'const r = /x/; execSync('ls ${r}');'`
- `"exec('find ' + userPath + ' -name \"*.js\"');"`
- `'execSync('ping -c 1 ${host}');'`

## False-positive guards — what it must NOT flag

34 asserted case(s). A rule with few of these has not been argued with.

- `'exec(userInput + " -la");'`
- `'exec(userInput + 'tail');'`
- `'exec('git clone ${userRepo}');'`
- `"execSync('rm -rf ' + path);"`
- `'exec('ls ${dir} | grep ${pattern}');'`
- `"const cmd = 'git'; exec(cmd + ' ' + userInput);"`
- `'child_process.exec('rm -rf ${dir}');'`
- `'execSync('npm install ${packageName}');'`
- `"let dir = '/srv'; execSync('ls ${dir}');"`
- `"const dir = cond ? '/a' : userDir; execSync('ls ${dir}');"`
- `'function installMacos(binTarget) { const filename = basename('${binTarget}.tgz'); execSyn`
- `"const n = 1 - 0; execSync('head -n ${n} f');"`
- `"const cfg = {...base, dir: '/srv'}; execSync('ls ${cfg.dir}');"`
- `"const cfg = {[k]: '/srv', dir: '/srv'}; execSync('ls ${cfg.other}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg.missing}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg[k]}');"`
- `"const cfg = {dir: '/srv'}; execSync('ls ${cfg.#p}');"`
- `'for (const row of rows) { execSync('ls ${row.d}'); }'`
- `"const rows = [{d: '/a'}, other]; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"const rows = ['/a', , '/b']; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"let rows = [{d: '/a'}]; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `"const rows = notAnArray; for (const row of rows) { execSync('ls ${row.d}'); }"`
- `'for (const row in rows) { execSync('ls ${row.d}'); }'`
- `"const rows = [{d: '/a'}]; for (let row of rows) { execSync('ls ${row.d}'); }"`
- `'for (const row of getRows()) { execSync('ls ${row.d}'); }'`
- `"const {dir} = cfg; execSync('ls ${dir}');"`
- `'function run(dir) { execSync('ls ${dir}'); }'`
- `'execSync('ls ${nowhere}');'`
- `'const chain = a; const a = chain; execSync('ls ${chain}');'`
- `'const chain = a; const a = chain; execSync('ls ${chain.d}');'`
- `'const chain = a; const a = chain; for (const row of chain) { execSync('ls ${row.d}'); }'`
- `'const r = /x/; execSync('ls ${r}');'`
- `"exec('find ' + userPath + ' -name \"*.js\"');"`
- `'execSync('ping -c 1 ${host}');'`

## Not covered

No `benchmarks/corpus/CWE-78/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

