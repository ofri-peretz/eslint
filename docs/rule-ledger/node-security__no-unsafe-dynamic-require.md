# `node-security/no-unsafe-dynamic-require`

**CWE-95** · severity CRITICAL · 14 catch cases · 25 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-95/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: detect-eval-with-expression — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-unsafe-dynamic-require
Detects dynamic require() calls that could lead to code injection

## True positives — what it catches

13 asserted case(s). These are the claims; each is only as good as the case.

- `'const module = require(moduleName);'`
- `'const mod = require(userInput);'`
- `'require('./modules/${moduleName}');'`
- `'require('./${dir}/${file}');'`
- `'const mod = require(config.moduleName);'`
- `'const mod = require(moduleName);'`
- `'(require)(moduleName);'`
- `'const req = require; req(moduleName);'`
- `'const mod = require(moduleName);'`
- `'app.get('/load', (req, res) => { const mod = require(req.query.name); });'`
- `'const plugin = require(process.argv[2]);'`
- `'function boot(req) {`
- `'function boot(payload) { return require(payload.mod); }'`

## False-positive guards — what it must NOT flag

22 asserted case(s). A rule with few of these has not been argued with.

- `'const fs = require("fs");'`
- `'const path = require("path");'`
- `'const module = require("./local-module");'`
- `'const fs = require('fs');'`
- `'const path = require('./utils/path');'`
- `'const result = myFunction(moduleName);'`
- `'const obj = { require: () => {} }; obj.require(name);'`
- `'const module = require(moduleName);'`
- `'const mod = require(userInput);'`
- `'require('./modules/${moduleName}');'`
- `'require('./${dir}/${file}');'`
- `'const mod = require(config.moduleName);'`
- `'const mod = require(moduleName);'`
- `'require();'`
- `'require(...args);'`
- `'(require)(moduleName);'`
- `'const req = require; req(moduleName);'`
- `'const mod = require(moduleName);'`
- `'app.get('/load', (req, res) => { const mod = require(req.query.name); });'`
- `'const plugin = require(process.argv[2]);'`
- `'function boot(req) {`
- `'function boot(payload) { return require(payload.mod); }'`

## Not covered

No `benchmarks/corpus/CWE-95/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

