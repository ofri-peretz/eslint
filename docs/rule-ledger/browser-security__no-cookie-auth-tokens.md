# `browser-security/no-cookie-auth-tokens`

**CWE-1004** · severity HIGH · 7 catch cases · 12 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useHttpOnlyCookie.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-1004/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-sensitive-cookie-js — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-cookie-auth-tokens
Detects storing auth tokens in cookies via JavaScript (should use HttpOnly)
CWE-1004: Sensitive Cookie Without 'HttpOnly' Flag

@see https://cwe.mitre.org/data/definitions/1004.html

## True positives — what it catches

7 asserted case(s). These are the claims; each is only as good as the case.

- `'document.cookie = 'token=abc123';'`
- `'document.cookie = 'authToken=xyz';'`
- `'document.cookie = 'jwt=eyJhbGciOiJIUzI1NiJ9';'`
- `'document.cookie = 'sessionId=12345';'`
- `'document.cookie = 'accessToken=' + token;'`
- `'document.cookie = \'auth=\${value}\';'`
- `'document.cookie = 'token=abc123';'`

## False-positive guards — what it must NOT flag

7 asserted case(s). A rule with few of these has not been argued with.

- `'document.cookie = 'token=abc123';'`
- `'document.cookie = 'authToken=xyz';'`
- `'document.cookie = 'jwt=eyJhbGciOiJIUzI1NiJ9';'`
- `'document.cookie = 'sessionId=12345';'`
- `'document.cookie = 'accessToken=' + token;'`
- `'document.cookie = \'auth=\${value}\';'`
- `'document.cookie = 'token=abc123';'`

## Not covered

No `benchmarks/corpus/CWE-1004/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

