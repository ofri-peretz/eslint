# `node-security/no-timing-unsafe-compare`

**CWE-208** · severity HIGH · 9 catch cases · 22 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: secretPatterns — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useTimingSafeEqual.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-208/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: no-timing-unsafe-compare
Detects === comparison of secrets, suggest crypto.timingSafeEqual()
CWE-208: Observable Timing Discrepancy

@see https://cwe.mitre.org/data/definitions/208.html

## True positives — what it catches

9 asserted case(s). These are the claims; each is only as good as the case.

- `'app.post('/hook', (req, res) => {`
- `'function check(req) { return req.query.token === process.env.ADMIN_TOKEN; }'`
- `'function check(req, stored) {`
- `'const API_KEY = process.env.API_KEY;`
- `'function check(req, vault) { return req.body.token === vault.token; }'`
- `'function f(req, o) { return req.body.sig === o[kind].hash; }'`
- `'function f(req, file) { return req.body.sig === file.hash; }'`
- `'function f(req) { return req.body.sig === get().hash; }'`
- `'function handler(incoming, stored) { return incoming.token === stored.token; }'`

## False-positive guards — what it must NOT flag

9 asserted case(s). A rule with few of these has not been argued with.

- `'app.post('/hook', (req, res) => {`
- `'function check(req) { return req.query.token === process.env.ADMIN_TOKEN; }'`
- `'function check(req, stored) {`
- `'const API_KEY = process.env.API_KEY;`
- `'function check(req, vault) { return req.body.token === vault.token; }'`
- `'function f(req, o) { return req.body.sig === o[kind].hash; }'`
- `'function f(req, file) { return req.body.sig === file.hash; }'`
- `'function f(req) { return req.body.sig === get().hash; }'`
- `'function handler(incoming, stored) { return incoming.token === stored.token; }'`

## Not covered

No `benchmarks/corpus/CWE-208/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

