# `browser-security/no-missing-security-headers`

**CWE-693** · severity HIGH · 5 catch cases · 12 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: addSecurityHeaders, useMiddleware.
- **`inert-suggestion`** (metadata-contract) — 3 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-693/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-missing-security-headers
Detects missing security headers in HTTP responses
CWE-693: Protection Mechanism Failure

@see https://cwe.mitre.org/data/definitions/693.html
@see https://owasp.org/www-project-secure-headers/

## True positives — what it catches

4 asserted case(s). These are the claims; each is only as good as the case.

- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("Content-Security-Policy", "default-src self");'`
- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("Other-Header", "value");'`

## False-positive guards — what it must NOT flag

7 asserted case(s). A rule with few of these has not been argued with.

- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("Content-Security-Policy", "default-src self");'`
- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("X-Custom", "value");'`
- `'res.setHeader("Custom-Header", "value");'`
- `'res.setHeader("Other-Header", "value");'`

## Not covered

No `benchmarks/corpus/CWE-693/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

