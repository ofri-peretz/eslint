# `browser-security/no-clickjacking`

**CWE-1021** · severity HIGH · 14 catch cases · 27 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: detectTransparentOverlays, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: clickjackingVulnerability, missingXFrameOptions, missingCspFrameAncestors, implementFrameBusting, useXFrameOptions, setCspFrameAncestors — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: missingFrameBusting.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-1021/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`whole-program-text`** (performance) — sourceCode.getText() with no node prints the whole file. Inside a visitor this is O(file) per node. **Probe:** time the rule against a 5k-line file versus a 500-line one; cost should be linear, not quadratic.

## Why this rule exists

ESLint Rule: no-clickjacking
Detects clickjacking vulnerabilities (CWE-1021)

Clickjacking tricks users into clicking on invisible or disguised elements
by overlaying them with transparent frames. This rule detects missing
protections against clickjacking attacks.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe iframe usage patterns
- Trusted frame sources
- JSDoc annotations (@trusted-frame, @safe-iframe)
- Frame-busting protections

## True positives — what it catches

14 asserted case(s). These are the claims; each is only as good as the case.

- A call result is not a frame reference — this is not frame-busting. — `'if (getTop() != self) { top.location = 'https://evil.test'; }'`
- An `if` that is NOT a frame comparison does not make the assignment frame-busting — this is a redirect gated on an unrelated flag. — `'if (isEmbedded) { top.location = 'https://evil.test'; }'`
- The guard does not reach through an escaping function. `var` is function-scoped, so this binding hoists out of the block and the call below resolves to it — the redirect can run with no frame check having happened at all.  The block-scoped spelling (`function doRedirect() {}` inside the `if`) is deliberately NOT here: nothing outside can resolve it, so it is unreachable rather than unguarded. — `'if (top != self) { var doRedirect = () => { top.location = 'https://evil.test'; }; } doRe`
- Stored on an object inside the guard, callable from anywhere. — `'if (top != self) { window.doRedirect = () => { top.location = 'https://evil.test'; }; }'`
- `'<iframe src="http://external-site.com"></iframe>'`
- `'<iframe src="https://untrusted.com/widget"></iframe>'`
- `'top.location = "http://evil.com";'`
- `'window.location = "http://evil.com";'`
- `'const css = "style=opacity: 0; position: absolute; top: 0; left: 0;";'`
- `'element.cssText = "css visibility: hidden; z-index: -1;";'`
- `'const style = 'style opacity: 0; position: absolute; top: 0; left: 0;';'`
- `'<iframe src="https://untrusted.com"></iframe>'`
- `'<iframe src="https://untrusted-social-widget.com/like" width="200" height="50" />'`
- `'window.location = "https://evil.com";'`

## False-positive guards — what it must NOT flag

27 asserted case(s). A rule with few of these has not been argued with.

- `'<iframe src="/local-content.html"></iframe>'`
- `"// CSP: frame-ancestors 'self'; const x = 1;"`
- `'const data = { name: "test" };'`
- `'if (top != self) { console.log("framed"); }'`
- A call result is not a frame reference — this is not frame-busting. — `'if (getTop() != self) { top.location = 'https://evil.test'; }'`
- An `if` that is NOT a frame comparison does not make the assignment frame-busting — this is a redirect gated on an unrelated flag. — `'if (isEmbedded) { top.location = 'https://evil.test'; }'`
- The guard does not reach through an escaping function. `var` is function-scoped, so this binding hoists out of the block and the call below resolves to it — the redirect can run with no frame check having happened at all.  The block-scoped spelling (`function doRedirect() {}` inside the `if`) is deliberately NOT here: nothing outside can resolve it, so it is unreachable rather than unguarded. — `'if (top != self) { var doRedirect = () => { top.location = 'https://evil.test'; }; } doRe`
- Stored on an object inside the guard, callable from anywhere. — `'if (top != self) { window.doRedirect = () => { top.location = 'https://evil.test'; }; }'`
- `'const x = 1; function handleClick() {} button;'`
- `'element.onClick = handler;'`
- `'<iframe src="http://external-site.com"></iframe>'`
- `'<iframe src="https://untrusted.com/widget"></iframe>'`
- `'top.location = "http://evil.com";'`
- `'window.location = "http://evil.com";'`
- `'const css = "style=opacity: 0; position: absolute; top: 0; left: 0;";'`
- `'element.cssText = "css visibility: hidden; z-index: -1;";'`
- `'const style = 'style opacity: 0; position: absolute; top: 0; left: 0;';'`
- `'<iframe src="/local-content.html"></iframe>'`
- `'if (top !== self) { console.log("framed"); }'`
- `'const loadingStyle = "opacity: 0; transition: opacity 0.3s;";'`
- `'<button onClick={handleClick}>Click me</button>'`
- `'const data = 123;'`
- `'<iframe src="https://trusted.com"></iframe>'`
- `'<iframe src="https://untrusted.com"></iframe>'`
- `'<form><input type="text" /><button>Submit</button></form>'`
- `'<iframe src="https://untrusted-social-widget.com/like" width="200" height="50" />'`
- `'window.location = "https://evil.com";'`

## Not covered

No `benchmarks/corpus/CWE-1021/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

