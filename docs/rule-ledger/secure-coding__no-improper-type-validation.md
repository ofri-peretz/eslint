# `secure-coding/no-improper-type-validation`

**CWE-1287** · severity MEDIUM · 27 catch cases · 52 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

5 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: safeTypeCheckFunctions, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: incompleteTypeValidation, useProperTypeGuards, validateUserInput, strategyTypeGuards, strategySchemaValidation, strategyDefensiveProgramming — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useTypeofCorrectly.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-1287/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-improper-type-validation
Detects improper type validation in user input handling (CWE-1287)

Improper type validation can lead to security vulnerabilities when
user input is not properly validated, allowing attackers to bypass
security checks or cause unexpected behavior.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe type checking patterns
- TypeScript type guards
- Proper validation functions
- JSDoc annotations (@validated, @type-checked)

## True positives — what it catches

24 asserted case(s). These are the claims; each is only as good as the case.

- `'if (typeof userInput === "object") { processData(userInput); }'`
- `'const isObject = typeof data === "object";'`
- `'if (userInput instanceof Array) { processArray(userInput); }'`
- `'if (data instanceof Object) { handleObject(data); }'`
- `'if (input != null) { processInput(input); }'`
- `'if (userData == null) { return; }'`
- `'if (userInput != null) { processData(userInput); }'`
- `'if (req.body == null) { return; }'`
- `'const type = userInput.constructor.name;'`
- `'if (data.constructor.name === "Array") { handleArray(data); }'`
- `'const type = typeof userInput === "object";'`
- `'if (typeof customInput === "object") { /* process */ }'`
- `'if (data instanceof Array) { /* process */ }'`
- `'if (userInput) { processData(userInput); }'`
- `'if (typeof req.body === "object") { processData(req.body); }'`
- `'if (userInput != null && typeof userInput === "object") { processData(userInput); }'`
- `'if (isReady && typeof userInput === "object") { processData(userInput); }'`
- `'if (typeof userInput === "object" && isReady) { processData(userInput); }'`
- `'if (userInput == undefinedVal) { processData(userInput); }'`
- `'if (undefinedVal == userInput) { processData(userInput); }'`
- `'if (data.constructor.name === "Array") { handleArray(data); }'`
- `'if ("Array" === data.constructor.name) { handleArray(data); }'`
- `'if (null != userInput) { processData(userInput); }'`
- `'if (something != nullinput) { processData(nullinput); }'`

## False-positive guards — what it must NOT flag

38 asserted case(s). A rule with few of these has not been argued with.

- `'if (value !== null && typeof value === "object") { /* process */ }'`
- `'if (Array.isArray(data)) { /* process array */ }'`
- `'if (typeof value === "string" && value.length > 0) { /* process */ }'`
- `'if (Number.isNaN(Number(value))) { /* handle NaN */ }'`
- `'if (Object.prototype.toString.call(value) === "[object Array]") { /* process */ }'`
- `'if (value !== null && value !== undefined) { /* process */ }'`
- `'if (typeof userInput === "object") { processData(userInput); }'`
- `'const isObject = typeof data === "object";'`
- `'if (userInput instanceof Array) { processArray(userInput); }'`
- `'if (data instanceof Object) { handleObject(data); }'`
- `'if (input != null) { processInput(input); }'`
- `'if (userData == null) { return; }'`
- `'if (userInput != null) { processData(userInput); }'`
- `'if (req.body == null) { return; }'`
- `'const type = userInput.constructor.name;'`
- `'if (data.constructor.name === "Array") { handleArray(data); }'`
- `'const type = typeof userInput === "object";'`
- `'if (typeof otherInput === "object") { /* process */ }'`
- `'if (typeof customInput === "object") { /* process */ }'`
- `'if (data instanceof Array) { /* process */ }'`
- `'if (userInput) { processData(userInput); }'`
- `'if (isReady) { doSomething(); }'`
- `'if (typeof req.body === "object") { processData(req.body); }'`
- `'if (typeof config.settings === "object") { /* process */ }'`
- `'if (userInput !== null && typeof userInput === "object") { processData(userInput); }'`
- `'if (userInput != null && typeof userInput === "object") { processData(userInput); }'`
- `'if (isReady && typeof userInput === "object") { processData(userInput); }'`
- `'if (typeof userInput === "object" && isReady) { processData(userInput); }'`
- `'if (config instanceof Array) { /* process */ }'`
- `'if (userInput == undefinedVal) { processData(userInput); }'`
- `'if (undefinedVal == userInput) { processData(userInput); }'`
- `'if (5 == undefined) { /* noop */ }'`
- `'if (data.constructor.name === "Array") { handleArray(data); }'`
- `'console.log(data.constructor.name);'`
- `'if ("Array" === data.constructor.name) { handleArray(data); }'`
- `'if (null != userInput) { processData(userInput); }'`
- `'if (something != nullinput) { processData(nullinput); }'`
- `'if (userInput !== null && userInput !== undefined) { processData(userInput); }'`

## Not covered

No `benchmarks/corpus/CWE-1287/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

