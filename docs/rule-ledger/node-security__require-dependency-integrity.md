# `node-security/require-dependency-integrity`

**CWE-494** · severity HIGH · 7 catch cases · 18 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-494/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

@fileoverview Require integrity hashes for external resources

## True positives — what it catches

6 asserted case(s). These are the claims; each is only as good as the case.

- `'const t = '<link href="https://cdn.example.com/style.css">';'`
- `[`
- `"const html = '<script src=\"https://cdn.example.com/lib.js\"></script>'"`
- `"const link = '<link href=\"https://cdnjs.cloudflare.com/style.css\">'"`
- `"const js = '<script src=\"https://unpkg.com/react@17/umd/react.js\"></script>'"`
- `"'<script src=\"https://jsdelivr.net/lib.js\"></script>'"`

## False-positive guards — what it must NOT flag

11 asserted case(s). A rule with few of these has not been argued with.

- `'const t = '<script src="https://example.com/app.js"></script>';'`
- `'const t = '<script src="https://cdn.example.com/app.js" integrity="sha384-abc"></script>'`
- `[`
- `[`
- `'const t = '<link rel="preconnect"><script src="/local/app.js"></script>';'`
- `'const t = '<link href="https://cdn.example.com/style.css">';'`
- `[`
- `"const html = '<script src=\"https://cdn.example.com/lib.js\"></script>'"`
- `"const link = '<link href=\"https://cdnjs.cloudflare.com/style.css\">'"`
- `"const js = '<script src=\"https://unpkg.com/react@17/umd/react.js\"></script>'"`
- `"'<script src=\"https://jsdelivr.net/lib.js\"></script>'"`

## Not covered

No `benchmarks/corpus/CWE-494/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

