# `secure-coding/no-unlimited-resource-allocation`

**CWE-770** · severity HIGH · 48 catch cases · 121 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

2 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: safeResourceFunctions, trustedSanitizers, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: unlimitedResourceAllocation, unlimitedNetworkConnections, missingResourceLimits, implementResourceLimits, validateResourceSize, useResourcePools, strategyResourceManagement, strategyRateLimiting, strategyResourceCleanup — dead metadata, or a report path that was lost.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-unlimited-resource-allocation
Detects unlimited resource allocation vulnerabilities (CWE-770)

Unlimited resource allocation can cause denial of service by exhausting
system resources like memory, file handles, or network connections.
This rule detects patterns where resources are allocated without limits.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe resource allocation patterns
- Proper resource limits
- JSDoc annotations (@limited-resource, @safe-allocation)
- Resource cleanup patterns

## True positives — what it catches

39 asserted case(s). These are the claims; each is only as good as the case.

- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const fast = Buffer.allocUnsafe(req.body.length);'`
- `'const largeBuf = Buffer.alloc(1024 * 1024 * 100);', // 100MB`
- `'const arr = new Array(req.body.size);'`
- `'const bigArray = Array(req.body.size);'`
- `'fs.readFile(req.query.file, callback);'`
- `'fs.writeFileSync(req.query.file, data);'`
- `'const buf = Buffer.alloc(req.body.size);'`
- `'const arr = new Array(inputSize);'`
- `'const buf = Buffer.alloc(2000000);', // 2MB, over 1MB default`
- `'const buf = Buffer.alloc(customSize);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const buf = new Buffer(req.body.size);'`
- `'const buf = new Buffer(1024 * 1024 * 100);', // 100MB`
- `'const arr = Array(req.query.length);'`
- `'const arr = new Array(input);'`
- `'const upload = multer({ dest: "./uploads" });'`
- `'const upload = multer({ limits: {} });'`
- `'const upload = multer({ ...baseOptions, [computedKey]: true, dest: "./uploads" });'`
- `'const buf = Buffer.alloc(1024 * 1024 * 2);', // 2MB`
- `'const buf = Buffer.alloc(1000000 + 100000);', // 1.1MB`
- unzipper.Extract still reports — `'const unzipper = require('unzipper');\nstream.pipe(unzipper.Extract({ path: 'out' }));'`
- unzipper.Parse still reports — `'const unzipper = require('unzipper');\nstream.pipe(unzipper.Parse());'`
- aliased require still reports — `'const unzip = require('unzipper');\nconst s = unzip.Extract({ path: '/tmp' });'`
- default import alias still reports — `'import zip from 'unzipper';\nstream.pipe(zip.Extract({ path: 'out' }));'`
- zlib.createGunzip still reports — `'req.pipe(zlib.createGunzip());'`
- tar.extract still reports — `'const tar = require('tar');\ntar.extract({ cwd: 'out' });'`
- binaries.ts:315 — gunzip with no output limit — `'const gzip = require('zlib'); const gunzip = gzip.createGunzip();'`
- options without a limit still reports — `'const zlib = require('zlib'); const g = zlib.createGunzip({level: 9});'`
- a spread options object establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({...opts});'`
- a computed option key establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({['maxOutputLength']: 1});'`
- a non-object options argument establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip(opts);'`
- xml2js.parseString via the module — `'const xml2js = require('xml2js'); xml2js.parseString(xmlString, cb);'`
- parseStringPromise via the module — `'const xml2js = require('xml2js'); xml2js.parseStringPromise(xmlString);'`
- a bare named import — `'import {parseString} from 'xml2js'; parseString(xmlString, cb);'`
- an instance constructed from a bare named import — `'import {Parser} from 'xml2js'; const p = new Parser(); p.parseString(xmlString, cb);'`

## False-positive guards — what it must NOT flag

85 asserted case(s). A rule with few of these has not been argued with.

- `'const buf = Buffer.alloc(1024);'`
- `'const limitedBuf = Buffer.alloc(Math.min(userSize, 1024 * 1024));'`
- `'const arr = new Array(10);'`
- `'if (stats.size < MAX_FILE_SIZE) { fs.readFile(path, callback); }'`
- `'import path from 'path'; fs.readFileSync(path.join(__dirname, 'data', 'users.json'));'`
- `'import path from 'path'; fs.readFileSync(path.resolve(__dirname, 'data', 'users.json'));'`
- `'const buf = Buffer.alloc(...sizeArgs);'`
- `'const buf = new Buffer(...sizeArgs);'`
- `'(new Foo())(req.query.size);'`
- `'(new obj.Foo())(req.query.size);'`
- `'const buf = Buffer.alloc();'`
- `'fs.readFile();'`
- `'fs.readFile(path.join(), callback);'`
- `'const arr = Array(10, 20);'`
- `'const arr = Array(localVar);'`
- `'resetCache(Buffer.alloc(10));'`
- `'resetCache(Buffer.alloc(10), plainValue);'`
- `'arr.map();'`
- `'const buf2 = new Buffer();'`
- `'const arr2 = new Array(10, 20);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const fast = Buffer.allocUnsafe(req.body.length);'`
- `'const largeBuf = Buffer.alloc(1024 * 1024 * 100);', // 100MB`
- `'const arr = new Array(req.body.size);'`
- `'const bigArray = Array(req.body.size);'`
- `'fs.readFile(req.query.file, callback);'`
- `'fs.writeFileSync(req.query.file, data);'`
- `'const buf = Buffer.alloc(req.body.size);'`
- `'const arr = new Array(inputSize);'`
- `'const buf = Buffer.alloc(validateSize(req.body.size));'`
- `'const buf = Buffer.alloc(Math.min(userSize, MAX_BUFFER_SIZE));'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const buf = Buffer.alloc(500000);', // 500KB, under 1MB default`
- `'const buf = Buffer.alloc(2000000);', // 2MB, over 1MB default`
- `'const buf = Buffer.alloc(customSize);'`
- `'const buf = Buffer.alloc(customSize);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buf = Buffer.alloc(req.query.size);'`
- `'const buffer = new Buffer(req.query.size);'`
- `'const buf = new Buffer(req.body.size);'`
- `'const buf = new Buffer(1024 * 1024 * 100);', // 100MB`
- `'const arr = Array(req.query.length);'`
- `'const arr = new Array(input);'`
- `'const upload = multer({ limits: { fileSize: 1000 } });'`
- `'const upload = multer();'`
- `'const upload = multer({ fileSize: 1000 });'`
- `'const upload = multer({ dest: "./uploads" });'`
- `'const upload = multer({ limits: {} });'`
- `'const upload = multer({ ...baseOptions, [computedKey]: true, dest: "./uploads" });'`
- `'const buf = Buffer.alloc(2048 / 2);'`
- `'const buf = Buffer.alloc(2000 - 1000);'`
- `'const buf = Buffer.alloc(2048 % 100);'`
- `'const buf = Buffer.alloc(localMultiplier * 100);'`
- `'const buf = Buffer.alloc(2048 / 0);'`
- `'const buf = Buffer.alloc(1000 - 1000);'`
- `'const buf = Buffer.alloc(1024 * 1024 * 2);', // 2MB`
- `'const buf = Buffer.alloc(1000000 + 100000);', // 1.1MB`
- namespace import of an archive module, non-decompression method — `'import * as unzipper from 'unzipper';\nunzipper.version();'`
- named import specifier is not a module binding — `'import { Extract } from 'unzipper';\nExtract({ path: 'out' });'`
- string-keyed member on an archive binding — `'const tar = require('tar');\ntar['extract']();'`
- unzipper.Extract still reports — `'const unzipper = require('unzipper');\nstream.pipe(unzipper.Extract({ path: 'out' }));'`
- unzipper.Parse still reports — `'const unzipper = require('unzipper');\nstream.pipe(unzipper.Parse());'`
- aliased require still reports — `'const unzip = require('unzipper');\nconst s = unzip.Extract({ path: '/tmp' });'`
- default import alias still reports — `'import zip from 'unzipper';\nstream.pipe(zip.Extract({ path: 'out' }));'`
- zlib.createGunzip still reports — `'req.pipe(zlib.createGunzip());'`
- tar.extract still reports — `'const tar = require('tar');\ntar.extract({ cwd: 'out' });'`
- parseStringArgument is not an XML parser — `'const token = parseStringArgument(args[i]);'`
- forEach over a local array — `'inputsFromRemediation.forEach((input) => { return Object.keys(input).map((k) => k); });'`
- map over a local array — `'const out = errors.map((error) => Object.keys(error).map((k) => k));'`
- gunzip with an output limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({maxOutputLength: 1000});'`
- gunzip with a maxSize limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({maxSize: 1000});'`
- a nested receiver — `'const zlib = require('zlib'); const g = zlib.streams.createGunzip();'`
- binaries.ts:315 — gunzip with no output limit — `'const gzip = require('zlib'); const gunzip = gzip.createGunzip();'`
- options without a limit still reports — `'const zlib = require('zlib'); const g = zlib.createGunzip({level: 9});'`
- a spread options object establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({...opts});'`
- a computed option key establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip({['maxOutputLength']: 1});'`
- a non-object options argument establishes no limit — `'const zlib = require('zlib'); const g = zlib.createGunzip(opts);'`
- xml2js.parseString via the module — `'const xml2js = require('xml2js'); xml2js.parseString(xmlString, cb);'`
- parseStringPromise via the module — `'const xml2js = require('xml2js'); xml2js.parseStringPromise(xmlString);'`
- a bare named import — `'import {parseString} from 'xml2js'; parseString(xmlString, cb);'`
- an instance constructed from a bare named import — `'import {Parser} from 'xml2js'; const p = new Parser(); p.parseString(xmlString, cb);'`

## Not covered

Corpus: 2 vulnerable, 1 safe fixtures under `CWE-770`.

