# `secure-coding/no-ldap-injection`

**CWE-90** · severity HIGH · 29 catch cases · 53 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

5 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: useParameterizedLdap, strategyInputValidation, strategySafeLibraries, strategyFilterConstruction — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useLdapEscaping.
- **`inert-suggestion`** (metadata-contract) — 5 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-90/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-ldap-injection
Detects LDAP injection vulnerabilities (CWE-90)

LDAP injection occurs when user input is improperly inserted into LDAP
queries, allowing attackers to:
- Bypass authentication and authorization
- Extract sensitive directory information
- Perform unauthorized LDAP operations
- Enumerate users through blind injection techniques

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe LDAP libraries with built-in escaping
- Input validation and sanitization functions
- JSDoc annotations (@ldap-safe, @escaped)
- Parameterized LDAP query construction

## True positives — what it catches

25 asserted case(s). These are the claims; each is only as good as the case.

- `'const filter = '(uid=${userInput})';'`
- `'client.search(baseDN, '(cn=${req.query.name})', options);'`
- `'const ldapFilter = "(uid=" + userId + ")"; client.search(baseDN, ldapFilter);'`
- `'const filter = "(uid=*)";'`
- `'const dangerousFilter = "(|(uid=" + input + "))";'`
- `'const badFilter = "(&(cn=" + userInput + "))";'`
- `'const notFilter = "(!(uid=" + input + "))";'`
- `'client.bind('cn=${username},dc=example,dc=com', password);'`
- `'const userDn = req.body.dn; ldap.modify(userDn, changes);'`
- `'const userDn = req.params.dn; client.search(userDn, filter);'`
- `'const userFilter = '(uid=${userInput})';'`
- The variable name alone is not evidence of LDAP — the file has to actually touch a directory. With `client.search()` present this is a real CWE-90; see the valid case below for the same assignment in a file with no LDAP anywhere, which is not. — `'const ldapQuery = req.query.filter; client.search(baseDN, ldapQuery);'`
- `'let searchFilter = "(cn=*)" + input;'`
- CommonJS `require('ldapjs')` counts as LDAP evidence too — most LDAP code in the wild is CJS, so an ESM-only check would have left the majority of real directory code unprotected. — `"const ldap = require('ldapjs'); const ldapQuery = req.query.filter;"`
- `'client.search(baseDN, request.query.filter, options);'`
- `'client.search(baseDN, query.params.filter, options);'`
- `'client.search(baseDN, params.raw.filter, options);'`
- `'client.search(baseDN, body.raw.filter, options);'`
- `'const ldapFilter = '(|(cn=${trustedVal})(*|*))';'`
- `"import ldapjs from 'ldapjs';\nconst matchFilePattern = '${inputDirectory}/(${themeFilesPa`
- `"import ldapjs from 'ldapjs';\nconst ldapQuery = req.query.filter;"`
- `"const ldap = require('ldapts');\nconst ldapQuery = req.query.filter;"`
- `"const ldap = await import('activedirectory2');\nconst ldapQuery = req.query.filter;"`
- `"import ldap = require('@ldapjs/filter');\nconst ldapQuery = req.query.filter;"`
- `"import ldap from 'npm:ldapjs';\nconst ldapQuery = req.query.filter;"`

## False-positive guards — what it must NOT flag

39 asserted case(s). A rule with few of these has not been argued with.

- `'const filter = '(uid=${ldap.escape.filterValue(userId)})';'`
- `'client.search(baseDN, filter, options);'`
- `'const cleanFilter = validateLdapInput(userInput); client.search(baseDN, cleanFilter);'`
- `'const result = database.query(sql);'`
- `'const filter = "(objectClass=person)";'`
- `'const filter = '(uid=${userInput})';'`
- `'client.search(baseDN, '(cn=${req.query.name})', options);'`
- `'const ldapFilter = "(uid=" + userId + ")"; client.search(baseDN, ldapFilter);'`
- `'const filter = "(uid=*)";'`
- `'const dangerousFilter = "(|(uid=" + input + "))";'`
- `'const badFilter = "(&(cn=" + userInput + "))";'`
- `'const notFilter = "(!(uid=" + input + "))";'`
- `'client.bind('cn=${username},dc=example,dc=com', password);'`
- `'const userDn = req.body.dn; ldap.modify(userDn, changes);'`
- `'const userDn = req.params.dn; client.search(userDn, filter);'`
- `'const userFilter = '(uid=${userInput})';'`
- The variable name alone is not evidence of LDAP — the file has to actually touch a directory. With `client.search()` present this is a real CWE-90; see the valid case below for the same assignment in a file with no LDAP anywhere, which is not. — `'const ldapQuery = req.query.filter; client.search(baseDN, ldapQuery);'`
- `'let searchFilter = "(cn=*)" + input;'`
- CommonJS `require('ldapjs')` counts as LDAP evidence too — most LDAP code in the wild is CJS, so an ESM-only check would have left the majority of real directory code unprotected. — `"const ldap = require('ldapjs'); const ldapQuery = req.query.filter;"`
- `'const adminFilter = "(objectClass=admin)";'`
- `'const data = { filter: someValue };'`
- `'myLdapClient.search(base, filter);'`
- `'const escaped = myLdapEscape(userInput); const filter = '(uid=${escaped})';'`
- `'client.search(baseDN, request.query.filter, options);'`
- `'client.search(baseDN, query.params.filter, options);'`
- `'client.search(baseDN, params.raw.filter, options);'`
- `'client.search(baseDN, body.raw.filter, options);'`
- `'const filter = '(uid=${ldap.escape.filterValue(userInput)})';'`
- `'client.search(baseDN);'`
- `'let ldapFilter;'`
- `'const [ldapFilter] = ["(objectClass=person)"];'`
- `'const ldapFilter = '(|(cn=${trustedVal})(*|*))';'`
- `'const ldapFilter = "uid=" + userInput;'`
- `"import ldapjs from 'ldapjs';\nconst matchFilePattern = '${inputDirectory}/(${themeFilesPa`
- `"import ldapjs from 'ldapjs';\nconst ldapQuery = req.query.filter;"`
- `"const ldap = require('ldapts');\nconst ldapQuery = req.query.filter;"`
- `"const ldap = await import('activedirectory2');\nconst ldapQuery = req.query.filter;"`
- `"import ldap = require('@ldapjs/filter');\nconst ldapQuery = req.query.filter;"`
- `"import ldap from 'npm:ldapjs';\nconst ldapQuery = req.query.filter;"`

## Not covered

No `benchmarks/corpus/CWE-90/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

