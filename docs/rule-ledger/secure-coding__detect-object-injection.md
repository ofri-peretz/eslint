# `secure-coding/detect-object-injection`

**CWE-915** · severity LOW · 65 catch cases · 174 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

5 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: additionalMethods, strategy — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: strategyValidate, strategyWhitelist, strategyFreeze — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useMapInstead, useHasOwnProperty, whitelistKeys, useObjectCreate, freezePrototypes.
- **`inert-suggestion`** (metadata-contract) — 5 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-915/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: detect-object-injection
Detects variable[key] as a left- or right-hand assignment operand (prototype pollution)
LLM-optimized with comprehensive object injection prevention guidance

Type-Aware Enhancement:
This rule uses TypeScript type information when available to reduce false positives.
If a property key is constrained to a union of string literals (e.g., 'name' | 'email'),
the access is considered safe because the values are statically known at compile time.

@see https://portswigger.net/web-security/prototype-pollution
@see https://cwe.mitre.org/data/definitions/915.html

## True positives — what it catches

48 asserted case(s). These are the claims; each is only as good as the case.

- `'obj[prefix + userInput] = value;'`
- `'obj[true + userInput] = value;'`
- `"obj['__pro' + rest] = value;"`
- `'function put(o, k) { o[k] = 1; }'`
- `'for (const k of userSuppliedKeys) { dst[k] = 1; }'`
- `'obj[typeof userInput] = value;'`
- `'obj[a === b] = value;'`
- `"obj['' + userInput] = value;"`
- `'obj[userInput] = value;'`
- `'const val = obj[userKey];'`
- `'obj['${prefix}${key}'] = value;'`
- `'obj[config.key] = value;'`
- `'obj["__proto__"] = value;'`
- `'obj[prototypeKey] = value;'`
- `'obj["constructor"] = value;'`
- `'obj[userInput] = value;'`
- `'obj[userInput] = value;'`
- `'obj[userInput] = value;'`
- `'obj[dangerousKey] = value;'`
- `'obj["__proto__"] = value;'`
- `'obj["constructor"] = value;'`
- `'obj["prototype"] = value;'`
- `'obj.users[userId] = data;'`
- `'obj[key1][key2] = value;'`
- `'a[b][c][d] = value;'`
- `'obj[getKey()] = value;'`
- `'obj[condition ? key1 : key2] = value;'`
- `"import React from 'react';\nobj[userInput] = value;"`
- `'const v = getMetadata(target)?.[dynamicIndex];'`
- `node.object` is a standalone ChainExpression (same shape as the safe case above) but its inner call is NOT `Reflect.*`, so the recursive call falls through to the final `return false`. — `'const v = (getMetadata?.(target))[dynamicIndex];'`
- `'Reflect.getMetadata(META_KEY, target)[dynamicIndex] = value;'`
- `'obj[true] = value;'`
- `'Object.assign(target, source);'`
- `"Object.assign(target, { a: 1 }, source);"`
- `'function f(arr, n) { for (let loopVar; loopVar < n; loopVar++) { arr[loopVar]; } }'`
- `'const val = a[b][c];'`
- `'const x = arr[a + b];'`
- `'const x = arr[obj.method()];'`
- `'function m(t, s) { for (const k in s) { /* copy each prototype key across */ t[k] = s[k];`
- `'function m(t, s, o) { for (const k in s) { for (const j in o) { t[k] = o[j]; } } }'`
- `"let ALLOWED = { a: 'A' }; function f(req) { return ALLOWED[req.body.k]; }"`
- `"const M = { a: someValue }; function f(req) { const t = M[req.body.s]; process.env[t] = r`
- `"const A = { a: 'A' }; A = other; function f(req) { return A[req.body.k]; }"`
- Both the read (`A[...]`) and the write (`process.env[t]`) report once the map is no longer a closed set. — `"const A = { a: 'A' }; A = other; function f(req) { const t = A[req.body.s]; process.env[t`
- `"const A = { a: 'A' }; function f(req) { let t = A[req.body.s]; t = req.body.raw; process.`
- `"const A = { a: 'A' }; function f(req) { const t = A[req.body.s]; t = req.body.raw; proces`
- `"var A = { a: 'A' }; var A = other; function f(req) { return A[req.body.k]; }"`
- `"function f(req) { var t = M[req.body.s]; var t = req.body.raw; process.env[t] = req.body.`

## False-positive guards — what it must NOT flag

78 asserted case(s). A rule with few of these has not been argued with.

- `'obj.name = value;'`
- `'obj["name"] = value;'`
- `'const val = obj.name;'`
- `'const val = obj["name"];'`
- `'obj.property = value;'`
- `'const val = obj.property;'`
- `'obj["static"] = value;'`
- `'const x = value;'`
- `'let y = 5;'`
- `'z += 10;'`
- `'obj[prefix + userInput] = value;'`
- `'obj[true + userInput] = value;'`
- `"obj['__pro' + rest] = value;"`
- `'function put(o, k) { o[k] = 1; }'`
- `'for (const k of userSuppliedKeys) { dst[k] = 1; }'`
- `'obj[typeof userInput] = value;'`
- `'obj[a === b] = value;'`
- `"obj['' + userInput] = value;"`
- `'obj[userInput] = value;'`
- `'const val = obj[userKey];'`
- `'obj['${prefix}${key}'] = value;'`
- `'obj[config.key] = value;'`
- `'obj["__proto__"] = value;'`
- `'obj[prototypeKey] = value;'`
- `'obj["constructor"] = value;'`
- `'obj[userInput] = value;'`
- `'obj["name"] = value;'`
- `'obj[userInput] = value;'`
- `'obj["name"] = value;'`
- `'obj["safeProperty"] = value;'`
- `'user["email"] = value;'`
- `'obj[userInput] = value;'`
- `'obj[dangerousKey] = value;'`
- `'obj.name = value;'`
- `'obj["email"] = value;'`
- `'obj["name"] = value;'`
- `'obj["email"] = value;'`
- `'obj["__proto__"] = value;'`
- `'obj["constructor"] = value;'`
- `'obj["prototype"] = value;'`
- `'obj.user.name = value;'`
- `'const result = obj["data"];'`
- `'obj.users[userId] = data;'`
- `'obj[key1][key2] = value;'`
- `'a[b][c][d] = value;'`
- `'obj[getKey()] = value;'`
- `'obj[condition ? key1 : key2] = value;'`
- `'obj[userInput] = value;'`
- `'obj[userInput] = value;'`
- `'obj[userInput] = value;'`
- `'obj[userInput] = value;'`
- `"import React from 'react';\nobj[userInput] = value;"`
- `"const v = Reflect.getMetadata(PARAMTYPES_METADATA, target)[dynamicIndex];"`
- `"const v = Reflect.getMetadata(PARAMTYPES_METADATA, target)?.[dynamicIndex];"`
- `"const v = (Reflect?.getMetadata(PARAMTYPES_METADATA, target))[dynamicIndex];"`
- `'const v = getMetadata(target)?.[dynamicIndex];'`
- `node.object` is a standalone ChainExpression (same shape as the safe case above) but its inner call is NOT `Reflect.*`, so the recursive call falls through to the final `return false`. — `'const v = (getMetadata?.(target))[dynamicIndex];'`
- `'Reflect.getMetadata(META_KEY, target)[dynamicIndex] = value;'`
- `'obj[true] = value;'`
- `'Object.assign(target, source);'`
- `"Object.assign(target, { a: 1 }, source);"`
- `'Object.assign(target, source);'`
- `'function f(arr, n) { for (let loopVar; loopVar < n; loopVar++) { arr[loopVar]; } }'`
- `'const val = a[b][c];'`
- `'const x = arr[a + b];'`
- `'const x = arr[obj.method()];'`
- `'function merge(t, s) { for (const k in s) { t[k] = s[k]; } return t; }'`
- `'function m(t, s) { for (const k in s) { /* copy each prototype key across */ t[k] = s[k];`
- `'function m(t, s, o) { for (const k in s) { for (const j in o) { t[k] = o[j]; } } }'`
- `"const ALLOWED = { locale: 'APP_LOCALE' }; function f(req) { const t = ALLOWED[req.body.s]`
- `"let ALLOWED = { a: 'A' }; function f(req) { return ALLOWED[req.body.k]; }"`
- `"const M = { a: someValue }; function f(req) { const t = M[req.body.s]; process.env[t] = r`
- `"const A = { a: 'A' }; A = other; function f(req) { return A[req.body.k]; }"`
- Both the read (`A[...]`) and the write (`process.env[t]`) report once the map is no longer a closed set. — `"const A = { a: 'A' }; A = other; function f(req) { const t = A[req.body.s]; process.env[t`
- `"const A = { a: 'A' }; function f(req) { let t = A[req.body.s]; t = req.body.raw; process.`
- `"const A = { a: 'A' }; function f(req) { const t = A[req.body.s]; t = req.body.raw; proces`
- `"var A = { a: 'A' }; var A = other; function f(req) { return A[req.body.k]; }"`
- `"function f(req) { var t = M[req.body.s]; var t = req.body.raw; process.env[t] = req.body.`

## Not covered

No `benchmarks/corpus/CWE-915/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

