# `node-security/detect-suspicious-dependencies`

**CWE-506** · severity HIGH · 8 catch cases · 27 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-506/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

@fileoverview Detect potential typosquatting in dependencies
@see https://owasp.org/www-project-mobile-top-10/
@see https://cwe.mitre.org/data/definitions/506.html

## True positives — what it catches

4 asserted case(s). These are the claims; each is only as good as the case.

- `"const r = require('reakt');"`
- `"const { get } = require('raect');"`
- `"import r = require('exprses');"`
- `"export async function boot() { return import('lodas'); }"`

## False-positive guards — what it must NOT flag

6 asserted case(s). A rule with few of these has not been argued with.

- `"import { h, render } from 'preact';"`
- `"import recast from 'recast';"`
- `"const r = require('reakt');"`
- `"const { get } = require('raect');"`
- `"import r = require('exprses');"`
- `"export async function boot() { return import('lodas'); }"`

## Not covered

No `benchmarks/corpus/CWE-506/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

