# `node-security/no-data-in-temp-storage`

**CWE-312** · severity HIGH · 11 catch cases · 52 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-312/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: require-secure-credential-storage, require-storage-encryption — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

@fileoverview Prevent sensitive data in temp directories

## True positives — what it catches

9 asserted case(s). These are the claims; each is only as good as the case.

- `"let p; p = '/tmp/data';\nfs.writeFileSync(p, data);"`
- `"const p = 'C:\\\\Windows\\\\Temp\\\\x'; fs.writeFileSync(p, d);"`
- `"const file = '/tmp/sensitive.txt';\nfs.writeFileSync(file, JSON.stringify(records));"`
- `"let file = '/var/tmp/data.json';\nfs.writeFile(file, data, cb);"`
- `"let file; file = '/tmp/data';\nfs.writeFileSync(file, data);"`
- `"const file = path.join(os.tmpdir(), 'report-cache.tmp');"`
- `"fs.writeFileSync(path.join(os.tmpdir(), 'report-cache.tmp'), buffer);"`
- `"let file; file = path.join(os.tmpdir(), 'cache', 'report.tmp');"`
- `"const file = path.resolve(os.tmpdir(), 'report-cache.tmp');"`

## False-positive guards — what it must NOT flag

18 asserted case(s). A rule with few of these has not been argued with.

- `"const p = '/tmp/cache';"`
- `"const p = 'safe-path';"`
- `"let p; p = '/tmp/data';\nfs.writeFileSync(p, data);"`
- `"const p = 'C:\\\\Windows\\\\Temp\\\\x'; fs.writeFileSync(p, d);"`
- `"const dir = fs.mkdtempSync(path.join(os.tmpdir(), 'app-export-'));"`
- `"const file = path.join(os.tmpdir(), crypto.randomUUID() + '.tmp');"`
- `"class C { #join(a, b) { return a + b; } m() { const f = this.#join(os.tmpdir(), 'report-c`
- `"function f() { return path.join(os.tmpdir(), 'report-cache.tmp'); }"`
- `"fs.writeFileSync(target, path.join(os.tmpdir(), 'report-cache.tmp'));"`
- `"cache.writeFileSync(path.join(os.tmpdir(), 'report-cache.tmp'), buf);"`
- `"fs.readFileSync(path.join(os.tmpdir(), 'report-cache.tmp'));"`
- `"const file = '/tmp/sensitive.txt';\nfs.writeFileSync(file, JSON.stringify(records));"`
- `"let file = '/var/tmp/data.json';\nfs.writeFile(file, data, cb);"`
- `"let file; file = '/tmp/data';\nfs.writeFileSync(file, data);"`
- `"const file = path.join(os.tmpdir(), 'report-cache.tmp');"`
- `"fs.writeFileSync(path.join(os.tmpdir(), 'report-cache.tmp'), buffer);"`
- `"let file; file = path.join(os.tmpdir(), 'cache', 'report.tmp');"`
- `"const file = path.resolve(os.tmpdir(), 'report-cache.tmp');"`

## Not covered

No `benchmarks/corpus/CWE-312/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

