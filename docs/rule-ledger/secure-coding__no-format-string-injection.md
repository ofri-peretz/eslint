# `secure-coding/no-format-string-injection`

**CWE-134** · severity MEDIUM · 35 catch cases · 74 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 3 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: formatSpecifiers, safeFormatLibraries, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: useSafeFormatting — dead metadata, or a report path that was lost.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-134/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`unguarded-recursion`** (performance) — containsFormatSpecifiersInExpression() recurses with no visited-set or depth bound — a cyclic binding chain overflows the stack. **Probe:** lint `let a = b; let b = a;` or a self-referential type and watch for RangeError.

## Why this rule exists

ESLint Rule: no-format-string-injection
Detects format string injection vulnerabilities (CWE-134)

Format string injection occurs when user input is used as a format string
in functions like util.format(), printf-style functions, or logging functions.
Attackers can use format specifiers (%s, %d, etc.) to leak information or
cause crashes.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe format strings (hardcoded, validated)
- Proper format string escaping
- JSDoc annotations (@safe-format, @validated)
- Trusted formatting libraries

## True positives — what it catches

21 asserted case(s). These are the claims; each is only as good as the case.

- `'util.format(userInput, arg1, arg2);'`
- `'sprintf(req.query.format, data);'`
- `'printf(userFormatString);'`
- `'util.format('User: ${userInput}', data);'`
- `'console.log('Error ${req.body.message}: %s', error);'`
- `'sprintf('Format: ${userTemplate}', values);'`
- `'console.log("Format: %s", userMessage); // userMessage could contain %'`
- `'util.format("%s", req.body.format); // Could contain % specifiers'`
- `'const formatStr = "User: %s, Data: %j"; util.format(formatStr, user, data);'`
- The specifier is what makes this CWE-134: `%s` consumes an argument, so attacker-controlled text placed here can read beyond what was passed. — `'const userFormat = 'Template: %s ${req.query.template}';'`
- `'let formatString = "%s-" + userInput;'`
- `'vsprintf(userInput, [arg1]);'`
- `'util.format(req.userInput, data);'`
- `'util.format(req.body.nested.userTemplate, data);'`
- `'util.format("a" + ("b" + ("%s" + userInput)), data);'`
- `'util.format(safeInput, data);'`
- `'util.format("User: " + userInput, data);'`
- `'util.format("Prefix " + (safe + userInput), data);'`
- `'printf(userInput, arg1);'`
- `'let unused, /** @safe-format */ formatTemplate = 'prefix %s ${userInput}';'`
- `'const formatStr = '%s: ${req.body.value}''`

## False-positive guards — what it must NOT flag

33 asserted case(s). A rule with few of these has not been argued with.

- `'util.format("User: %s, Age: %d", name, age);'`
- `'console.log("Error: %s", error.message);'`
- `'console.log('User ${name} logged in');'`
- `'sprintf("%s-%s", prefix, suffix);'`
- `'console.log(userMessage);'`
- `'const safeFormat = validateFormat(userInput); util.format(safeFormat, data);'`
- `'util.format(userInput, arg1, arg2);'`
- `'sprintf(req.query.format, data);'`
- `'printf(userFormatString);'`
- `'util.format('User: ${userInput}', data);'`
- `'console.log('Error ${req.body.message}: %s', error);'`
- `'sprintf('Format: ${userTemplate}', values);'`
- `'console.log("Format: %s", userMessage); // userMessage could contain %'`
- `'util.format("%s", req.body.format); // Could contain % specifiers'`
- `'const formatStr = "User: %s, Data: %j"; util.format(formatStr, user, data);'`
- The specifier is what makes this CWE-134: `%s` consumes an argument, so attacker-controlled text placed here can read beyond what was passed. — `'const userFormat = 'Template: %s ${req.query.template}';'`
- `'let formatString = "%s-" + userInput;'`
- `'const format = "%s-%s-%s"; util.format(format, a, b, c);'`
- `'vsprintf(userInput, [arg1]);'`
- `'util.format(req.userInput, data);'`
- `'util.format(req.body.nested.userTemplate, data);'`
- The format string itself (`fmt`) is not user-input by name so no userControlledFormatString fires; the format-specifier/user-input secondary check treats args[1..] — validate(userInput) as the second arg is itself a sanitizer call, exercising the `isInputValidated` ancestor CallExpression walk (trustedSanitizers match) so it is excluded from `hasUserInputInArgs`. — `'const fmt = "%s"; util.format(fmt, validate(userInput));'`
- `'util.format("a" + ("b" + ("%s" + userInput)), data);'`
- `'myLogger.format(message, data);'`
- `'util.format(safeInput, data);'`
- `'util.format(safeInput, data);'`
- `'util.format("User: " + userInput, data);'`
- `'util.format("Prefix " + (safe + userInput), data);'`
- `'printf(userInput, arg1);'`
- `'const userFormat = /** @safe-format */ 'Template: ${req.query.template}';'`
- `'let unused, /** @safe-format */ formatTemplate = 'prefix %s ${userInput}';'`
- `'let unused, /** @safe-format */ formatString = "%s-" + userInput;'`
- `'const formatStr = '%s: ${req.body.value}''`

## Not covered

No `benchmarks/corpus/CWE-134/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

