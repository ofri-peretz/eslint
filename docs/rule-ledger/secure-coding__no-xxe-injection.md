# `secure-coding/no-xxe-injection`

**CWE-611** · severity CRITICAL · 14 catch cases · 15 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

2 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: safeParserOptions — those branches ship unexecuted.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-611/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.

## Why this rule exists

ESLint Rule: no-xxe-injection
Detects XML External Entity (XXE) injection vulnerabilities (CWE-611)

XXE injection occurs when XML parsers process external entity references,
allowing attackers to:
- Read sensitive local files
- Make HTTP requests to internal services
- Cause DoS through entity expansion (billion laughs)
- Perform SSRF attacks

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe XML libraries (libxmljs with secure config, xmldom with entity resolution disabled)
- Proper parser configuration
- JSDoc annotations (@safe, @xxe-safe)
- Input validation and sanitization

## True positives — what it catches

14 asserted case(s). These are the claims; each is only as good as the case.

- `'parser.parse(xmlString, { resolveExternals: true });'`
- `'libxmljs.parseXmlString(xml, { noent: true });'`
- `'parser.parse(xml, { expandEntityReferences: true });'`
- `'const userXml = req.query.xml; libxmljs.parseXmlString(userXml);'`
- `'const xmlData = fs.readFileSync(userFile, "utf8"); const doc = DOMParser.parse(xmlData);'`
- `'const input = req.body; parser.parse(input);'`
- `'const parser = new DOMParser(); const doc = parser.parseFromString(xmlString, "text/xml")`
- `'new DOMParser();'`
- `'new XMLHttpRequest();'`
- `'new ActiveXObject("Microsoft.XMLDOM");'`
- `'DOMParser(xmlString);'`
- `'parser.parseFromString(fs.readFileSync(path), "text/xml");'`
- `'const xml = "<!ENTITY xxe SYSTEM 'file:///etc/passwd'>";'`
- `'parser.parse(xmlString, { ...base, resolveExternals: true });'`

## False-positive guards — what it must NOT flag

15 asserted case(s). A rule with few of these has not been argued with.

- `'const cleanXml = validateXml(req.body); parser.parse(cleanXml);'`
- `'parser.parse(xmlString, { resolveExternals: true });'`
- `'libxmljs.parseXmlString(xml, { noent: true });'`
- `'parser.parse(xml, { expandEntityReferences: true });'`
- `'const userXml = req.query.xml; libxmljs.parseXmlString(userXml);'`
- `'const xmlData = fs.readFileSync(userFile, "utf8"); const doc = DOMParser.parse(xmlData);'`
- `'const input = req.body; parser.parse(input);'`
- `'const parser = new DOMParser(); const doc = parser.parseFromString(xmlString, "text/xml")`
- `'new DOMParser();'`
- `'new XMLHttpRequest();'`
- `'new ActiveXObject("Microsoft.XMLDOM");'`
- `'DOMParser(xmlString);'`
- `'parser.parseFromString(fs.readFileSync(path), "text/xml");'`
- `'const xml = "<!ENTITY xxe SYSTEM 'file:///etc/passwd'>";'`
- `'parser.parse(xmlString, { ...base, resolveExternals: true });'`

## Not covered

No `benchmarks/corpus/CWE-611/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

