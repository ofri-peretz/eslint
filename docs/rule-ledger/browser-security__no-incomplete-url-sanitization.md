# `browser-security/no-incomplete-url-sanitization`

**CWE-020** · severity HIGH · 33 catch cases · 77 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

No check fired. That means no KNOWN gap — it is not evidence of correctness.

## Why this rule exists

ESLint Rule: no-incomplete-url-sanitization
CWE-020: Improper Input Validation

Two URL checks that read as security decisions but cannot make one:

1. **A substring test standing in for a host check.**
   `url.includes('trusted.com')` is true for `https://evil.io/?r=trusted.com`
   and for `https://trusted.com.evil.io/`. The host of a URL lives in exactly
   one place — the authority component — and the only way to read it is to
   parse the URL. `indexOf(…) !== -1` and `lastIndexOf(…) !== -1` are the same
   test spelled differently; `lastIndexOf` in particular is often written
   *intending* a suffix check, which it only becomes when compared against
   `length - needle.length`.

2. **A dangerous-scheme denylist that stops at `javascript:`.**
   A sanitiser that rejects `javascript:` and returns everything else still
   hands `data:text/html;base64,…` — which executes script in `href` on every
   current browser — straight to the sink. The rule only speaks up when
   `javascript:` is explicitly denied and `data:` is never mentioned: a lone
   `data:` test is nearly always feature detection ("is this an inline
   image?"), not sanitisation, and flagging it would be noise.

@see https://cwe.mitre.org/data/definitions/20.html
@see https://cwe.mitre.org/data/definitions/601.html

## True positives — what it catches

28 asserted case(s). These are the claims; each is only as good as the case.

- `"if (url.includes('https://app.example.com')) { go(); }"`
- `"if (url.includes('//example.com')) { go(); }"`
- `"if (url.includes('example.com:8443')) { go(); }"`
- `"if (url.includes(' trusted.com ')) { go(); }"`
- `"function f(req) { if (req.headers.host.includes('trusted.com')) { go(); } }"`
- `"if (href.toLowerCase().includes('trusted.com')) { go(); }"`
- `"if (String(rawUrl).includes('trusted.com')) { go(); }"`
- `"if (location.hash.includes('trusted.com')) { go(); }"`
- `"if (url.indexOf('trusted.com') != -1) { go(); }"`
- `"if (url.indexOf('trusted.com') > -1) { go(); }"`
- `"if (url.indexOf('trusted.com') >= 0) { go(); }"`
- `"if (-1 !== url.indexOf('trusted.com')) { go(); }"`
- `"if (-1 < url.indexOf('trusted.com')) { go(); }"`
- `"while (url.includes('trusted.com')) { go(); }"`
- `"do { go(); } while (url.includes('trusted.com'));"`
- `"const ok = url.includes('trusted.com');"`
- `"const v = url.includes('trusted.com') ? 1 : 2;"`
- `"for (; url.includes('trusted.com'); ) { go(); }"`
- `"if (!url.includes('trusted.com')) { deny(); }"`
- `"const p = () => url.includes('trusted.com');"`
- `"if (href.startsWith('javascript:')) { block(); }"`
- `"const f = function (href) { if (href.startsWith('javascript:')) { block(); } };"`
- `"const f = (href) => href.startsWith('javascript:');"`
- `"function f(href) { if (/^javascript:/i.test(href)) { block(); } }"`
- `"function f(p) { if (p === 'javascript:') { block(); } }"`
- `"function f(p) { if ('javascript:' === p) { block(); } }"`
- `"function f(p) { if (p === ' javascript: ') { block(); } }"`
- `"function f(href) { if (href.indexOf('javascript:') !== -1) { block(); } }"`

## False-positive guards — what it must NOT flag

29 asserted case(s). A rule with few of these has not been argued with.

- `"if (host.lastIndexOf('.trusted.com') === host.length - 13) { go(); }"`
- `"if (url.includes('https://app.example.com')) { go(); }"`
- `"if (url.includes('//example.com')) { go(); }"`
- `"if (url.includes('example.com:8443')) { go(); }"`
- `"if (url.includes(' trusted.com ')) { go(); }"`
- `"function f(req) { if (req.headers.host.includes('trusted.com')) { go(); } }"`
- `"if (href.toLowerCase().includes('trusted.com')) { go(); }"`
- `"if (String(rawUrl).includes('trusted.com')) { go(); }"`
- `"if (location.hash.includes('trusted.com')) { go(); }"`
- `"if (url.indexOf('trusted.com') != -1) { go(); }"`
- `"if (url.indexOf('trusted.com') > -1) { go(); }"`
- `"if (url.indexOf('trusted.com') >= 0) { go(); }"`
- `"if (-1 !== url.indexOf('trusted.com')) { go(); }"`
- `"if (-1 < url.indexOf('trusted.com')) { go(); }"`
- `"while (url.includes('trusted.com')) { go(); }"`
- `"do { go(); } while (url.includes('trusted.com'));"`
- `"const ok = url.includes('trusted.com');"`
- `"const v = url.includes('trusted.com') ? 1 : 2;"`
- `"for (; url.includes('trusted.com'); ) { go(); }"`
- `"if (!url.includes('trusted.com')) { deny(); }"`
- `"const p = () => url.includes('trusted.com');"`
- `"if (href.startsWith('javascript:')) { block(); }"`
- `"const f = function (href) { if (href.startsWith('javascript:')) { block(); } };"`
- `"const f = (href) => href.startsWith('javascript:');"`
- `"function f(href) { if (/^javascript:/i.test(href)) { block(); } }"`
- `"function f(p) { if (p === 'javascript:') { block(); } }"`
- `"function f(p) { if ('javascript:' === p) { block(); } }"`
- `"function f(p) { if (p === ' javascript: ') { block(); } }"`
- `"function f(href) { if (href.indexOf('javascript:') !== -1) { block(); } }"`

## Not covered

Corpus: 5 vulnerable, 5 safe fixtures under `CWE-020`.

