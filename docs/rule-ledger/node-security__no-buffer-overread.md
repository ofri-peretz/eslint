# `node-security/no-buffer-overread`

**CWE-126** · severity HIGH · 50 catch cases · 83 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: bufferTypes, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: bufferOverread, bufferLengthNotChecked, useSafeBufferAccess, validateBufferIndices, checkBufferBounds, strategyBoundsChecking, strategyInputValidation, strategySafeBuffers — dead metadata, or a report path that was lost.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-126/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-buffer-overread
Detects buffer access beyond bounds (CWE-126)

Buffer overread occurs when reading from buffers beyond their allocated
length, potentially leading to information disclosure, crashes, or
other security issues.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe buffer access patterns
- Bounds checking operations
- JSDoc annotations (@safe, @validated)
- Input validation functions

## True positives — what it catches

48 asserted case(s). These are the claims; each is only as good as the case.

- `'buf[validateIndex(n)]; buf[other];'`
- `'function f() { const cap = Math.min(buf.length, n); return buf[cap]; }'`
- `'buf[cursor];'`
- `'buf[req.query.idx];'`
- `'buf[userData.pos];'`
- `'buf[obj.query.pos];'`
- `'buf[cfg.data.pos];'`
- `'function m() { return buf[this.offset]; }'`
- `'function h(req) { const pos = req.body.index; return buf[pos]; }'`
- `'function h2() { const pos = settings.userLimit; return buf[pos]; }'`
- `'function h3() { const pos = cfg.max; return buf[pos]; }'`
- `'const pos = Number(req.query.i); buf[pos];'`
- `'const pos = parseInt(); buf[pos];'`
- `'const pos = compute(); buf[pos];'`
- `'const rawIdx = req.body.i; const pos = rawIdx; buf[pos];'`
- `'var pos = pos; buf[pos];'`
- `'buf[Number(req.query.i)];'`
- `'buf[Number(five)];'`
- `'buf[obj.next()];'`
- One site, one finding: the slice handler owns the view methods, so `missingBoundsCheck` no longer fires alongside `unsafeBufferSlice`. — `'buf.slice(req.query.start);'`
- `'const idx = pickIdx(buf[idx]);'`
- `'const idx = helper.clamp(buf[idx]);'`
- `'const idx = Math.abs(buf[idx]);'`
- `'function f4(buf) { return buf[q]; }'`
- `'const { [buf[k]]: a } = -5;'`
- `'if (ready) { use(buf[cursor]); }'`
- `'if (checkLen(buf.length)) { use(buf[cursor]); }'`
- `'let hold, out = wrap(buf[cursor]);'`
- `'const total = buf.length + extra, out2 = wrap2(buf[cursor]);'`
- `'const byte = buffer[userInput];'`
- `'const value = buffer[offset];'`
- `'const byte = buffer[-1];'`
- `'const value = buffer[userInput - 10];'`
- `'buffer.readUInt32LE(userOffset);'`
- `'buffer.writeUInt16LE(value, userOffset);'`
- `'buffer.copy(targetBuffer, userOffset);'`
- One site, one finding — the slice handler owns the view methods. — `'buffer.slice(userOffset);'`
- `'buffer.customRead(userOffset);'`
- `'const idx = validateIndex(userIndex); const val = buffer[idx];'`
- `'const safeIdx = Math.min(buffer.length - 1, userIndex); const val = buffer[safeIdx];'`
- `'const neg = -5; buffer[neg];'`
- `'const n = -1; buffer[n];'`
- `'const byte = buffer[userOffset]; const value = buffer.readUInt32LE(userOffset);'`
- `'buffer.readUInt16LE(userIndex); buffer.readUInt32LE(userOffset);'`
- `'app.get('/b', (req, res) => { const buf = Buffer.alloc(64); res.end(buf[req.query.i]); })`
- `'const buf = Buffer.alloc(8); const x = buf[-1];'`
- `'function read(buf, req) { return buf.subarray(req.query.start, req.query.end); }'`
- `'function read(buf, req) { return buf.slice(req.query.start); }'`

## False-positive guards — what it must NOT flag

58 asserted case(s). A rule with few of these has not been argued with.

- `'const idx = validateIndex(buf[idx]);'`
- `'buf[validateIndex(n)]; buf[other];'`
- `'function f() { const cap = Math.min(buf.length, n); return buf[cap]; }'`
- `'buf[cursor];'`
- `'buf[req.query.idx];'`
- `'buf[userData.pos];'`
- `'buf[obj.query.pos];'`
- `'buf[cfg.data.pos];'`
- `'function m() { return buf[this.offset]; }'`
- `'function h(req) { const pos = req.body.index; return buf[pos]; }'`
- `'function h2() { const pos = settings.userLimit; return buf[pos]; }'`
- `'function h3() { const pos = cfg.max; return buf[pos]; }'`
- `'const pos = Number(req.query.i); buf[pos];'`
- `'const pos = parseInt(); buf[pos];'`
- `'const pos = compute(); buf[pos];'`
- `'const rawIdx = req.body.i; const pos = rawIdx; buf[pos];'`
- `'var pos = pos; buf[pos];'`
- `'buf[Number(req.query.i)];'`
- `'buf[Number(five)];'`
- `'buf[obj.next()];'`
- One site, one finding: the slice handler owns the view methods, so `missingBoundsCheck` no longer fires alongside `unsafeBufferSlice`. — `'buf.slice(req.query.start);'`
- `'const idx = pickIdx(buf[idx]);'`
- `'const idx = helper.clamp(buf[idx]);'`
- `'const idx = Math.abs(buf[idx]);'`
- `'function f4(buf) { return buf[q]; }'`
- `'const { [buf[k]]: a } = -5;'`
- `'if (ready) { use(buf[cursor]); }'`
- `'if (checkLen(buf.length)) { use(buf[cursor]); }'`
- `'let hold, out = wrap(buf[cursor]);'`
- `'const total = buf.length + extra, out2 = wrap2(buf[cursor]);'`
- `'if (index >= 0 && index < buffer.length) { const byte = buffer[index]; }'`
- `'const value = buffer.readUInt32LE(0);'`
- `'const item = array[index];'`
- `'const firstByte = buffer[0];'`
- `'const byte = buffer[userInput];'`
- `'const value = buffer[offset];'`
- `'const byte = buffer[-1];'`
- `'const value = buffer[userInput - 10];'`
- `'buffer.readUInt32LE(userOffset);'`
- `'buffer.writeUInt16LE(value, userOffset);'`
- `'buffer.copy(targetBuffer, userOffset);'`
- One site, one finding — the slice handler owns the view methods. — `'buffer.slice(userOffset);'`
- `'const header = buffer.slice(0, 4);'`
- `'const byte = data[userInput];'`
- `'buffer.customRead(userOffset);'`
- `'buffer.customRead(userOffset);'`
- `'buffer[checkBounds(userIndex)];'`
- `'const idx = validateIndex(userIndex); const val = buffer[idx];'`
- `'const safeIdx = Math.min(buffer.length - 1, userIndex); const val = buffer[safeIdx];'`
- `'const neg = -5; buffer[neg];'`
- `'const n = -1; buffer[n];'`
- `'if (buffer.length - 1 > index) {}'`
- `'const byte = buffer[userOffset]; const value = buffer.readUInt32LE(userOffset);'`
- `'buffer.readUInt16LE(userIndex); buffer.readUInt32LE(userOffset);'`
- `'app.get('/b', (req, res) => { const buf = Buffer.alloc(64); res.end(buf[req.query.i]); })`
- `'const buf = Buffer.alloc(8); const x = buf[-1];'`
- `'function read(buf, req) { return buf.subarray(req.query.start, req.query.end); }'`
- `'function read(buf, req) { return buf.slice(req.query.start); }'`

## Not covered

No `benchmarks/corpus/CWE-126/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

