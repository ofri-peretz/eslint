# `browser-security/no-http-urls`

**CWE-319** · severity HIGH · 27 catch cases · 60 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-319/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-insecure-websocket — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

@fileoverview Disallow hardcoded HTTP URLs
@see https://owasp.org/www-project-mobile-top-10/
@see https://cwe.mitre.org/data/definitions/319.html

## True positives — what it catches

23 asserted case(s). These are the claims; each is only as good as the case.

- `'const u = 'http://api.acmecorp.io/${path}';'`
- `'const u = 'http://api.${env}.acmecorp.io/x';'`
- `'const u = 'http://';'`
- `'const u = 'http://evil.acmecorp.io?next=${target}';'`
- `"fetch('http://cdn.acmecorp.io/w3.org/lib.js')"`
- `"const el = { href: 'http://acmecorp.io/ns' }"`
- `"const el = { 1: 'http://acmecorp.io/ns' }"`
- `"const apiUrl = 'http://api.acmecorp.io/data'"`
- `"fetch('http://insecure.acmecorp.io/api')"`
- `"const url = 'http://external.com/api/${path}'"`
- `"const url = 'http://prod.acmecorp.io/api'"`
- `'<img src="http://cdn.acmecorp.io/logo.png" />'`
- `'<img {...{ src: "http://cdn.acmecorp.io/a.png" }} />'`
- `"const {origin, pathname} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {href} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {pathname, ...rest} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {[k]: v} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const u = new URL('/api', 'http://prod.acmecorp.io'); fetch(u);"`
- `"fetch(new URL('/api', 'http://prod.acmecorp.io'));"`
- `"const {pathname} = new URL('http://prod.acmecorp.io/a');"`
- `"const {pathname} = new Request(p, 'http://prod.acmecorp.io');"`
- `"const {pathname} = new global.URL(p, 'http://prod.acmecorp.io');"`
- `"const [a] = new URL(p, 'http://prod.acmecorp.io');"`

## False-positive guards — what it must NOT flag

25 asserted case(s). A rule with few of these has not been argued with.

- `"const devUrl = 'http://dev.local/api'"`
- `"const devUrl = 'http://0.0.0.0:5000/api'"`
- `'const u = 'http://api.acmecorp.io/${path}';'`
- `'const u = 'http://api.${env}.acmecorp.io/x';'`
- `'const u = 'http://';'`
- `'const u = 'http://evil.acmecorp.io?next=${target}';'`
- `"fetch('http://cdn.acmecorp.io/w3.org/lib.js')"`
- `"const el = { href: 'http://acmecorp.io/ns' }"`
- `"const el = { 1: 'http://acmecorp.io/ns' }"`
- `"const apiUrl = 'http://api.acmecorp.io/data'"`
- `"fetch('http://insecure.acmecorp.io/api')"`
- `"const url = 'http://external.com/api/${path}'"`
- `"const url = 'http://prod.acmecorp.io/api'"`
- `'<img src="http://cdn.acmecorp.io/logo.png" />'`
- `'<img {...{ src: "http://cdn.acmecorp.io/a.png" }} />'`
- `"const {origin, pathname} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {href} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {pathname, ...rest} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const {[k]: v} = new URL(p, 'http://prod.acmecorp.io');"`
- `"const u = new URL('/api', 'http://prod.acmecorp.io'); fetch(u);"`
- `"fetch(new URL('/api', 'http://prod.acmecorp.io'));"`
- `"const {pathname} = new URL('http://prod.acmecorp.io/a');"`
- `"const {pathname} = new Request(p, 'http://prod.acmecorp.io');"`
- `"const {pathname} = new global.URL(p, 'http://prod.acmecorp.io');"`
- `"const [a] = new URL(p, 'http://prod.acmecorp.io');"`

## Not covered

No `benchmarks/corpus/CWE-319/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

