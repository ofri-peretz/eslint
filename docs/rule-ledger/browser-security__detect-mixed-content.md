# `browser-security/detect-mixed-content`

**CWE-311** · severity MEDIUM · 7 catch cases · 20 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-311/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

@fileoverview Detect HTTP resources in HTTPS pages
@see https://owasp.org/www-project-mobile-top-10/
@see https://cwe.mitre.org/data/definitions/311.html

## True positives — what it catches

6 asserted case(s). These are the claims; each is only as good as the case.

- `"fetch('http://cdn.acmecorp.io/w3.org/lib.js')"`
- `"const base = 'http://localhost.evil.com/api'"`
- `"const base = 'http://'"`
- `'<img src="http://cdn.acmecorp.io/logo.png" />'`
- `"const {origin} = new URL(p, 'http://prod.acmecorp.io');"`
- `"fetch(new URL('/api', 'http://prod.acmecorp.io'));"`

## False-positive guards — what it must NOT flag

6 asserted case(s). A rule with few of these has not been argued with.

- `"fetch('http://cdn.acmecorp.io/w3.org/lib.js')"`
- `"const base = 'http://localhost.evil.com/api'"`
- `"const base = 'http://'"`
- `'<img src="http://cdn.acmecorp.io/logo.png" />'`
- `"const {origin} = new URL(p, 'http://prod.acmecorp.io');"`
- `"fetch(new URL('/api', 'http://prod.acmecorp.io'));"`

## Not covered

No `benchmarks/corpus/CWE-311/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

