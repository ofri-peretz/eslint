# `browser-security/require-blob-url-revocation`

**CWE-401** · severity MEDIUM · 3 catch cases · 6 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: addRevocation.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-401/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: require-blob-url-revocation
Detects Blob URLs that are not revoked, preventing memory leaks
CWE-401: Missing Release of Memory after Effective Lifetime

@see https://cwe.mitre.org/data/definitions/401.html
@see https://developer.mozilla.org/en-US/docs/Web/API/URL/revokeObjectURL

## True positives — what it catches

2 asserted case(s). These are the claims; each is only as good as the case.

- `'const url = URL.createObjectURL(blob);'`
- `'const url = URL.createObjectURL(blob);'`

## False-positive guards — what it must NOT flag

4 asserted case(s). A rule with few of these has not been argued with.

- `'const url = URL.createObjectURL(blob);'`
- `'const url = someOtherFunction(blob);'`
- `'const url = URL.createObjectURL(blob);'`
- `'const url = URL.createObjectURL(blob);'`

## Not covered

No `benchmarks/corpus/CWE-401/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

