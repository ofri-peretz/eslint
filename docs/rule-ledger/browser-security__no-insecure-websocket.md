# `browser-security/no-insecure-websocket`

**CWE-319** · severity HIGH · 3 catch cases · 7 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-319/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-http-urls, require-websocket-wss — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

@fileoverview Require secure WebSocket connections (wss://)

## True positives — what it catches

3 asserted case(s). These are the claims; each is only as good as the case.

- `"new WebSocket('ws://acmecorp.io')"`
- `"new WebSocket('ws://acmecorp.io/${path}')"`
- `"const url = 'ws://acmecorp.io'"`

## False-positive guards — what it must NOT flag

3 asserted case(s). A rule with few of these has not been argued with.

- `"new WebSocket('ws://acmecorp.io')"`
- `"new WebSocket('ws://acmecorp.io/${path}')"`
- `"const url = 'ws://acmecorp.io'"`

## Not covered

No `benchmarks/corpus/CWE-319/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

