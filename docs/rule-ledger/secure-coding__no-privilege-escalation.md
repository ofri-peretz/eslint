# `secure-coding/no-privilege-escalation`

**CWE-269** · severity HIGH · 37 catch cases · 71 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: testFilePattern — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: addRoleCheck.
- **`inert-suggestion`** (metadata-contract) — 3 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-269/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: no-privilege-escalation
Detects potential privilege escalation vulnerabilities
CWE-269: Improper Privilege Management

@see https://cwe.mitre.org/data/definitions/269.html
@see https://owasp.org/www-community/vulnerabilities/Improper_Access_Control

## True positives — what it catches

25 asserted case(s). These are the claims; each is only as good as the case.

- `'user.role = req.body.role;'`
- `'user.permission = req.query.permission;'`
- `'user.privilege = request.body.privilege;'`
- `'grant(user, req.body.permission);'`
- `'setRole(user, req.query.role);'`
- `'userService.elevate(user, req.body.level);'`
- `'authService.updateRole(user, req.body.role);'`
- `'user.role = req.body.role;'`
- `'user.role = customInput.role;'`
- `'user.permission = req.body.permission;'`
- `'user.role = req.body.role;'`
- `'if (userService.hasRole(user)) { user.role = req.body.role; }'`
- `'const result = hasRole(user) ? user.role = req.body.role : null;'`
- `'const result = checkRole(user) ? user.role = req.body.role : null;'`
- `'const result = isAdmin() ? (user.role = req.body.role) : null;'`
- `'const result = user.hasPermission() ? (user.role = req.body.role) : null;'`
- `'grant(user, req.body.permission);'`
- `'grant(user, req.body.permission);'`
- `'checkRole(user, user.role = req.body.role);'`
- `'userService.verifyRole(user, user.role = req.body.role);'`
- `'requireRole(admin, user.permission = req.body.permission);'`
- `'if (auth.isAuthorized(user)) { user.access = req.body.access; }'`
- `'if (service.checkPermission(user)) { user.level = req.body.level; }'`
- `'updateUser({ role: req.body.role });'`
- `'updateUser({ role: req.body.role });'`

## False-positive guards — what it must NOT flag

47 asserted case(s). A rule with few of these has not been argued with.

- `'if (hasRole(user, "admin")) { user.role = req.body.role; }'`
- `'if (checkRole(user, requiredRole)) { grant(user, permission); }'`
- `'const role = "admin"; user.role = role;'`
- `'user.role = getDefaultRole();'`
- `'if (isAuthorized(user)) { setRole(user, req.body.role); }'`
- `'user.role = req.body.role;'`
- `'user.role = req.body.role;'`
- `'user.role = req.body.role;'`
- `'user.permission = req.query.permission;'`
- `'user.privilege = request.body.privilege;'`
- `'grant(user, req.body.permission);'`
- `'setRole(user, req.query.role);'`
- `'userService.elevate(user, req.body.level);'`
- `'authService.updateRole(user, req.body.role);'`
- `'user.role = req.body.role;'`
- `'user.role = req.body.role;'`
- `'if (myCustomCheck(user)) { user.role = req.body.role; }'`
- `'user.role = customInput.role;'`
- `'user.role = req.body.role;'`
- `'user.permission = req.body.permission;'`
- `'user.role = req.body.role;'`
- `'if (userService.hasRole(user)) { user.role = req.body.role; }'`
- `'const result = hasRole(user) ? user.role = req.body.role : null;'`
- `'const result = checkRole(user) ? user.role = req.body.role : null;'`
- `'const result = isAdmin() ? (user.role = req.body.role) : null;'`
- `'const result = user.hasPermission() ? (user.role = req.body.role) : null;'`
- `'grant(user, req.body.permission);'`
- `'grant(user, req.body.permission);'`
- `'checkRole(user, user.role = req.body.role);'`
- `'userService.verifyRole(user, user.role = req.body.role);'`
- `'requireRole(admin, user.permission = req.body.permission);'`
- `'if (auth.isAuthorized(user)) { user.access = req.body.access; }'`
- `'if (service.checkPermission(user)) { user.level = req.body.level; }'`
- `'updateUser({ role: req.body.role });'`
- `'if (userService.hasRole(user)) { user.role = req.body.role; }'`
- `'const result = hasRole(user) ? user.role = req.body.role : null;'`
- `'const result = checkRole(user) ? user.role = req.body.role : null;'`
- `'const result = isAdmin() ? (user.role = req.body.role) : null;'`
- `'const result = user.hasPermission() ? (user.role = req.body.role) : null;'`
- `'grant(user, req.body.permission);'`
- `'grant(user, req.body.permission);'`
- `'checkRole(user, user.role = req.body.role);'`
- `'userService.verifyRole(user, user.role = req.body.role);'`
- `'requireRole(admin, user.permission = req.body.permission);'`
- `'if (auth.isAuthorized(user)) { user.access = req.body.access; }'`
- `'if (service.checkPermission(user)) { user.level = req.body.level; }'`
- `'updateUser({ role: req.body.role });'`

## Not covered

No `benchmarks/corpus/CWE-269/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

