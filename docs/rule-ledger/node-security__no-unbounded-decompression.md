# `node-security/no-unbounded-decompression`

**CWE-409** · severity HIGH · 15 catch cases · 53 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

No check fired. That means no KNOWN gap — it is not evidence of correctness.

## Why this rule exists

ESLint Rule: no-unbounded-decompression
Detects zlib one-shot decompression with no `maxOutputLength` ceiling.
CWE-409: Improper Handling of Highly Compressed Data (Decompression Bomb)

`zlib.gunzip(body, cb)` buffers the ENTIRE expansion in memory before the
callback runs. A ~1 KB crafted gzip member expands to gigabytes, so one
request kills the process — no loop, no recursion, nothing a rate limiter
sees. `maxOutputLength` makes zlib abort with `ERR_BUFFER_TOO_LARGE` once
output passes the cap, which is the only in-band defence Node offers.

Scope note (rule partition): the streaming factories
(`zlib.createGunzip`/`createUnzip`/`createInflate`) are owned by
`secure-coding/no-unlimited-resource-allocation`. This rule owns only the
buffer-at-once entry points, which that rule does not match.

@see https://cwe.mitre.org/data/definitions/409.html
@see https://nodejs.org/api/zlib.html#class-options

## True positives — what it catches

12 asserted case(s). These are the claims; each is only as good as the case.

- `'${REQUIRE}zlib.gunzip(reqBody, (err, buf) => cb(err, buf));'`
- `'const z = require('node:zlib'); z.unzipSync(body);'`
- `'import zlib from 'zlib'; zlib.inflateRawSync(body);'`
- `'import * as zlib from 'node:zlib'; zlib.zstdDecompressSync(body);'`
- `'import { gunzipSync as inflateIt } from 'zlib'; inflateIt(body);'`
- `'import { 'gunzipSync' as inflateIt } from 'zlib'; inflateIt(body);'`
- `'const { gunzip: gz } = require('zlib'); gz(body, cb);'`
- `'${REQUIRE}zlib.gunzip(body, { chunkSize: 4096 }, cb);'`
- `'${REQUIRE}zlib.gunzipSync(body, { 'chunkSize': 4096 });'`
- `'${REQUIRE}zlib.gunzipSync(body, { [key]: 4096 });'`
- `'function f(body) { return zlib.gunzipSync(body); }\n${REQUIRE}'`
- `'${REQUIRE}zlib.gunzip(body, cb);'`

## False-positive guards — what it must NOT flag

14 asserted case(s). A rule with few of these has not been argued with.

- `'${REQUIRE}zlib.gunzip(reqBody, { maxOutputLength: MAX }, cb);'`
- `'${REQUIRE}zlib.gunzip(body, cb);'`
- `'${REQUIRE}zlib.gunzip(reqBody, (err, buf) => cb(err, buf));'`
- `'const z = require('node:zlib'); z.unzipSync(body);'`
- `'import zlib from 'zlib'; zlib.inflateRawSync(body);'`
- `'import * as zlib from 'node:zlib'; zlib.zstdDecompressSync(body);'`
- `'import { gunzipSync as inflateIt } from 'zlib'; inflateIt(body);'`
- `'import { 'gunzipSync' as inflateIt } from 'zlib'; inflateIt(body);'`
- `'const { gunzip: gz } = require('zlib'); gz(body, cb);'`
- `'${REQUIRE}zlib.gunzip(body, { chunkSize: 4096 }, cb);'`
- `'${REQUIRE}zlib.gunzipSync(body, { 'chunkSize': 4096 });'`
- `'${REQUIRE}zlib.gunzipSync(body, { [key]: 4096 });'`
- `'function f(body) { return zlib.gunzipSync(body); }\n${REQUIRE}'`
- `'${REQUIRE}zlib.gunzip(body, cb);'`

## Not covered

Corpus: 1 vulnerable, 1 safe fixtures under `CWE-409`.

