# `secure-coding/no-electron-security-issues`

**CWE-16** · severity CRITICAL · 21 catch cases · 51 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: allowInDev, safePreloadPatterns, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: electronSecurityIssue, enableSecurityFeatures, useContextIsolation, securePreloadScripts, strategySecureDefaults, strategyProcessSeparation, strategyInputValidation — dead metadata, or a report path that was lost.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-16/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-electron-security-issues
Detects Electron security vulnerabilities (CWE-16)

Electron applications can be vulnerable to security issues when not properly
configured. This rule detects insecure Electron configurations and patterns
that could allow privilege escalation, code execution, or data leakage.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe Electron configurations
- Development vs production environments
- JSDoc annotations (@electron-safe, @dev-only)
- Trusted Electron security patterns

## True positives — what it catches

13 asserted case(s). These are the claims; each is only as good as the case.

- `'new BrowserWindow({ nodeIntegration: true });'`
- `'const win = new BrowserWindow({ contextIsolation: false });'`
- `'new BrowserWindow({ webSecurity: false });'`
- `'new BrowserWindow({ allowRunningInsecureContent: true });'`
- `'new BrowserWindow({ sandbox: false });'`
- `'const fs = require("fs");'`
- Was `view.js`. A bare `view.js` / `ui.js` basename is generic — a Vue view, an MVC view — so matching it is the same over-matching that made this rule fire on any path containing the letters "ui". `renderer.*` and `preload.*` remain matched as basenames because those ARE unambiguous Electron conventions; `view` and `ui` are only matched as directory segments. — `'const { exec } = require("child_process");'`
- `'win.webContents.preload = "node_modules/evil.js";'`
- `'win.webContents.preload = "https://evil.com/script.js";'`
- `'ipcRenderer.send("untrusted-channel", data);'`
- `'ipcMain.handle("dangerous", async (event, arg) => { return sensitiveData; });'`
- `'ipcRenderer.send("untrusted-channel", data);'`
- `'process.exit(1);'`

## False-positive guards — what it must NOT flag

33 asserted case(s). A rule with few of these has not been argued with.

- `'new BrowserWindow({ contextIsolation: true, nodeIntegration: false });'`
- `'const win = new BrowserWindow({ webSecurity: true, sandbox: true });'`
- `'ipcRenderer.send("safe-channel", data);'`
- `'win.loadFile("app/index.html", { preload: "preload.js" });'`
- `'new BrowserWindow({ nodeIntegration: true });'`
- `'const win = new BrowserWindow({ contextIsolation: false });'`
- `'new BrowserWindow({ webSecurity: false });'`
- `'new BrowserWindow({ allowRunningInsecureContent: true });'`
- `'new BrowserWindow({ sandbox: false });'`
- `'const fs = require("fs");'`
- Was `view.js`. A bare `view.js` / `ui.js` basename is generic — a Vue view, an MVC view — so matching it is the same over-matching that made this rule fire on any path containing the letters "ui". `renderer.*` and `preload.*` remain matched as basenames because those ARE unambiguous Electron conventions; `view` and `ui` are only matched as directory segments. — `'const { exec } = require("child_process");'`
- `'win.webContents.preload = "node_modules/evil.js";'`
- `'win.webContents.preload = "https://evil.com/script.js";'`
- `'ipcRenderer.send("untrusted-channel", data);'`
- `'ipcMain.handle("dangerous", async (event, arg) => { return sensitiveData; });'`
- `'ipcRenderer.send("allowed-channel", data);'`
- `'const { contextBridge } = require("electron");'`
- `'require("fs");'`
- `'ipcRenderer.send("trusted-channel", data);'`
- `'ipcRenderer.send("untrusted-channel", data);'`
- `'const extra = { nodeIntegration: false }; new BrowserWindow({ ...extra });'`
- `'const flag = computeFlag(); new BrowserWindow({ nodeIntegration: flag });'`
- `'new BrowserWindow();'`
- `'new BrowserWindow(existingOptions);'`
- `'new SomeOtherClass({ nodeIntegration: true });'`
- `'ipcRenderer.send();'`
- `'ipcRenderer.send(channelVar, data);'`
- `'require(moduleName);'`
- `'process.exit(1);'`
- `'win.webContents.other = "node_modules/evil.js";'`
- `'win.webContents.preload = preloadPathVar;'`
- `'win.webContents.preload = "./preload.js";'`
- `'const opts = { webPreferences: existingPreferences };'`

## Not covered

No `benchmarks/corpus/CWE-16/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

