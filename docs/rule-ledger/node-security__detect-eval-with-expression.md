# `node-security/detect-eval-with-expression`

**CWE-95** · severity CRITICAL · 65 catch cases · 68 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: evalWithExpression, useFunctionConstructor, useSaferAlternative — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: strategyMessageId.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-95/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-unsafe-dynamic-require — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.
- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: detect-eval-with-expression
Detects eval(variable) which can allow an attacker to run arbitrary code
LLM-optimized with comprehensive fix guidance and security context

@see https://owasp.org/www-community/attacks/Code_Injection
@see https://cwe.mitre.org/data/definitions/95.html

## True positives — what it catches

59 asserted case(s). These are the claims; each is only as good as the case.

- `'eval(userInput);'`
- `'eval('code: ${value}');'`
- `'const result = eval(expression);'`
- `'const runner = (code) => eval(code);'`
- `'eval(a || b);'`
- `'(eval)(code);'`
- `'eval("JSON.parse(" + jsonString + ")");'`
- `'eval("obj[" + key + "]");'`
- `'eval("object[" + key + "]");'`
- `'eval("obj." + property);'`
- `'eval("someOtherPattern" + value);'`
- `'eval("customPattern" + data);'`
- `'eval(\'Math.sin(\' + angle + \')\');'`
- `'eval("parseInt(" + value + ")");'`
- `'eval("parseFloat(" + num + ")");'`
- `'eval("${" + name + "}");'`
- `'eval("template " + variable);'`
- `'eval("interpolat" + value);'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(variable);'`
- `'eval();'`
- `'Function(code);'`
- `'Function("arg1", "arg2", "return arg1 + arg2");'`
- `'(new Function)(code);'`
- `'(new Function)("arg1", "return arg1 * 2");'`
- `'new Function(code);'`
- `'new Function("arg1", "arg2", "return arg1 + arg2");'`
- `'const fn = new Function(userCode);'`
- `'const fn = new Function("x", "y", "return x + y");'`
- `'customEval(userCode);'`
- `'myEval(code);'`
- `'const vm = require('vm');`
- `'const { NodeVM } = require('vm2');`
- `'const vm = require('node:vm'); vm.runInThisContext(userCode);'`
- `'const vm = require('vm'); vm.runInContext(userCode, ctx);'`
- `'const vm = require('vm'); vm.compileFunction(userCode);'`
- `'const vm = require('vm'); new vm.Script(userCode);'`
- `'const vm = require('vm'); new vm.SourceTextModule(userCode);'`
- `'const { runInNewContext } = require('vm'); runInNewContext(userCode);'`
- `'import { runInNewContext } from 'vm'; runInNewContext(userCode);'`
- `'import * as vm from 'vm'; vm.runInThisContext(userCode);'`
- `'const { runInNewContext: run } = require('vm'); run(userCode);'`
- `'function go(userCode) { return vm.runInThisContext(userCode); }`
- `'const { NodeVM } = require('vm2'); new NodeVM().run(userCode);'`
- `'const vm2 = require('vm2'); const s = new vm2.VM(); s.run(userCode);'`
- `'import { VMScript } from 'vm2'; new VMScript(userCode);'`
- `'const { NodeVM: Sandbox } = require('vm2'); const s = new Sandbox(); s.run(userCode);'`
- `'const vm = require('vm'); vm.runInThisContext(SCRIPT);'`
- `'const vm = require('vm'); let s = 'x = 1'; s = req.body.code; vm.runInThisContext(s);'`
- `'const vm = require('vm'); const s = buildScript(); vm.runInThisContext(s);'`
- `'const vm = require('vm'); let s; s = 'x = 1'; vm.runInThisContext(s);'`
- `'const vm = require('vm'); function go(s) { vm.runInThisContext(s); }'`
- `'const vm = require('vm'); function go(s) { s = 'x = 1'; vm.runInThisContext(s); }'`
- `'const vm = require('vm'); vm.runInThisContext(SCRIPT);'`
- `'const vm = require('vm'); vm.runInThisContext(42);'`
- `'const vm = require("vm"); vm.runInThisContext('total = ${expr}');'`

## False-positive guards — what it must NOT flag

61 asserted case(s). A rule with few of these has not been argued with.

- `'eval(userInput);'`
- `'eval('code: ${value}');'`
- `'const result = eval(expression);'`
- `'const runner = (code) => eval(code);'`
- `'eval(a || b);'`
- `'(eval)(code);'`
- `'eval("JSON.parse(" + jsonString + ")");'`
- `'eval("obj[" + key + "]");'`
- `'eval("object[" + key + "]");'`
- `'eval("obj." + property);'`
- `'eval("someOtherPattern" + value);'`
- `'eval("customPattern" + data);'`
- `'eval(\'Math.sin(\' + angle + \')\');'`
- `'eval("parseInt(" + value + ")");'`
- `'eval("parseFloat(" + num + ")");'`
- `'eval("${" + name + "}");'`
- `'eval("template " + variable);'`
- `'eval("interpolat" + value);'`
- `'eval("literal string");'`
- `'eval(\'another literal\');'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(userInput);'`
- `'eval(variable);'`
- `'eval();'`
- `'Function(code);'`
- `'Function("arg1", "arg2", "return arg1 + arg2");'`
- `'(new Function)(code);'`
- `'(new Function)("arg1", "return arg1 * 2");'`
- `'new Function(code);'`
- `'new Function("arg1", "arg2", "return arg1 + arg2");'`
- `'const fn = new Function(userCode);'`
- `'const fn = new Function("x", "y", "return x + y");'`
- `'customEval(userCode);'`
- `'myEval(code);'`
- `'const vm = require('vm');`
- `'const { NodeVM } = require('vm2');`
- `'const vm = require('node:vm'); vm.runInThisContext(userCode);'`
- `'const vm = require('vm'); vm.runInContext(userCode, ctx);'`
- `'const vm = require('vm'); vm.compileFunction(userCode);'`
- `'const vm = require('vm'); new vm.Script(userCode);'`
- `'const vm = require('vm'); new vm.SourceTextModule(userCode);'`
- `'const { runInNewContext } = require('vm'); runInNewContext(userCode);'`
- `'import { runInNewContext } from 'vm'; runInNewContext(userCode);'`
- `'import * as vm from 'vm'; vm.runInThisContext(userCode);'`
- `'const { runInNewContext: run } = require('vm'); run(userCode);'`
- `'function go(userCode) { return vm.runInThisContext(userCode); }`
- `'const { NodeVM } = require('vm2'); new NodeVM().run(userCode);'`
- `'const vm2 = require('vm2'); const s = new vm2.VM(); s.run(userCode);'`
- `'import { VMScript } from 'vm2'; new VMScript(userCode);'`
- `'const { NodeVM: Sandbox } = require('vm2'); const s = new Sandbox(); s.run(userCode);'`
- `'const vm = require('vm'); vm.runInThisContext(SCRIPT);'`
- `'const vm = require('vm'); let s = 'x = 1'; s = req.body.code; vm.runInThisContext(s);'`
- `'const vm = require('vm'); const s = buildScript(); vm.runInThisContext(s);'`
- `'const vm = require('vm'); let s; s = 'x = 1'; vm.runInThisContext(s);'`
- `'const vm = require('vm'); function go(s) { vm.runInThisContext(s); }'`
- `'const vm = require('vm'); function go(s) { s = 'x = 1'; vm.runInThisContext(s); }'`
- `'const vm = require('vm'); vm.runInThisContext(SCRIPT);'`
- `'const vm = require('vm'); vm.runInThisContext(42);'`
- `'const vm = require("vm"); vm.runInThisContext('total = ${expr}');'`

## Not covered

No `benchmarks/corpus/CWE-95/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

