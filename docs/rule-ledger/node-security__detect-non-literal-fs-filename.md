# `node-security/detect-non-literal-fs-filename`

**CWE-22** · severity LOW · 67 catch cases · 113 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: taintSources, additionalMethods, allowedExtensions — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: validatePath, usePathResolve, whitelistExtensions, useBasename, createSafeDir.
- **`inert-suggestion`** (metadata-contract) — 8 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-22/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-arbitrary-file-access, no-zip-slip — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: detect-non-literal-fs-filename
Detects variable in filename argument of fs calls, which might allow an attacker to access anything on your system
LLM-optimized with comprehensive path traversal prevention guidance

@see https://owasp.org/www-community/attacks/Path_Traversal
@see https://cwe.mitre.org/data/definitions/22.html

## True positives — what it catches

22 asserted case(s). These are the claims; each is only as good as the case.

- `"const p = process.argv[2];\nfs.readFile(path.join('/uploads', p));"`
- `"fs.readFileSync('/etc/app/' + process.env.NAME);"`
- `'fs.readFileSync('${BASE}/${process.env.NAME}');'`
- `"fs.readFile('../../etc/passwd');"`
- `'fs.readFile(userPath, callback);'`
- Free variable inside a template interpolation. — `"const fs = require('fs'); fs.readFile('template with ${filename}');"`
- Free variable as a call argument. — `"import fs from 'fs'; import path from 'path'; const key = fs.readFileSync(path.resolve(__`
- The free variable is still caught when it is genuinely free. — `"const fs = require('fs'); const path = require('path'); fs.readFileSync(path.resolve(__di`
- a named import from node:fs/promises — `"import { readFile } from 'node:fs/promises';\nreadFile(userPath);"`
- a renamed named import still resolves to its fs method — `"import { readFile as read } from 'fs/promises';\nread(userPath);"`
- Parity with the import path, which already reads the string form. a string-literal destructuring key resolves the same method — `"const { 'readFile': read } = require('fs');\nread(userPath);"`
- the string-literal import name resolves the same method — `"import { 'readFile' as read } from 'fs/promises';\nread(userPath);"`
- a renamed default import — `"import nodeFs from 'node:fs';\nnodeFs.readFileSync(userPath);"`
- a namespace import — `"import * as fileSystem from 'fs';\nfileSystem.writeFile(userPath, data);"`
- fs.promises off a renamed binding — `"import nodeFs from 'fs';\nnodeFs.promises.readFile(userPath);"`
- a renamed require — `"const nodeFs = require('node:fs');\nnodeFs.unlink(userPath);"`
- a destructured require — `"const { readdir } = require('fs');\nreaddir(userDir);"`
- a renamed destructured require keeps the fs method name — `"const { readFile: rf } = require('node:fs/promises');\nrf(userPath);"`
- `promises` is the one fs export that is itself a module object. Filed under methods it was unresolvable, so the whole promise API reached through this idiom was silently unchecked. a destructured promises object binds a namespace, not a method — `"const { promises } = require('fs');\npromises.readFile(userPath);"`
- the same through a named import — `"import { promises as fsp } from 'node:fs';\nfsp.writeFile(userPath, data);"`
- the bare fs identifier still reports with no import at all — `'fs.readFile(userPath);'`
- Judging in visit order would miss this: the binding is established after the call site. Statement order is not a security property. a require below the call site still binds — `"function read(p) { return nodeFs.readFileSync(p); }\nconst nodeFs = require('fs');"`

## False-positive guards — what it must NOT flag

52 asserted case(s). A rule with few of these has not been argued with.

- `[`
- `[`
- `[`
- `[`
- `'fs.readFile("/path/to/file.txt", callback);'`
- `'fs.writeFile("./config.json", data, callback);'`
- `'fs.stat("/var/log/app.log", callback);'`
- `'fs.readdir("./src", callback);'`
- `'myFunction.readFile(filename);'`
- `'obj.readFile(userPath);'`
- `'fs.readFileSync(path.join(__dirname, 'data', 'users.json'));'`
- `"const p = process.argv[2];\nfs.readFile(path.join('/uploads', p));"`
- `"fs.readFileSync('/etc/app/' + process.env.NAME);"`
- `'fs.readFileSync('${BASE}/${process.env.NAME}');'`
- `"fs.readFile('../../etc/passwd');"`
- `"fs.readFile('../../etc/passwd');"`
- `'fs.readFile("/path/to/file.txt", callback);'`
- `'fs.readFile("/path/to/file.txt", callback);'`
- `'fs.readFile(userPath, callback);'`
- `'fs.constants.F_OK;'`
- `'fs.createReadStream("/safe/path");'`
- Free variable inside a template interpolation. — `"const fs = require('fs'); fs.readFile('template with ${filename}');"`
- Free variable as a call argument. — `"import fs from 'fs'; import path from 'path'; const key = fs.readFileSync(path.resolve(__`
- The free variable is still caught when it is genuinely free. — `"const fs = require('fs'); const path = require('path'); fs.readFileSync(path.resolve(__di`
- a same-named binding from an unrelated module is not fs — `"import { readFile } from './my-utils';\nreadFile(userPath);"`
- a bare call with no fs binding in the file — `'readFile(userPath);'`
- a require of something else does not bind fs — `"const fsx = require('path');\nfsx.readFile(userPath);"`
- a non-dangerous method on a resolved namespace — `"import fsp from 'node:fs/promises';\nfsp.realpath(userPath);"`
- a literal path through a resolved named import — `"import { readFile } from 'node:fs/promises';\nreadFile('./config.json');"`
- a computed member on the module object is not resolved — `"import fs2 from 'fs';\nfs2[method](userPath);"`
- a non-promises member chain is not an fs sink — `"import fs2 from 'fs';\nfs2.constants.readFile(userPath);"`
- require with no arguments is not an fs require — `'const fs3 = require();\nfs3.readFile(userPath);'`
- a destructured require of a non-fs module — `"const { readFile } = require('./local');\nreadFile(userPath);"`
- A numeric key is not an fs method name; better silent than guessing. a numeric destructuring key binds no method name — `"const { 0: readFile } = require('fs');\nreadFile(userPath);"`
- a computed destructuring key binds no method name — `"const { [k]: readFile } = require('fs');\nreadFile(userPath);"`
- a nested destructuring pattern is not a method binding — `"const { promises: { readFile } } = require('fs');\nreadFile(userPath);"`
- a declarator with no initialiser — `'let fsLater;\nreadFile(userPath);'`
- fs has no array shape, so an array pattern binds nothing this rule can name — better silent than guessing which element is which. an array-pattern require binds no fs name — `"const [readFile] = require('fs');\nreadFile(userPath);"`
- a named import from node:fs/promises — `"import { readFile } from 'node:fs/promises';\nreadFile(userPath);"`
- a renamed named import still resolves to its fs method — `"import { readFile as read } from 'fs/promises';\nread(userPath);"`
- Parity with the import path, which already reads the string form. a string-literal destructuring key resolves the same method — `"const { 'readFile': read } = require('fs');\nread(userPath);"`
- the string-literal import name resolves the same method — `"import { 'readFile' as read } from 'fs/promises';\nread(userPath);"`
- a renamed default import — `"import nodeFs from 'node:fs';\nnodeFs.readFileSync(userPath);"`
- a namespace import — `"import * as fileSystem from 'fs';\nfileSystem.writeFile(userPath, data);"`
- fs.promises off a renamed binding — `"import nodeFs from 'fs';\nnodeFs.promises.readFile(userPath);"`
- a renamed require — `"const nodeFs = require('node:fs');\nnodeFs.unlink(userPath);"`
- a destructured require — `"const { readdir } = require('fs');\nreaddir(userDir);"`
- a renamed destructured require keeps the fs method name — `"const { readFile: rf } = require('node:fs/promises');\nrf(userPath);"`
- `promises` is the one fs export that is itself a module object. Filed under methods it was unresolvable, so the whole promise API reached through this idiom was silently unchecked. a destructured promises object binds a namespace, not a method — `"const { promises } = require('fs');\npromises.readFile(userPath);"`
- the same through a named import — `"import { promises as fsp } from 'node:fs';\nfsp.writeFile(userPath, data);"`
- the bare fs identifier still reports with no import at all — `'fs.readFile(userPath);'`
- Judging in visit order would miss this: the binding is established after the call site. Statement order is not a security property. a require below the call site still binds — `"function read(p) { return nodeFs.readFileSync(p); }\nconst nodeFs = require('fs');"`

## Not covered

No `benchmarks/corpus/CWE-22/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

