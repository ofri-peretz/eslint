# `node-security/no-math-random-crypto`

**CWE-338** · severity CRITICAL · 12 catch cases · 27 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useRandomBytes, useRandomUUID.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-338/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-math-random-crypto
Detects Math.random() used in cryptographic contexts
CWE-338: Use of Cryptographically Weak Pseudo-Random Number Generator

Math.random() is not cryptographically secure and should never be used
for tokens, keys, IVs, salts, or any security-sensitive random values.

@see https://cwe.mitre.org/data/definitions/338.html

## True positives — what it catches

9 asserted case(s). These are the claims; each is only as good as the case.

- `'function generateToken() { const x = Math.random(); }'`
- `'session.token = Math.random();'`
- `'const gen = function generateToken() { return Math.random(); };'`
- `'const token = Math.random().toString(36);'`
- `'const sessionSecret = Math.random();'`
- `'export function vuln_random_token() {`
- `'export function vuln_random_session() {`
- `'const config = { apiKey: Math.random().toString(36) };'`
- `'export function genRandomString(length) {`

## False-positive guards — what it must NOT flag

11 asserted case(s). A rule with few of these has not been argued with.

- `'function generateToken() { const x = Math.random(); }'`
- `'session.token = Math.random();'`
- `'const gen = function generateToken() { return Math.random(); };'`
- `'export function safe_random_shuffle(array) {`
- `'const secret = Math.random();'`
- `'const token = Math.random().toString(36);'`
- `'const sessionSecret = Math.random();'`
- `'export function vuln_random_token() {`
- `'export function vuln_random_session() {`
- `'const config = { apiKey: Math.random().toString(36) };'`
- `'export function genRandomString(length) {`

## Not covered

No `benchmarks/corpus/CWE-338/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

