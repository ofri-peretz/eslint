# `browser-security/no-unencrypted-transmission`

**CWE-319** · severity HIGH · 10 catch cases · 18 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-319/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: no-unencrypted-transmission
Detects unencrypted data transmission (HTTP vs HTTPS, plain text protocols)
CWE-319: Cleartext Transmission of Sensitive Information

@see https://cwe.mitre.org/data/definitions/319.html
@see https://owasp.org/www-community/vulnerabilities/Insecure_Transport

## True positives — what it catches

10 asserted case(s). These are the claims; each is only as good as the case.

- The literal is the RECEIVER, not an argument — an http:// URL written into source, which is the thing this rule is for. — `'const ok = 'http://legacy.example.com'.startsWith(prefix);'`
- The replacement argument is content being written, not a search operand — an insecure destination that must still report. — `'const fixed = url.replace(/^https:/, 'http://legacy.example.com');'`
- `'const url = "http://api.example.com";'`
- `'fetch("http://api.example.com/data");'`
- `'const ws = new WebSocket("ws://acmecorp.io");'`
- `'const db = "mongodb://user:pass@localhost:27017";'`
- `'const redis = "redis://localhost:6379";'`
- `'const url = 'http://${host}/api';'`
- `'const url = "http://staging.example.com";'`
- `'const smtp = "smtp://mail.example.com";'`

## False-positive guards — what it must NOT flag

18 asserted case(s). A rule with few of these has not been argued with.

- `'const url = "https://api.example.com";'`
- `'fetch("https://api.example.com/data");'`
- `'const ws = new WebSocket("wss://acmecorp.io");'`
- `'const db = "mongodb+srv://user:pass@cluster.mongodb.net";'`
- `'const redis = "rediss://localhost:6379";'`
- `'const url = "http://localhost:3000";'`
- The literal is the RECEIVER, not an argument — an http:// URL written into source, which is the thing this rule is for. — `'const ok = 'http://legacy.example.com'.startsWith(prefix);'`
- The replacement argument is content being written, not a search operand — an insecure destination that must still report. — `'const fixed = url.replace(/^https:/, 'http://legacy.example.com');'`
- `'const url = "http://api.example.com";'`
- `'fetch("http://api.example.com/data");'`
- `'const ws = new WebSocket("ws://acmecorp.io");'`
- `'const db = "mongodb://user:pass@localhost:27017";'`
- `'const redis = "redis://localhost:6379";'`
- `'const url = 'http://${host}/api';'`
- `'const url = "http://staging.example.com";'`
- `'const url = "http://internal.example.com";'`
- `'const smtp = "smtp://mail.example.com";'`
- `'const url = 'http://${host}/api';'`

## Not covered

No `benchmarks/corpus/CWE-319/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

