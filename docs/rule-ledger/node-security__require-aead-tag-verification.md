# `node-security/require-aead-tag-verification`

**CWE-327** · severity HIGH · 5 catch cases · 25 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

No check fired. That means no KNOWN gap — it is not evidence of correctness.

## Why this rule exists

ESLint Rule: require-aead-tag-verification
Detects AEAD (GCM / CCM / OCB / ChaCha20-Poly1305) decryption that never
verifies the authentication tag.
CWE-327: Use of a Broken or Risky Cryptographic Algorithm

An AEAD mode is only authenticated if BOTH halves of the contract run:

  1. `decipher.setAuthTag(tag)` hands the tag to the cipher, and
  2. `decipher.final()` is what actually compares it and throws on mismatch.

Skip either one and Node happily returns attacker-chosen plaintext from
`update()`. The mode still *looks* authenticated — the algorithm string says
`aes-256-gcm` — which is precisely why no algorithm-name rule catches it.

@see https://cwe.mitre.org/data/definitions/327.html
@see https://nodejs.org/api/crypto.html#decipher_setauthtagbuffer

## True positives — what it catches

1 asserted case(s). These are the claims; each is only as good as the case.

- `'const d = crypto.createDecipheriv('aes-256-gcm', key, iv); d.update(ct);'`

## False-positive guards — what it must NOT flag

2 asserted case(s). A rule with few of these has not been argued with.

- `'const d = crypto.createDecipheriv('aes-256-gcm', key, iv); d.update(ct);'`
- `'const d = crypto.createDecipheriv('aes-256-gcm', key, iv); d.update(ct);'`

## Not covered

Corpus: 4 vulnerable, 4 safe fixtures under `CWE-327`.

