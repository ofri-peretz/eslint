# `browser-security/no-disabled-certificate-validation`

**CWE-295** · severity CRITICAL · 12 catch cases · 25 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-allow-arbitrary-loads — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

@fileoverview Prevent disabled SSL/TLS certificate validation

## True positives — what it catches

7 asserted case(s). These are the claims; each is only as good as the case.

- `"tls.connect({ checkServerIdentity: () => null })"`
- `"tls.connect({ checkServerIdentity: () => void 0 })"`
- `"tls.connect({ checkServerIdentity: () => {} })"`
- `"tls.connect({ checkServerIdentity: function () { return; } })"`
- `"tls.connect({ checkServerIdentity: function (h, c) { log(h); return undefined; } })"`
- `"tls.connect({ checkServerIdentity: (h, c) => { return true; } })"`
- `"tls.connect({ checkServerIdentity: () => { emit({ level: 'debug' }); } })"`

## False-positive guards — what it must NOT flag

10 asserted case(s). A rule with few of these has not been argued with.

- `"https.request({ host, path: '/secret', method: 'GET' }, onRes)"`
- `"tls.connect({ checkServerIdentity: (h, c) => tls.checkServerIdentity(h, c) })"`
- `"tls.connect({ checkServerIdentity: function (h, c) { throw new Error('nope'); } })"`
- `"tls.connect({ checkServerIdentity: () => null })"`
- `"tls.connect({ checkServerIdentity: () => void 0 })"`
- `"tls.connect({ checkServerIdentity: () => {} })"`
- `"tls.connect({ checkServerIdentity: function () { return; } })"`
- `"tls.connect({ checkServerIdentity: function (h, c) { log(h); return undefined; } })"`
- `"tls.connect({ checkServerIdentity: (h, c) => { return true; } })"`
- `"tls.connect({ checkServerIdentity: () => { emit({ level: 'debug' }); } })"`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-295`.

