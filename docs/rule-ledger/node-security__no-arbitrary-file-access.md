# `node-security/no-arbitrary-file-access`

**CWE-22** · severity HIGH · 32 catch cases · 53 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-22/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: detect-non-literal-fs-filename, no-zip-slip — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

@fileoverview Prevent file access from user input

False Positive Reduction:
This rule detects safe patterns including:
- path.basename() sanitization
- path.join() with validated base directories
- startsWith() validation guards
- Early-return throw patterns

## True positives — what it catches

27 asserted case(s). These are the claims; each is only as good as the case.

- `[`
- `[`
- `[`
- `[`
- `[`
- `"function read(filePath) { fs.readFileSync(filePath); } read(req.query.f);"`
- `"function read(userFile, cb) { fs.readFile(userFile, cb); } read(req.params.name, cb);"`
- `"function read(userDir) { fs.readdir(userDir); } read(body.dir);"`
- `"function read(scanPath) { fs.readdirSync(scanPath); } read(query.dir);"`
- `"function read(targetPath) { fs.stat(targetPath); } read(request.path);"`
- `"function read(checkPath) { fs.statSync(checkPath); } read(params.p);"`
- `"function write(outputPath, data, cb) { fs.writeFile(outputPath, data, cb); } write(req.bo`
- `"function write(destPath, content) { fs.writeFileSync(destPath, content); } write(req.quer`
- `"function write(logPath, text, cb) { fs.appendFile(logPath, text, cb); } write(req.query.l`
- `"function write(filePath, data) { fs.appendFileSync(filePath, data); } write(req.body.f, d`
- `"function read(p) { fs.readFileSync(p); }\napp.get('/f', (req, res) => read(req.query.name`
- `"const p = req.query.file; fs.readFileSync(p);"`
- `"fs.readFileSync(req);"`
- `"const read = function (userPath) { fs.readFileSync(userPath); }; read(req.query.f);"`
- `"const read = (userPath) => fs.readFileSync(userPath); read(req.query.f);"`
- `"const handlers = { read(userPath) { fs.readFileSync(userPath); } }; handlers.read(req.que`
- `"let read; read = (userPath) => { fs.readFileSync(userPath); }; read(req.query.f);"`
- `"const read = function inner(userPath) { fs.readFileSync(userPath); }; inner(req.query.f);`
- `"const userFile = req.query.file;\nconst content = fs.readFileSync(path.join('/uploads', u`
- `"const userFile = req.params.filename;\nconst content = fs.readFileSync('/uploads/' + user`
- `'const userFile = req.body.name;\nfs.readFileSync('/uploads/${userFile}');'`
- `"fs.readFile(req.body.upload.path, cb)"`

## False-positive guards — what it must NOT flag

30 asserted case(s). A rule with few of these has not been argued with.

- `[`
- `[`
- `[`
- `[`
- `[`
- `[`
- `[`
- `[`
- `"function read(filePath) { fs.readFileSync(filePath); } read(req.query.f);"`
- `"function read(userFile, cb) { fs.readFile(userFile, cb); } read(req.params.name, cb);"`
- `"function read(userDir) { fs.readdir(userDir); } read(body.dir);"`
- `"function read(scanPath) { fs.readdirSync(scanPath); } read(query.dir);"`
- `"function read(targetPath) { fs.stat(targetPath); } read(request.path);"`
- `"function read(checkPath) { fs.statSync(checkPath); } read(params.p);"`
- `"function write(outputPath, data, cb) { fs.writeFile(outputPath, data, cb); } write(req.bo`
- `"function write(destPath, content) { fs.writeFileSync(destPath, content); } write(req.quer`
- `"function write(logPath, text, cb) { fs.appendFile(logPath, text, cb); } write(req.query.l`
- `"function write(filePath, data) { fs.appendFileSync(filePath, data); } write(req.body.f, d`
- `"function read(p) { fs.readFileSync(p); }\napp.get('/f', (req, res) => read(req.query.name`
- `"const p = req.query.file; fs.readFileSync(p);"`
- `"fs.readFileSync(req);"`
- `"const read = function (userPath) { fs.readFileSync(userPath); }; read(req.query.f);"`
- `"const read = (userPath) => fs.readFileSync(userPath); read(req.query.f);"`
- `"const handlers = { read(userPath) { fs.readFileSync(userPath); } }; handlers.read(req.que`
- `"let read; read = (userPath) => { fs.readFileSync(userPath); }; read(req.query.f);"`
- `"const read = function inner(userPath) { fs.readFileSync(userPath); }; inner(req.query.f);`
- `"const userFile = req.query.file;\nconst content = fs.readFileSync(path.join('/uploads', u`
- `"const userFile = req.params.filename;\nconst content = fs.readFileSync('/uploads/' + user`
- `'const userFile = req.body.name;\nfs.readFileSync('/uploads/${userFile}');'`
- `"fs.readFile(req.body.upload.path, cb)"`

## Not covered

No `benchmarks/corpus/CWE-22/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

