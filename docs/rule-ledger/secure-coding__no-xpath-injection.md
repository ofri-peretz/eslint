# `secure-coding/no-xpath-injection`

**CWE-643** · severity HIGH · 44 catch cases · 228 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

5 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: escapeXpathInput, validateXpathQueries, strategyParameterizedQueries, strategyInputValidation, strategySafeConstruction — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useParameterizedXpath.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-643/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`unguarded-recursion`** (performance) — reachesXpathSink() recurses with no visited-set or depth bound — a cyclic binding chain overflows the stack. **Probe:** lint `let a = b; let b = a;` or a self-referential type and watch for RangeError.

## Why this rule exists

ESLint Rule: no-xpath-injection
Detects XPath injection vulnerabilities (CWE-643)

XPath injection occurs when user input is improperly inserted into XPath
queries, allowing attackers to:
- Access unauthorized XML nodes and data
- Extract sensitive information from XML documents
- Perform XPath-based attacks and data exfiltration
- Bypass authentication or authorization checks

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe XPath construction methods
- Input validation and sanitization
- JSDoc annotations (@xpath-safe, @validated)
- Trusted XPath libraries

## True positives — what it catches

32 asserted case(s). These are the claims; each is only as good as the case.

- Template quasis are string content too — same expression, backticks. — `"function q(req) { const x = '//user[name='' + req.query.n + '']'; doc.evaluate(x); }"`
- `'const xpath = "/users/user[name=\'" + userInput + "\']";'`
- `'const query = '/users/user[@id="${userId}"]';'`
- `'document.evaluate(userQuery, document);'`
- `'const xpath = "//users/user/..";'`
- `'const query = "/root/*";'`
- `'const xpathWithWildcard = "//user[*]";'`
- `'xmlDoc.selectNodes(userSearchQuery);'`
- `'const xpathVar = userInput; document.evaluate(xpathVar, document);'`
- `'const userXPath = "//users/user/..";'`
- `'const xpathQuery = req.params.query; doc.evaluate(xpathQuery, doc);'`
- `'let searchPath = userInput; xpath.select(searchPath, doc);'`
- The asymmetry itself, pinned: a predicate step with no `//`. Caught by the concatenation handler all along; the template handler used to be blind to it. — `'const xpath = '/root/node[${req.query.id}]'; doc.evaluate(xpath);'`
- `'select("//user")'`
- `'document.evaluate("//user[@id=" + req.id + "]", document);'`
- `'selectNodes("//user[@name=" + userInput + "]")'`
- `"const xpath = '//node[@id='a+b']'; document.evaluate(xpath, doc);"`
- `'const xpath = '//node[@id="a+b"]'; document.evaluate(xpath, doc);'`
- `'const xpath = '//users/..'; document.evaluate(xpath, doc);'`
- `'select('//users/..');'`
- `'document.evaluate('//users/..' + suffix, doc);'`
- `'function f({id}) { document.evaluate("//user[@name=" + id + "]", document); }'`
- `'const r = userInput + "//users/.."'`
- descendant axis with an interpolation — `'function f(userInput) { return '//user[@id="${userInput}"]'; }'`
- a named axis is still an axis — `'function f(userInput) { return '/root/child::user[@id="${userInput}"]'; }'`
- ancestor-or-self is an axis — `'function f(userInput) { return 'ancestor-or-self::user[${userInput}]'; }'`
- concatenated XPath predicate — `'const xpath = "/users/user[name='" + userInput + "']";'`
- a parent axis inside a real XPath literal still reports — `'const xpathQuery = "//users/user/..";'`
- `'const q = "/users/user[name='" + userInput + "']";'`
- `'const q = "//u[@id='" + req.params.id + "']"; console.log(q); xpath.select(q, doc);'`
- `'xpath.select("//user[@id='" + req.params.id + "']", doc);'`
- `'xpath.select("//user[@id='" + userId + "']", doc);'`

## False-positive guards — what it must NOT flag

125 asserted case(s). A rule with few of these has not been argued with.

- `'document.evaluate(expr, '//users/..');'`
- `'/** @xpath-safe */\nconst q = '//users/..';\ndocument.evaluate(q, doc);'`
- `'const o = { q: '//users/..' };'`
- `'const getEval = () => doc.evaluate; (getEval())('//users/..');'`
- `'const q = '//users/..'; console.log(q);'`
- `'const banner = '// Generated by bin/generate-version.js. Do not edit.';'`
- `'const ca = 'MIIDTzCCAjegAwIBAgIJAKSVpiDswLc//wMA0GCSqGSIb3DQEBBQUAMD4';'`
- `'const rel = '../lib/index.js';'`
- `'const result = document.evaluate("/users/user[@id=\'123\']", document);'`
- `'const safeId = validateId(userInput); const xpath = '/users/user[@id="${safeId}"]';'`
- `'const xpath = buildXPath("users", { id: "123" });'`
- `'const result = array.find(item => item.id === userInput);'`
- `'const xpath = '/users/user[@name="${escapeXPath(userName)}"]';'`
- Template quasis are string content too — same expression, backticks. — `"function q(req) { const x = '//user[name='' + req.query.n + '']'; doc.evaluate(x); }"`
- `'const xpath = "/users/user[name=\'" + userInput + "\']";'`
- `'const query = '/users/user[@id="${userId}"]';'`
- `'document.evaluate(userQuery, document);'`
- `'const xpath = "//users/user/..";'`
- `'const query = "/root/*";'`
- `'const xpathWithWildcard = "//user[*]";'`
- `'xmlDoc.selectNodes(userSearchQuery);'`
- `'const xpathVar = userInput; document.evaluate(xpathVar, document);'`
- `'const userXPath = "//users/user/..";'`
- `'const xpathQuery = req.params.query; doc.evaluate(xpathQuery, doc);'`
- `'let searchPath = userInput; xpath.select(searchPath, doc);'`
- `'const configPath = "/config/database";'`
- `'myXpathLib.evaluate(query, doc);'`
- `'const xpath = mySafeBuilder("users", userId);'`
- `'const xpath = unsafeBuilder(userId);'`
- The asymmetry itself, pinned: a predicate step with no `//`. Caught by the concatenation handler all along; the template handler used to be blind to it. — `'const xpath = '/root/node[${req.query.id}]'; doc.evaluate(xpath);'`
- `'select("//user")'`
- `'document.evaluate("//user[@id=" + req.id + "]", document);'`
- `'selectNodes("//user[@name=" + userInput + "]")'`
- `'/** @xpath-safe */`
- `'/** @xpath-safe */`
- `'/** @xpath-safe */`
- `"const xpath = '//node[@id='a+b']'; document.evaluate(xpath, doc);"`
- `'const xpath = '//node[@id="a+b"]'; document.evaluate(xpath, doc);'`
- `'const xpath = '//users/..'; document.evaluate(xpath, doc);'`
- `'select('//users/..');'`
- `'document.evaluate('//users/..' + suffix, doc);'`
- `'function f({id}) { document.evaluate("//user[@name=" + id + "]", document); }'`
- `'const r = userInput + "//users/.."'`
- a GID template is not a descendant axis — `'function gid(id) { return 'gid://shopify/BulkOperation/${id}'; }'`
- a GID template passed to a helper — `'function gid(id) { return encodeGid('gid://organization/ShopifyShop/${id}'); }'`
- a URL assembled from req.protocol — `'const parsedUrl = new URL(req.protocol + '://' + req.get('host') + req.originalUrl);'`
- a URL template with a dynamic scheme — `'function u(req) { return '${req.protocol}://${req.get("host")}${req.originalUrl}'; }'`
- a double-colon cache key is not an axis — `'function k(topic, uri, filter) { return '${topic}::${uri}::${filter}'; }'`
- a relative file path is not a parent axis — `'const OKTA_ENV_SCRIPT_PATH = '../env/index.js';'`
- descendant axis with an interpolation — `'function f(userInput) { return '//user[@id="${userInput}"]'; }'`
- a named axis is still an axis — `'function f(userInput) { return '/root/child::user[@id="${userInput}"]'; }'`
- ancestor-or-self is an axis — `'function f(userInput) { return 'ancestor-or-self::user[${userInput}]'; }'`
- concatenated XPath predicate — `'const xpath = "/users/user[name='" + userInput + "']";'`
- a parent axis inside a real XPath literal still reports — `'const xpathQuery = "//users/user/..";'`
- `'const q = "/users/user[name='" + userInput + "']";'`
- `'const q = "//u[@id='" + req.params.id + "']"; console.log(q); xpath.select(q, doc);'`
- `'xpath.select("//user[@id='" + req.params.id + "']", doc);'`
- `'xpath.select("//user[@id='" + userId + "']", doc);'`
- Template quasis are string content too — same expression, backticks. — `"function q(req) { const x = '//user[name='' + req.query.n + '']'; doc.evaluate(x); }"`
- `'const xpath = "/users/user[name=\'" + userInput + "\']";'`
- `'const query = '/users/user[@id="${userId}"]';'`
- `'document.evaluate(userQuery, document);'`
- `'const xpath = "//users/user/..";'`
- `'const query = "/root/*";'`
- `'const xpathWithWildcard = "//user[*]";'`
- `'xmlDoc.selectNodes(userSearchQuery);'`
- `'const xpathVar = userInput; document.evaluate(xpathVar, document);'`
- `'const userXPath = "//users/user/..";'`
- `'const xpathQuery = req.params.query; doc.evaluate(xpathQuery, doc);'`
- `'let searchPath = userInput; xpath.select(searchPath, doc);'`
- `'const configPath = "/config/database";'`
- `'myXpathLib.evaluate(query, doc);'`
- `'const xpath = mySafeBuilder("users", userId);'`
- `'const xpath = unsafeBuilder(userId);'`
- The asymmetry itself, pinned: a predicate step with no `//`. Caught by the concatenation handler all along; the template handler used to be blind to it. — `'const xpath = '/root/node[${req.query.id}]'; doc.evaluate(xpath);'`
- `'select("//user")'`
- `'document.evaluate("//user[@id=" + req.id + "]", document);'`
- `'selectNodes("//user[@name=" + userInput + "]")'`
- `'/** @xpath-safe */`
- `'/** @xpath-safe */`
- `'/** @xpath-safe */`
- `"const xpath = '//node[@id='a+b']'; document.evaluate(xpath, doc);"`
- `'const xpath = '//node[@id="a+b"]'; document.evaluate(xpath, doc);'`
- `'const xpath = '//users/..'; document.evaluate(xpath, doc);'`
- `'select('//users/..');'`
- `'document.evaluate('//users/..' + suffix, doc);'`
- `'function f({id}) { document.evaluate("//user[@name=" + id + "]", document); }'`
- `'const r = userInput + "//users/.."'`
- a GID template is not a descendant axis — `'function gid(id) { return 'gid://shopify/BulkOperation/${id}'; }'`
- a GID template passed to a helper — `'function gid(id) { return encodeGid('gid://organization/ShopifyShop/${id}'); }'`
- a URL assembled from req.protocol — `'const parsedUrl = new URL(req.protocol + '://' + req.get('host') + req.originalUrl);'`
- a URL template with a dynamic scheme — `'function u(req) { return '${req.protocol}://${req.get("host")}${req.originalUrl}'; }'`
- a double-colon cache key is not an axis — `'function k(topic, uri, filter) { return '${topic}::${uri}::${filter}'; }'`
- a relative file path is not a parent axis — `'const OKTA_ENV_SCRIPT_PATH = '../env/index.js';'`
- descendant axis with an interpolation — `'function f(userInput) { return '//user[@id="${userInput}"]'; }'`
- a named axis is still an axis — `'function f(userInput) { return '/root/child::user[@id="${userInput}"]'; }'`
- ancestor-or-self is an axis — `'function f(userInput) { return 'ancestor-or-self::user[${userInput}]'; }'`
- concatenated XPath predicate — `'const xpath = "/users/user[name='" + userInput + "']";'`
- a parent axis inside a real XPath literal still reports — `'const xpathQuery = "//users/user/..";'`
- `'const q = "/users/user[name='" + userInput + "']";'`
- `'const q = "//u[@id='" + req.params.id + "']"; console.log(q); xpath.select(q, doc);'`
- `'xpath.select("//user[@id='" + req.params.id + "']", doc);'`
- `'xpath.select("//user[@id='" + userId + "']", doc);'`
- `"const xpath = '//node[@id='a+b']'; document.evaluate(xpath, doc);"`
- `'const xpath = '//node[@id="a+b"]'; document.evaluate(xpath, doc);'`
- `'const xpath = '//users/..'; document.evaluate(xpath, doc);'`
- `'select('//users/..');'`
- `'document.evaluate('//users/..' + suffix, doc);'`
- `'function f({id}) { document.evaluate("//user[@name=" + id + "]", document); }'`
- `'const r = userInput + "//users/.."'`
- a GID template is not a descendant axis — `'function gid(id) { return 'gid://shopify/BulkOperation/${id}'; }'`
- a GID template passed to a helper — `'function gid(id) { return encodeGid('gid://organization/ShopifyShop/${id}'); }'`
- a URL assembled from req.protocol — `'const parsedUrl = new URL(req.protocol + '://' + req.get('host') + req.originalUrl);'`
- a URL template with a dynamic scheme — `'function u(req) { return '${req.protocol}://${req.get("host")}${req.originalUrl}'; }'`
- a double-colon cache key is not an axis — `'function k(topic, uri, filter) { return '${topic}::${uri}::${filter}'; }'`
- a relative file path is not a parent axis — `'const OKTA_ENV_SCRIPT_PATH = '../env/index.js';'`
- descendant axis with an interpolation — `'function f(userInput) { return '//user[@id="${userInput}"]'; }'`
- a named axis is still an axis — `'function f(userInput) { return '/root/child::user[@id="${userInput}"]'; }'`
- ancestor-or-self is an axis — `'function f(userInput) { return 'ancestor-or-self::user[${userInput}]'; }'`
- concatenated XPath predicate — `'const xpath = "/users/user[name='" + userInput + "']";'`
- a parent axis inside a real XPath literal still reports — `'const xpathQuery = "//users/user/..";'`
- `'const q = "/users/user[name='" + userInput + "']";'`
- `'const q = "//u[@id='" + req.params.id + "']"; console.log(q); xpath.select(q, doc);'`
- `'xpath.select("//user[@id='" + req.params.id + "']", doc);'`
- `'xpath.select("//user[@id='" + userId + "']", doc);'`

## Not covered

No `benchmarks/corpus/CWE-643/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

