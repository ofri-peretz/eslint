# `secure-coding/no-homoglyph-identifiers`

**CWE-1007** · severity MEDIUM · 10 catch cases · 23 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

No check fired. That means no KNOWN gap — it is not evidence of correctness.

## Why this rule exists

ESLint Rule: no-homoglyph-identifiers
Detects characters that a code reviewer cannot see, or cannot tell apart
from ASCII.
CWE-1007: Insufficient Visual Distinction of Homoglyphs Presented to User

Two different attacks, two separate detections:

  const adminRole = 'admin';
  const аdminRole = 'guest';   // Cyrillic 'а' (U+0430) — a *different* binding

  const ADMIN_GROUP = 'admin&lt;U+200B>';   // never equal to 'admin'

Both survive review because the diff looks correct. The rule's whole job is
to print the codepoint, since the character itself cannot be shown.

@see https://cwe.mitre.org/data/definitions/1007.html
@see https://trojansource.codes/

## True positives — what it catches

8 asserted case(s). These are the claims; each is only as good as the case.

- a private class field with a Cyrillic homoglyph — `'class Session { #${CYRILLIC_A}dmin = true; }'`
- a fullwidth uppercase letter — `'const ＡdminRole = 1;'`
- a fullwidth lowercase letter — `'const admiｎ = 1;'`
- a Greek omicron in a property name — `'config.passwοrd = secret;'`
- a bidi override inside a string (Trojan Source) — `'const note = 'admin${RLO}// safe';'`
- an invisible character in a template chunk — `'const greeting = \'hello${ZWSP}world\';'`
- a template chunk that starts with an invisible character — `'const greeting = \'${ZWSP}world\';'`
- a template chunk that ends with an invisible character — `'const greeting = \'world${ZWSP}\';'`

## False-positive guards — what it must NOT flag

19 asserted case(s). A rule with few of these has not been argued with.

- an identifier written entirely in Cyrillic Single-script: nothing here is disguised as ASCII, so there is nothing to confuse it with. — `"const имя = 'x';"`
- a mixed-script identifier whose non-ASCII part is not confusable — `'const userשם = 1;'`
- an emoji family sequence built with ZWJ — `'const family = '\u{1f468}${ZWJ}\u{1f469}${ZWJ}\u{1f467}';'`
- a Persian compound joined with ZWNJ — `'const label = 'می${ZWNJ}شود';'`
- a zero-width space written as an escape stays visible in review — `"const ADMIN_GROUP = 'admin\\u200b';"`
- a lone invisible character with no ASCII neighbour in a template — `'const zwsp = \'${ZWSP}\';'`
- a non-string literal is not scanned — `'const limit = 42;'`
- a regular expression literal is not scanned — `'const pattern = /admin/;'`
- checkIdentifiers: false disables the identifier scan — `'const ${CYRILLIC_A}dminRole = 'guest';'`
- checkStrings: false disables the literal scan — `'const ADMIN_GROUP = 'admin${ZWSP}';'`
- checkStrings: false also disables the template scan — `'const banner = \'admin${ZWSP}\';'`
- a private class field with a Cyrillic homoglyph — `'class Session { #${CYRILLIC_A}dmin = true; }'`
- a fullwidth uppercase letter — `'const ＡdminRole = 1;'`
- a fullwidth lowercase letter — `'const admiｎ = 1;'`
- a Greek omicron in a property name — `'config.passwοrd = secret;'`
- a bidi override inside a string (Trojan Source) — `'const note = 'admin${RLO}// safe';'`
- an invisible character in a template chunk — `'const greeting = \'hello${ZWSP}world\';'`
- a template chunk that starts with an invisible character — `'const greeting = \'${ZWSP}world\';'`
- a template chunk that ends with an invisible character — `'const greeting = \'world${ZWSP}\';'`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-1007`.

