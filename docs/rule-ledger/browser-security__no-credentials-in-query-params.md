# `browser-security/no-credentials-in-query-params`

**CWE-798** · severity CRITICAL · 12 catch cases · 21 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

No check fired. That means no KNOWN gap — it is not evidence of correctness.

## Why this rule exists

@fileoverview Disallow credentials in URL query parameters
@see https://owasp.org/www-project-mobile-top-10/
@see https://cwe.mitre.org/data/definitions/598.html

## True positives — what it catches

7 asserted case(s). These are the claims; each is only as good as the case.

- `"const u = '${baseUrl}/login/sessionCookieRedirect?check=true&token=${token}'"`
- `"const u = '${base}?password=${pw}'"`
- `'const url = 'https://api.example.com/data?token=abc123';'`
- `'const url = '/reset?password=hunter2';'`
- `'const url = '/reset?apikey=sk_live_abc';'`
- `'const url = '/reset?secret=shhh';'`
- `'const url = '/reset?token=abc&apikey=sk_live';'`

## False-positive guards — what it must NOT flag

7 asserted case(s). A rule with few of these has not been argued with.

- `"const u = '${baseUrl}/login/sessionCookieRedirect?check=true&token=${token}'"`
- `"const u = '${base}?password=${pw}'"`
- `'const url = 'https://api.example.com/data?token=abc123';'`
- `'const url = '/reset?password=hunter2';'`
- `'const url = '/reset?apikey=sk_live_abc';'`
- `'const url = '/reset?secret=shhh';'`
- `'const url = '/reset?token=abc&apikey=sk_live';'`

## Not covered

Corpus: 5 vulnerable, 4 safe fixtures under `CWE-798`.

