# `secure-coding/detect-non-literal-regexp`

**CWE-400** · severity LOW · 23 catch cases · 48 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: additionalPatterns — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useStaticRegex, validateInput, useRegexLibrary, addTimeout, escapeUserInput.
- **`inert-suggestion`** (metadata-contract) — 5 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-400/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-unchecked-loop-condition — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.
- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: detect-non-literal-regexp
Detects RegExp(variable), which might allow an attacker to DOS your server with a long-running regular expression
LLM-optimized with comprehensive ReDoS prevention guidance

@see https://owasp.org/www-community/attacks/Regular_expression_Denial_of_Service_-_ReDoS
@see https://cwe.mitre.org/data/definitions/400.html

## True positives — what it catches

21 asserted case(s). These are the claims; each is only as good as the case.

- `'const pattern = new RegExp(userInput);'`
- `'const regex = RegExp(userPattern);'`
- `'new RegExp('^${userInput}$');'`
- `'new RegExp(config.pattern);'`
- `'const pattern = new RegExp(userInput);'`
- `'const regex = new RegExp(userInput);'`
- `'const RegExp = myFunction; RegExp(pattern);'`
- `'new RegExp(userInput);'`
- `'new RegExp(userInput);'`
- `'new RegExp(userInput);'`
- `'new RegExp(a++);'`
- `'function build(pattern) { return new RegExp(pattern); }'`
- `'let p = "^a"; p = readInput(); const r = new RegExp(p);'`
- `'function f(parts) { return new RegExp(parts.join("|")); }'`
- `'const A = ["x"]; const r = new RegExp(A.map(String).join("|"));'`
- `'const N = 2; const r = new RegExp(String(N - 1));'`
- `'function f(rest) { const A = ["x", ...rest]; return new RegExp(A.join("|")); }'`
- `'const A = ["x", , "y"]; const r = new RegExp(A.join("|"));'`
- `'const r = new RegExp(globalPattern);'`
- `'const r = new RegExp("^[a-z]+$");'`
- `'const r = new RegExp("${'x'.repeat(300)}");'`

## False-positive guards — what it must NOT flag

26 asserted case(s). A rule with few of these has not been argued with.

- `'const result = myFunction(pattern);'`
- `'obj.RegExp(pattern);'`
- `'new RegExp();'`
- `'const pattern = new RegExp(userInput);'`
- `'const regex = RegExp(userPattern);'`
- `'new RegExp('^${userInput}$');'`
- `'new RegExp(config.pattern);'`
- `'const pattern = new RegExp(userInput);'`
- `'const regex = new RegExp(userInput);'`
- `'new RegExp("^test$");'`
- `'const RegExp = myFunction; RegExp(pattern);'`
- `'new RegExp("^test$");'`
- `'new RegExp(userInput);'`
- `'new RegExp(userInput);'`
- `'new RegExp(userInput);'`
- `'new RegExp(a++);'`
- `'function build(pattern) { return new RegExp(pattern); }'`
- `'let p = "^a"; p = readInput(); const r = new RegExp(p);'`
- `'function f(parts) { return new RegExp(parts.join("|")); }'`
- `'const A = ["x"]; const r = new RegExp(A.map(String).join("|"));'`
- `'const N = 2; const r = new RegExp(String(N - 1));'`
- `'function f(rest) { const A = ["x", ...rest]; return new RegExp(A.join("|")); }'`
- `'const A = ["x", , "y"]; const r = new RegExp(A.join("|"));'`
- `'const r = new RegExp(globalPattern);'`
- `'const r = new RegExp("^[a-z]+$");'`
- `'const r = new RegExp("${'x'.repeat(300)}");'`

## Not covered

No `benchmarks/corpus/CWE-400/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

