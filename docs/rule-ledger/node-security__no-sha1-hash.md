# `node-security/no-sha1-hash`

**CWE-327** · severity HIGH · 3 catch cases · 11 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-dynamic-algorithm-selection — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-sha1-hash
Detects sha1() usage from crypto-hash package
CWE-327: SHA-1 is cryptographically broken

The crypto-hash package itself warns: "SHA-1 is insecure and should not be used"
@see https://www.npmjs.com/package/crypto-hash

## True positives — what it catches

3 asserted case(s). These are the claims; each is only as good as the case.

- `'import { sha1 } from "crypto-hash";'`
- `'import { sha1 as hash } from "crypto-hash";'`
- Test sha1() call after import - this triggers the CallExpression path — `'import { sha1 } from "crypto-hash"; sha1(data);'`

## False-positive guards — what it must NOT flag

5 asserted case(s). A rule with few of these has not been argued with.

- `"import { sha1 } from 'crypto-hash';\nsha1('data');"`
- `"import { sha256 } from 'crypto-hash';\nsha256('data');"`
- `'import { sha1 } from "crypto-hash";'`
- `'import { sha1 as hash } from "crypto-hash";'`
- Test sha1() call after import - this triggers the CallExpression path — `'import { sha1 } from "crypto-hash"; sha1(data);'`

## Not covered

Corpus: 4 vulnerable, 4 safe fixtures under `CWE-327`.

