# `node-security/no-zip-slip`

**CWE-22** · severity HIGH · 21 catch cases · 44 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 3 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: pathValidationFunctions, archiveModules — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: zipSlipVulnerability, validateArchivePaths, sanitizeArchiveNames, strategyPathValidation, strategySafeLibraries, strategySandboxing — dead metadata, or a report path that was lost.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-22/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: detect-non-literal-fs-filename, no-arbitrary-file-access — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-zip-slip
Detects zip slip/archive extraction vulnerabilities (CWE-22)

Zip slip vulnerabilities occur when extracting archives without properly
validating file paths, allowing attackers to write files outside the
intended extraction directory using path traversal sequences like "../".

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe archive extraction patterns
- Path validation functions
- JSDoc annotations (@safe, @validated)
- Trusted extraction libraries

## True positives — what it catches

17 asserted case(s). These are the claims; each is only as good as the case.

- `"zip.extractAllTo('/etc/app', true);"`
- `"zip.extract('a.zip');"`
- `"extractArchive('a.zip', './out');"`
- `"extract('../../evil', './out');"`
- `"const AdmZip = require('adm-zip'); doStuff(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); helper.wrap(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); path.dirname(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); if (flag) { fs.cp(path.join(dest, entry.name)); }"`
- `"const AdmZip = require('adm-zip'); if (!entryPath.includes('..')) { fs.cp(path.join(dest,`
- `"const AdmZip = require('adm-zip'); if (!checkIt(p)) { fs.cp(path.join(dest, entry.name));`
- `'archive.unzip(dest);'`
- `"zip.extractAllTo('../../output');"`
- `'const AdmZip = require('adm-zip');`
- `'import unzipper from 'unzipper';`
- `'const zip = new AdmZip(file);`
- `'unzip(zipFile, "/root/backups");'`
- `'unsafeLib.extract(file, dest);'`

## False-positive guards — what it must NOT flag

29 asserted case(s). A rule with few of these has not been argued with.

- `'if (full.startsWith(dest)) { fs.writeFileSync(path.join(dest, entry.name)); }'`
- `'if (!full.startsWith(dest)) { throw new Error(path.join(dest, entry.name)); }'`
- `"if (entryPath.includes('..')) { reject(path.join(dest, entry.name)); }"`
- `"zip.extractAllTo('/etc/app', true);"`
- `"zip.extract('a.zip');"`
- `"extractArchive('a.zip', './out');"`
- `"extract('../../evil', './out');"`
- `"const AdmZip = require('adm-zip'); doStuff(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); helper.wrap(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); path.dirname(path.join(dest, entry.name));"`
- `"const AdmZip = require('adm-zip'); if (flag) { fs.cp(path.join(dest, entry.name)); }"`
- `"const AdmZip = require('adm-zip'); if (!entryPath.includes('..')) { fs.cp(path.join(dest,`
- `"const AdmZip = require('adm-zip'); if (!checkIt(p)) { fs.cp(path.join(dest, entry.name));`
- `'const safeExtract = require("safe-archive-extract"); safeExtract(file, dest);'`
- `'const safePath = validatePath(entry.name); fs.writeFileSync(path.join(dest, safePath), da`
- `'const yauzl = require("yauzl"); yauzl.open(zipFile, callback);'`
- `'const data = fs.readFileSync(filePath);'`
- `'const filePath = "safe-file.txt";'`
- `'archive.unzip(dest);'`
- `"zip.extractAllTo('../../output');"`
- `'const AdmZip = require('adm-zip');`
- `'import unzipper from 'unzipper';`
- `'const zip = new AdmZip(file);`
- `'unzip(zipFile, "/root/backups");'`
- `'zip.extractAllTo("./uploads/extracted");'`
- `'const configPath = "./config/backup.zip";'`
- `'myExtractor.extract(zipFile, dest);'`
- `'mySafeLib.extract(file, dest);'`
- `'unsafeLib.extract(file, dest);'`

## Not covered

No `benchmarks/corpus/CWE-22/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

