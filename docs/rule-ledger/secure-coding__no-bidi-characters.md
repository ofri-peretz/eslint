# `secure-coding/no-bidi-characters`

**CWE-1007** · severity HIGH · 7 catch cases · 16 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`whole-program-text`** (performance) — sourceCode.getText() with no node prints the whole file. Inside a visitor this is O(file) per node. **Probe:** time the rule against a 5k-line file versus a 500-line one; cost should be linear, not quadratic.

## Why this rule exists

ESLint Rule: no-bidi-characters
Detects Unicode bidirectional control characters (CWE-1007, "Trojan Source").

Bidi controls reorder how text is DISPLAYED without changing how it is COMPILED. An
attacker can therefore write source that a reviewer reads as one program while the
compiler reads another — the canonical form hides an early `return` inside what looks
like a comment:

  if (accessLevel != "user\u2066 // Check if admin\u2069 \u2066") {

Because the payload lives in the raw bytes, this scans source text rather than the AST:
the characters survive in string literals, comments, and identifiers alike, and an
AST-only rule would miss the comment case entirely — which is the one the original
Trojan Source paper demonstrated.

@see https://trojansource.codes/
@see https://cwe.mitre.org/data/definitions/1007.html

## True positives — what it catches

4 asserted case(s). These are the claims; each is only as good as the case.

- `'if (accessLevel != 'user${LRI} // Check if admin${PDI} ${LRI}') {\n grantAdmin();\n}'`
- `'const s = '${ZWSP}';'`
- `'const s = '${BEAM}';'`
- `'const label = '${RLO}admin';'`

## False-positive guards — what it must NOT flag

4 asserted case(s). A rule with few of these has not been argued with.

- `'if (accessLevel != 'user${LRI} // Check if admin${PDI} ${LRI}') {\n grantAdmin();\n}'`
- `'const s = '${ZWSP}';'`
- `'const s = '${BEAM}';'`
- `'const label = '${RLO}admin';'`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-1007`.

