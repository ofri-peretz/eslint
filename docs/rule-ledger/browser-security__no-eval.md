# `browser-security/no-eval`

**CWE-95** · severity CRITICAL · 11 catch cases · 23 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useSafeAlternative.
- **`inert-suggestion`** (metadata-contract) — 4 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-95/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-websocket-eval — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-eval
Detects dangerous eval() and similar code execution patterns
CWE-95: Improper Neutralization of Directives in Dynamically Evaluated Code (Eval Injection)

@see https://cwe.mitre.org/data/definitions/95.html
@see https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/11-Testing_for_Code_Injection

## True positives — what it catches

9 asserted case(s). These are the claims; each is only as good as the case.

- `'eval('alert(1)');'`
- `'const fn = new Function('a', 'return a * 2');'`
- `'eval();'`
- `'window.eval(code);'`
- `'global.eval(code);'`
- `'globalThis['eval'](code);'`
- `'execScript(code);'`
- `'setTimeout('doSomething()', 1000);'`
- `'setInterval('tick()', 500);'`

## False-positive guards — what it must NOT flag

15 asserted case(s). A rule with few of these has not been argued with.

- `'const data = JSON.parse(jsonString);'`
- `'setTimeout(() => doSomething(), 1000);'`
- `'setInterval(callback, 500);'`
- `'const fn = new Function('a', 'return a * 2');'`
- `'eval('test code');'`
- `'const result = evaluate(expression);'`
- `'eval('alert(1)');'`
- `'const fn = new Function('a', 'return a * 2');'`
- `'eval();'`
- `'window.eval(code);'`
- `'global.eval(code);'`
- `'globalThis['eval'](code);'`
- `'execScript(code);'`
- `'setTimeout('doSomething()', 1000);'`
- `'setInterval('tick()', 500);'`

## Not covered

No `benchmarks/corpus/CWE-95/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

