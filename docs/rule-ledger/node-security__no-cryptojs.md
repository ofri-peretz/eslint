# `node-security/no-cryptojs`

**CWE-1104** · severity MEDIUM · 4 catch cases · 7 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: severity — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useNativeCrypto, useWebCrypto.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-1104/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-dynamic-dependency-loading, prefer-native-crypto — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-cryptojs
Warns on usage of deprecated crypto-js library
CWE-1104: Use of Unmaintained Third Party Components

crypto-js is not maintained since 2022 and recommends using native crypto
@see https://www.npmjs.com/package/crypto-js

## True positives — what it catches

4 asserted case(s). These are the claims; each is only as good as the case.

- `'import CryptoJS from "crypto-js";'`
- `'import { AES } from "crypto-js/aes";'`
- `'const CryptoJS = require("crypto-js");'`
- `'const MD5 = require("crypto-js/md5");'`

## False-positive guards — what it must NOT flag

4 asserted case(s). A rule with few of these has not been argued with.

- `'import CryptoJS from "crypto-js";'`
- `'import { AES } from "crypto-js/aes";'`
- `'const CryptoJS = require("crypto-js");'`
- `'const MD5 = require("crypto-js/md5");'`

## Not covered

No `benchmarks/corpus/CWE-1104/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

