# `browser-security/no-sensitive-indexeddb`

**CWE-922** · severity HIGH · 7 catch cases · 12 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useEncryption.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-922/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-jwt-in-storage, no-sensitive-localstorage, no-sensitive-sessionstorage — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-sensitive-indexeddb
Detects storing sensitive data in IndexedDB
CWE-922: Insecure Storage of Sensitive Information

@see https://cwe.mitre.org/data/definitions/922.html

## True positives — what it catches

7 asserted case(s). These are the claims; each is only as good as the case.

- `'db.createObjectStore('passwords');'`
- `'db.createObjectStore('secrets');'`
- `'db.createObjectStore('apiKeys');'`
- `'db.createObjectStore('credentials');'`
- `'store.add({ password: userPassword, email: 'test@test.com' });'`
- `'store.put({ apiKey: key, user: 'admin' });'`
- `'db.createObjectStore('passwords');'`

## False-positive guards — what it must NOT flag

7 asserted case(s). A rule with few of these has not been argued with.

- `'db.createObjectStore('passwords');'`
- `'db.createObjectStore('secrets');'`
- `'db.createObjectStore('apiKeys');'`
- `'db.createObjectStore('credentials');'`
- `'store.add({ password: userPassword, email: 'test@test.com' });'`
- `'store.put({ apiKey: key, user: 'admin' });'`
- `'db.createObjectStore('passwords');'`

## Not covered

No `benchmarks/corpus/CWE-922/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

