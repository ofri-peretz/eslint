# `browser-security/require-postmessage-origin-check`

**CWE-346** · severity HIGH · 21 catch cases · 37 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: trustedOrigins — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: addOriginCheck.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-postmessage-wildcard-origin — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: require-postmessage-origin-check
Detects postMessage listeners without origin validation
CWE-346: Origin Validation Error

@see https://cwe.mitre.org/data/definitions/346.html
@see https://developer.mozilla.org/en-US/docs/Web/API/Window/postMessage#security_concerns

## True positives — what it catches

4 asserted case(s). These are the claims; each is only as good as the case.

- `'new EventEmitter().addEventListener('message', (event) => render(event.data));'`
- `'getSocket().addEventListener('message', (event) => render(event.data));'`
- `'a.b.c.addEventListener('message', (event) => render(event.data));'`
- `'addEventListener('message', (event) => render(event.data));'`

## False-positive guards — what it must NOT flag

5 asserted case(s). A rule with few of these has not been argued with.

- `'new BroadcastChannel('sync').addEventListener('message', (event) => apply(event.data));'`
- `'new EventEmitter().addEventListener('message', (event) => render(event.data));'`
- `'getSocket().addEventListener('message', (event) => render(event.data));'`
- `'a.b.c.addEventListener('message', (event) => render(event.data));'`
- `'addEventListener('message', (event) => render(event.data));'`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-346`.

