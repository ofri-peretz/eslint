# `secure-coding/no-unchecked-loop-condition`

**CWE-400** · severity CRITICAL · 39 catch cases · 81 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

4 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: maxRecursionDepth, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: limitLoopIterations.
- **`inert-suggestion`** (metadata-contract) — 1 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-400/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: detect-non-literal-regexp — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-unchecked-loop-condition
Detects unchecked loop conditions that could cause DoS (CWE-400, CWE-606)

Loops with unchecked conditions can cause denial of service by consuming
excessive CPU time or memory. This includes infinite loops, loops with
user-controlled bounds, and loops without proper termination conditions.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe loop patterns with clear termination
- Development/debugging loops
- JSDoc annotations (@safe-loop, @intentional)
- Timeout protections

## True positives — what it catches

26 asserted case(s). These are the claims; each is only as good as the case.

- `'for (;;) { console.log("infinite"); }'`
- `'while (true) { /* no break */ }'`
- `'for (let i = 0; i < req.query.limit; i++) { processItem(); }'`
- `'const userInput = req.query.count; while (userInput-- > 0) { doWork(); }'`
- `'const iterations = req.body.count; for (let i = 0; i < iterations; i++) { /* work */ }'`
- `'const userInput = req.query.count; while (-userInput > 0) { /* UnaryExpression */ process`
- `'const userInput = req.query.count; while (userInput++ < 100) { /* UpdateExpression */ pro`
- `'const userInput = req.query.count; while (!userInput) { /* UnaryExpression ! */ process()`
- `'const userInput = req.query.count; while (check(userInput)) { /* CallExpression with user`
- `'for (let i = 0; i < 100000; i++) { processItem(); }'`
- `'for (let i = 0; i <= 50000; i++) { /* work */ }'`
- `'for (let i = 0; ; i++) { if (i > 10) break; }', // Missing condition in for loop`
- `'for (const item of req.body.items) { processItem(item); }'`
- `'const userInput = req.query.count; for (const key in userInput) { console.log(key); }'`
- `'for (let i = 0; i < 1500; i++) { /* exceeds limit */ }'`
- `'while (customInput-- > 0) { /* flagged */ }'`
- `'while (true) { processData(); if (done) break; }'`
- `'while (cache.match(rx)) { cache = cache.replace(rx, \'\'); }'`
- `'while (page < pageSize) { advance(); }'`
- Uses `limit` (not `pageSize`) so the pagination branch's `.includes('page')` check doesn't intercept it first — isolates the arithmetic-overflow branch (`*` combined with `limit`). — `'while (offset * limit < total) { advance(); }'`
- `'const userInput = req.query.count; do { doWork(); } while (userInput-- > 0);'`
- `'while (shouldContinue) { doWork(); }'`
- `'while (isActive) { doWork(); }'`
- `'while (isEnabled) { doWork(); }'`
- `'for (const key in req.body) { use(key); }'`
- `'for (const item of request.query.items) { use(item); }'`

## False-positive guards — what it must NOT flag

41 asserted case(s). A rule with few of these has not been argued with.

- `'for (let i = 0; i < 10; i++) { console.log(i); }'`
- `'for (let i = 0; i < items.length; i++) { processItem(items[i]); }'`
- `'while (condition && attempts < 3) { attemptOperation(); attempts++; }'`
- `'while (a < b) { advance(); }'`
- `'while (flag) { doWork(); }'`
- `'while (true) { processData(); if (shouldStop) break; }'`
- `'function factorial(n, depth = 0) { if (depth > 10) return 1; return n * factorial(n-1, de`
- `'const maxIterations = 100; for (let i = 0; i < maxIterations; i++) { /* work */ }'`
- `'for (;;) { console.log("infinite"); }'`
- `'while (true) { /* no break */ }'`
- `'for (let i = 0; i < req.query.limit; i++) { processItem(); }'`
- `'const userInput = req.query.count; while (userInput-- > 0) { doWork(); }'`
- `'const iterations = req.body.count; for (let i = 0; i < iterations; i++) { /* work */ }'`
- `'const userInput = req.query.count; while (-userInput > 0) { /* UnaryExpression */ process`
- `'const userInput = req.query.count; while (userInput++ < 100) { /* UpdateExpression */ pro`
- `'const userInput = req.query.count; while (!userInput) { /* UnaryExpression ! */ process()`
- `'const userInput = req.query.count; while (check(userInput)) { /* CallExpression with user`
- `'for (let i = 0; i < 100000; i++) { processItem(); }'`
- `'for (let i = 0; i <= 50000; i++) { /* work */ }'`
- `'for (let i = 0; ; i++) { if (i > 10) break; }', // Missing condition in for loop`
- `'for (const item of req.body.items) { processItem(item); }'`
- `'const userInput = req.query.count; for (const key in userInput) { console.log(key); }'`
- `'for (let i = 0; i < 100; i++) { /* safe small loop */ }'`
- `'for (let i = 0; i < 500; i++) { /* within limit */ }'`
- `'for (let i = 0; i < 1500; i++) { /* exceeds limit */ }'`
- `'for (let i = 0; i < customInput; i++) { /* not flagged */ }'`
- `'while (customInput-- > 0) { /* flagged */ }'`
- `'while (true) { processData(); if (done) break; }'`
- `'while (cache.match(rx)) { cache = cache.replace(rx, \'\'); }'`
- `'while (page < pageSize) { advance(); }'`
- Uses `limit` (not `pageSize`) so the pagination branch's `.includes('page')` check doesn't intercept it first — isolates the arithmetic-overflow branch (`*` combined with `limit`). — `'while (offset * limit < total) { advance(); }'`
- `'const userInput = req.query.count; do { doWork(); } while (userInput-- > 0);'`
- `'do { doWork(); } while (attempts < 3);'`
- `'for (const item of localItems) { processItem(item); }'`
- `'while (shouldContinue) { doWork(); }'`
- `'while (isActive) { doWork(); }'`
- `'while (isEnabled) { doWork(); }'`
- `'for (let i = 0; iterator.hasNext(); i++) { advance(iterator); }'`
- `'for (let i = 0; i !== 10; i++) { console.log(i); }'`
- `'for (const key in req.body) { use(key); }'`
- `'for (const item of request.query.items) { use(item); }'`

## Not covered

No `benchmarks/corpus/CWE-400/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

