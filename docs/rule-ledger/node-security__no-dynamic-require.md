# `node-security/no-dynamic-require`

**CWE-94** · severity HIGH · 5 catch cases · 15 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

2 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: allowPatterns — those branches ship unexecuted.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-94/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

ESLint Rule: no-dynamic-require
Forbid `require()` calls with expressions (eslint-plugin-import inspired)

## True positives — what it catches

5 asserted case(s). These are the claims; each is only as good as the case.

- `'const mod = require(moduleName);'`
- `'const mod = require('./plugins/${name}');'`
- `'const mod = require("./handlers/" + handler);'`
- `'const mod = require(getModulePath());'`
- `'const mod = require(config.pluginPath);'`

## False-positive guards — what it must NOT flag

15 asserted case(s). A rule with few of these has not been argued with.

- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'require(modulePath);'`
- `'const mod = require(moduleName);'`
- `'const fixture = require(fixturePath);'`
- `'const mod = require(moduleName);'`
- `'const mod = require('./plugins/${name}');'`
- `'const mod = require("./handlers/" + handler);'`
- `'const mod = require(getModulePath());'`
- `'const mod = require(config.pluginPath);'`

## Not covered

No `benchmarks/corpus/CWE-94/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

