# `secure-coding/no-unsafe-deserialization`

**CWE-502** · severity CRITICAL · 20 catch cases · 49 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: safeLibraries, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: validateBeforeDeserialization, avoidEval, strategySafeLibraries, strategyInputValidation, strategySandboxing — dead metadata, or a report path that was lost.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-502/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-unsafe-deserialization
Detects unsafe deserialization of untrusted data (CWE-502)

Unsafe deserialization occurs when untrusted data is deserialized in a way that
allows attackers to execute arbitrary code or manipulate application logic.
This includes:
- Using dangerous deserialization libraries
- eval() or Function() on untrusted data
- YAML/XML parsers that can execute code
- Unsafe use of serialization libraries

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe deserialization patterns
- Input validation and sanitization
- JSDoc annotations (@safe, @validated)
- Trusted deserialization libraries

## True positives — what it catches

15 asserted case(s). These are the claims; each is only as good as the case.

- `'eval(req.body.script);'`
- `'const func = new Function(req.body.input);'`
- `'const func = Function("a", "b", req.query.code);'`
- `'const yaml = require("yaml"); const obj = yaml.parse(userInput);'`
- `'const serialize = require("node-serialize"); serialize.unserialize(userInput);'`
- `'const myLib = require("node-serialize"); myLib.unserialize(userInput);'`
- `'function run(code) { eval(code); }'`
- `'async function f(res) { eval(await res.text()); }'`
- `'app.post("/x", (req, res) => { new Function(req.body); });'`
- `'const data = customDeserialize(req.body);'`
- `'const data = fs.readFileSync(dynamicPath); eval(data);'`
- `'let data; data = req.body.payload; eval(data);'`
- `'function run(userCode) { setTimeout("alert(" + userCode + ")", 100); }'`
- `'function run(userCode) { setInterval('do(${userCode})', 100); }'`
- `'function handle(req) { serialize.unserialize(req.body.payload); }'`

## False-positive guards — what it must NOT flag

24 asserted case(s). A rule with few of these has not been argued with.

- `'const data = JSON.parse(input);'`
- `'const yaml = require("js-yaml"); const data = yaml.safeLoad(input);'`
- `'const data = safeJsonParse(input);'`
- `'const result = calculate(input);'`
- `'const cleanData = validateInput(req.body); const obj = JSON.parse(cleanData);'`
- `'eval(req.body.script);'`
- `'const func = new Function(req.body.input);'`
- `'const func = Function("a", "b", req.query.code);'`
- `'const yaml = require("yaml"); const obj = yaml.parse(userInput);'`
- `'const serialize = require("node-serialize"); serialize.unserialize(userInput);'`
- `'const myLib = require("node-serialize"); myLib.unserialize(userInput);'`
- `'function run(code) { eval(code); }'`
- `'async function f(res) { eval(await res.text()); }'`
- `'app.post("/x", (req, res) => { new Function(req.body); });'`
- `'const config = JSON.parse(fs.readFileSync("config.json", "utf8"));'`
- `'const data = customDeserialize(value);'`
- `'const data = customDeserialize(req.body);'`
- `'const clean = myValidator(req.body); const data = JSON.parse(clean);'`
- `'/** @safe */\nfunction test() { const s = require("node-serialize"); s.unserialize(req.bo`
- `'const data = fs.readFileSync(dynamicPath); eval(data);'`
- `'let data; data = req.body.payload; eval(data);'`
- `'function run(userCode) { setTimeout("alert(" + userCode + ")", 100); }'`
- `'function run(userCode) { setInterval('do(${userCode})', 100); }'`
- `'function handle(req) { serialize.unserialize(req.body.payload); }'`

## Not covered

No `benchmarks/corpus/CWE-502/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

