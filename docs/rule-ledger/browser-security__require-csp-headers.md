# `browser-security/require-csp-headers`

**CWE-1021** · severity MEDIUM · 8 catch cases · 15 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 0 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-1021/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

## Why this rule exists

@fileoverview Require Content Security Policy

## True positives — what it catches

3 asserted case(s). These are the claims; each is only as good as the case.

- `"import express from 'express'; res.render('index');"`
- `"const helmetish = require('helmetish'); res.render('index');"`
- `"const mod = require(name); res.render('index');"`

## False-positive guards — what it must NOT flag

3 asserted case(s). A rule with few of these has not been argued with.

- `"import express from 'express'; res.render('index');"`
- `"const helmetish = require('helmetish'); res.render('index');"`
- `"const mod = require(name); res.render('index');"`

## Not covered

No `benchmarks/corpus/CWE-1021/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

