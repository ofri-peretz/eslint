# `browser-security/no-missing-cors-check`

**CWE-346** · severity HIGH · 13 catch cases · 60 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: useCorsMiddleware — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useOriginValidation.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.

### Smells — unproven, each with its probe

- **`nominal-inference-suppress`** (detection-soundness) — a name-substring test can silence a finding. Costs recall rather than trust — a false negative is invisible to the user, which is why it is triaged after reports. **Probe:** lint a genuine vulnerability whose identifier hits the allowlist vocabulary.

## Why this rule exists

ESLint Rule: no-missing-cors-check
Detects missing CORS validation (wildcard CORS, missing origin check)
CWE-346: Origin Validation Error

@see https://cwe.mitre.org/data/definitions/346.html
@see https://owasp.org/www-community/attacks/CORS_Misconfiguration

## True positives — what it catches

10 asserted case(s). These are the claims; each is only as good as the case.

- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: "*", credentials: true }));'`
- `'res.setHeader("Access-Control-Allow-Origin", "*");'`
- `'res.header("Access-Control-Allow-Origin", "*");'`
- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: "*", allowedOrigins: ["https://example.com"] }));'`
- `'const config = { origin: "*" }; app.use(cors(config));'`
- `'cors({ origin: "*" });'`
- `'app.use(cors({ allowedOrigins: "*" }));'`
- `'enable(cors({ origin: "*" }));'`

## False-positive guards — what it must NOT flag

53 asserted case(s). A rule with few of these has not been argued with.

- `'app.use(cors({ origin: allowedOrigins }));'`
- `'app.use(cors({ origin: "https://example.com" }));'`
- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: safeOrigin }));'`
- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: "*", credentials: true }));'`
- `'res.setHeader("Access-Control-Allow-Origin", "*");'`
- `'res.header("Access-Control-Allow-Origin", "*");'`
- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: "*" }));'`
- `'app.use(cors({ origin: testOrigin }));'`
- `'app.use(myCors({ origin: "*" }));'`
- `'app.use(cors({ origin: "https://example.com" }));'`
- `'app.use(cors({ origin: 123 }));'`
- `'app.use(cors({ origin: true }));'`
- `'const config = { origin: "*" };'`
- `'const data = { allowedOrigins: "*" };'`
- `'app.use(cors({ origin: allowedOrigins, credentials: true }));'`
- `'res.setHeader("Access-Control-Allow-Origin", origin);'`
- `'res.setHeader("Content-Type", "*");'`
- `'res.setHeader("Content-Type", "*");'`
- `'res.header("Content-Type", "*");'`
- `'app.get("/api", handler);'`
- `'router.post("/users", controller);'`
- `'app.use(cors({ origin: "*", allowedOrigins: ["https://example.com"] }));'`
- `'const config = { origin: "*" }; app.use(cors(config));'`
- `'app.use(cors({ origin: "*" }));'`
- `'cors({ origin: "*" });'`
- `'app.use(cors({ allowedOrigins: "*" }));'`
- `'safeRes.setHeader("Access-Control-Allow-Origin", "*");'`
- `'enable(cors({ origin: "*" }));'`
- `'app.use(cors(unknownVar));'`
- `'app.use(myCors({ origin: "*" }));'`
- `'app.use(cors({ origin: "https://example.com" }));'`
- `'app.use(cors({ origin: 123 }));'`
- `'app.use(cors({ origin: true }));'`
- `'const config = { origin: "*" };'`
- `'const data = { allowedOrigins: "*" };'`
- `'app.use(cors({ origin: allowedOrigins, credentials: true }));'`
- `'res.setHeader("Access-Control-Allow-Origin", origin);'`
- `'res.setHeader("Content-Type", "*");'`
- `'res.setHeader("Content-Type", "*");'`
- `'res.header("Content-Type", "*");'`
- `'app.get("/api", handler);'`
- `'router.post("/users", controller);'`
- `'app.use(cors({ origin: "*", allowedOrigins: ["https://example.com"] }));'`
- `'const config = { origin: "*" }; app.use(cors(config));'`
- `'app.use(cors({ origin: "*" }));'`
- `'cors({ origin: "*" });'`
- `'app.use(cors({ allowedOrigins: "*" }));'`
- `'safeRes.setHeader("Access-Control-Allow-Origin", "*");'`
- `'enable(cors({ origin: "*" }));'`
- `'app.use(cors(unknownVar));'`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-346`.

