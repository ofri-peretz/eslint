# `secure-coding/no-insecure-comparison`

**CWE-697** · severity HIGH · 24 catch cases · 57 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

1 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-697/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`dynamic-regexp`** (performance) — a RegExp is constructed from a non-literal — if the source is a user option, a pathological pattern hangs the lint run. **Probe:** set the option to a nested-quantifier pattern and lint a long identifier.

## Why this rule exists

ESLint Rule: no-insecure-comparison
Detects insecure comparison operators (==, !=) that can lead to type coercion vulnerabilities
CWE-697: Incorrect Comparison

@see https://cwe.mitre.org/data/definitions/697.html
@see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Equality_comparisons_and_sameness

## True positives — what it catches

21 asserted case(s). These are the claims; each is only as good as the case.

- Still reported, but with NO auto-applied `output`: swapping == for === can change behaviour when operand types differ, so it may only be offered as a suggestion the author opts into. — `'if (count == "5") { go(); }'`
- `'if (x == y) {}'`
- `'if (user.id == userId) {}'`
- `'const result = a == b ? 1 : 0;'`
- `'if (x != y) {}'`
- `'if (x == y) {}'`
- `'if (x == y) {}'`
- `'if (token === userToken) {}'`
- `'if (apiKey !== storedSecret) {}'`
- `'if (candidate === expectedPassword) {}'`
- `"if (node.key == 'foo') {}"`
- `'if (providedToken === storedToken) {}'`
- `'if (req.headers.apiKey === config.apiKey) {}'`
- `'if (computedHmac !== expectedSignature) {}'`
- `'let role = 'admin'; role = readInput(); if (role == 'admin') { go(); }'`
- `'function check(role) { return role == 'admin'; }'`
- `'const n = 1; if (n == '1') { go(); }'`
- `'if (globalRole == 'admin') { go(); }'`
- `'if (user.role == 'admin') { go(); }'`
- `'var a = b; var b = a; if (a == y) { go(); }'`
- `'let role; role = input; if (role == 'admin') { go(); }'`

## False-positive guards — what it must NOT flag

38 asserted case(s). A rule with few of these has not been argued with.

- `'if (x === y) {}'`
- `'if (x !== y) {}'`
- `'const result = a === b ? 1 : 0;'`
- `'if (value !== null && value !== undefined) {}'`
- `'if (user.id === userId) {}'`
- `'if (x == y) {}'`
- `'if (x == y) {}'`
- Still reported, but with NO auto-applied `output`: swapping == for === can change behaviour when operand types differ, so it may only be offered as a suggestion the author opts into. — `'if (count == "5") { go(); }'`
- `'if (x == y) {}'`
- `'if (user.id == userId) {}'`
- `'const result = a == b ? 1 : 0;'`
- `'if (x != y) {}'`
- `'if (x == y) {}'`
- `'if (x == y) {}'`
- `'if (x == y) {}'`
- `'if (a != b) {}'`
- `'if (fn() == y) {}'`
- `'if (x == y) {}'`
- `'if (token === userToken) {}'`
- `'if (apiKey !== storedSecret) {}'`
- `'if (candidate === expectedPassword) {}'`
- `'if (token.length === expectedToken.length) {}'`
- `'if (expectedToken.length !== token.length) {}'`
- `"if (node.key === 'foo') {}"`
- `"if (node.key === 'foo') {}"`
- `"if (node.key === 'foo') {}"`
- `"if (node.key === 'foo') {}"`
- `"if (node.key == 'foo') {}"`
- `'if (providedToken === storedToken) {}'`
- `'if (req.headers.apiKey === config.apiKey) {}'`
- `'if (computedHmac !== expectedSignature) {}'`
- `'let role = 'admin'; role = readInput(); if (role == 'admin') { go(); }'`
- `'function check(role) { return role == 'admin'; }'`
- `'const n = 1; if (n == '1') { go(); }'`
- `'if (globalRole == 'admin') { go(); }'`
- `'if (user.role == 'admin') { go(); }'`
- `'var a = b; var b = a; if (a == y) { go(); }'`
- `'let role; role = input; if (role == 'admin') { go(); }'`

## Not covered

No `benchmarks/corpus/CWE-697/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

