# `browser-security/no-jwt-in-storage`

**CWE-922** · severity HIGH · 17 catch cases · 25 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

3 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useHttpOnlyCookie.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-922/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-sensitive-indexeddb, no-sensitive-localstorage, no-sensitive-sessionstorage — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-jwt-in-storage
Detects storing JWT tokens in localStorage/sessionStorage
CWE-922: Insecure Storage of Sensitive Information

@see https://cwe.mitre.org/data/definitions/922.html
@see https://auth0.com/docs/secure/security-guidance/data-security/token-storage

## True positives — what it catches

17 asserted case(s). These are the claims; each is only as good as the case.

- `'localStorage.setItem('jwt', token);'`
- `'localStorage.setItem('token', authToken);'`
- `'localStorage.setItem('accessToken', result.token);'`
- `'localStorage.setItem('access_token', response.access_token);'`
- `'localStorage.setItem('refreshToken', refresh);'`
- `'localStorage.setItem('id_token', idToken);'`
- `'sessionStorage.setItem('jwt', token);'`
- `'sessionStorage.setItem('authToken', auth.token);'`
- `'localStorage.setItem('data', '${EXAMPLE_JWT}');'`
- `'localStorage['token'] = jwt;'`
- `'sessionStorage['accessToken'] = token;'`
- `'localStorage.setItem('bearer', authBearer);'`
- `'localStorage.setItem('jwt', token);'`
- `'localStorage.setItem(accessToken, value);'`
- `'localStorage.jwt = tokenValue;'`
- `'sessionStorage.setItem(refreshToken, value);'`
- `'localStorage['userData'] = '${EXAMPLE_JWT}';'`

## False-positive guards — what it must NOT flag

25 asserted case(s). A rule with few of these has not been argued with.

- `'localStorage.setItem('theme', 'dark');'`
- `'sessionStorage.setItem('locale', 'en-US');'`
- `'localStorage.setItem('preference', 'compact');'`
- `'const theme = localStorage.getItem('theme');'`
- `'myStorage.setItem('token', jwt);'`
- `'localStorage.setItem('token', jwt);'`
- `'sessionStorage.setItem('accessToken', token);'`
- `'localStorage.setItem('tokenCount', '5');'`
- `'localStorage.setItem('jwt', token);'`
- `'localStorage.setItem('token', authToken);'`
- `'localStorage.setItem('accessToken', result.token);'`
- `'localStorage.setItem('access_token', response.access_token);'`
- `'localStorage.setItem('refreshToken', refresh);'`
- `'localStorage.setItem('id_token', idToken);'`
- `'sessionStorage.setItem('jwt', token);'`
- `'sessionStorage.setItem('authToken', auth.token);'`
- `'localStorage.setItem('data', '${EXAMPLE_JWT}');'`
- `'localStorage['token'] = jwt;'`
- `'sessionStorage['accessToken'] = token;'`
- `'localStorage.setItem('bearer', authBearer);'`
- `'localStorage.setItem('jwt', token);'`
- `'localStorage.setItem(accessToken, value);'`
- `'localStorage.jwt = tokenValue;'`
- `'sessionStorage.setItem(refreshToken, value);'`
- `'localStorage['userData'] = '${EXAMPLE_JWT}';'`

## Not covered

No `benchmarks/corpus/CWE-922/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

