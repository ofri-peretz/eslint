# `secure-coding/no-weak-password-recovery`

**CWE-640** · severity HIGH · 19 catch cases · 27 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

2 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: minTokenEntropy, maxTokenLifetimeHours, recoveryKeywords, secureTokenFunctions, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: weakPasswordRecovery, unlimitedRecoveryAttempts, weakRecoveryVerification, tokenReuseVulnerability, implementRateLimiting, useCryptographicallySecureTokens, implementTokenExpiration, secureRecoveryFlow, strategyMultiFactor, strategyOutOfBandVerification, strategyTimeBoundTokens — dead metadata, or a report path that was lost.

### Smells — unproven, each with its probe

- **`nominal-inference-report`** (detection-soundness) — a name-substring test sits on a path that can report. If the spelling is the whole verdict this ships false positives; if it narrows an already-proven sink or taint root, it is sound and permitted. **Probe:** lint a snippet that matches the vocabulary and nothing else (`const passwordLength = 8`). A report means the name decided.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-weak-password-recovery
Detects weak password recovery mechanisms (CWE-640)

Weak password recovery mechanisms can allow attackers to reset passwords
for other users, gain unauthorized access, or perform account takeover.
This rule detects obvious vulnerabilities in password recovery logic.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Proper recovery implementations
- Rate limiting mechanisms
- Secure token generation
- JSDoc annotations (@secure-recovery, @rate-limited)

## True positives — what it catches

19 asserted case(s). These are the claims; each is only as good as the case.

- `'const passwordResetToken = String(Math.random()).slice(2);'`
- `'const forgotPasswordToken = Date.now();'`
- `'function handlePasswordReset(email) { const token = makeToken(); save(email, token); }'`
- `'function forgotPassword() { const token = makeToken(); if(token.expired) return; reset(to`
- `'function resetPassword() { const token = makeToken(); checkRateLimit(); sendEmail(token);`
- `'function recoverPasswordOuter() { const go = () => { const resetToken = mint(); return re`
- `'function resetPasswordFlow() { const v = nanoid(); mail(v); }'`
- `'function resetPasswordWithToken(resetToken) { applyReset(resetToken); }'`
- `'console.log("Token:", passwordResetToken);'`
- `'console.log("Code:", forgotPasswordCode);'`
- `'const passwordResetToken = Date.now() + salt;'`
- `'console.log("value:", pwdResetCode);'`
- `'logger.error("data:", pwdRecoveryCode);'`
- Math.random — `'const resetPasswordToken = String(Math.random()).slice(2);'`
- Date.now — `'const passwordResetToken = String(Date.now());'`
- new Date().getTime() — `'const passwordResetToken = String(new Date().getTime());'`
- time-based uuid v1 — `'const resetPasswordToken = uuid.v1();'`
- bare v1() import — `'const resetPasswordToken = v1();'`
- handler with no expiry or rate limit — `'async function forgotPassword(req, res) { const t = crypto.randomBytes(32); await mail(t)`

## False-positive guards — what it must NOT flag

26 asserted case(s). A rule with few of these has not been argued with.

- `'const passwordResetToken = String(Math.random()).slice(2);'`
- `'const forgotPasswordToken = Date.now();'`
- `'function handlePasswordReset(email) { const token = makeToken(); save(email, token); }'`
- `'function forgotPassword() { const token = makeToken(); if(token.expired) return; reset(to`
- `'function resetPassword() { const token = makeToken(); checkRateLimit(); sendEmail(token);`
- `'function recoverPasswordOuter() { const go = () => { const resetToken = mint(); return re`
- `'function resetPasswordFlow() { const v = nanoid(); mail(v); }'`
- `'function resetPasswordWithToken(resetToken) { applyReset(resetToken); }'`
- `'console.log("Token:", passwordResetToken);'`
- `'console.log("Code:", forgotPasswordCode);'`
- `'const passwordResetToken = Date.now() + salt;'`
- `'console.log("value:", pwdResetCode);'`
- `'logger.error("data:", pwdRecoveryCode);'`
- route handler wrapped in catchAsync — `'const forgotPassword = catchAsync(async (req, res) => { await svc.forgot(req.body.email);`
- resetPassword handler — `'const resetPassword = catchAsync(async (req, res) => { await svc.reset(req.query.token);`
- opaque project helper — `'const resetPasswordToken = generateToken(user.id, expires, RESET);'`
- service call — `'const resetPasswordToken = await tokenService.generateResetPasswordToken(email);'`
- crypto is fine — `'const resetPasswordToken = crypto.randomBytes(32).toString('hex');'`
- Math.random — `'const resetPasswordToken = String(Math.random()).slice(2);'`
- Date.now — `'const passwordResetToken = String(Date.now());'`
- new Date().getTime() — `'const passwordResetToken = String(new Date().getTime());'`
- time-based uuid v1 — `'const resetPasswordToken = uuid.v1();'`
- bare v1() import — `'const resetPasswordToken = v1();'`
- swagger doc decorator factory — `'export function UserPublicForgotPasswordDoc() { return applyDecorators(Doc({ summary: 'fo`
- reset doc decorator factory — `'export function UserPublicResetPasswordDoc() { return applyDecorators(Doc({ summary: 'res`
- handler with no expiry or rate limit — `'async function forgotPassword(req, res) { const t = crypto.randomBytes(32); await mail(t)`

## Not covered

Corpus: 2 vulnerable, 1 safe fixtures under `CWE-640`.

