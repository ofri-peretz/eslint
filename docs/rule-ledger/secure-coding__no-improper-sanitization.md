# `secure-coding/no-improper-sanitization`

**CWE-116** · severity HIGH · 32 catch cases · 119 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

2 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: safeSanitizers, trustedLibraries, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: improperSanitization, missingContextEncoding, sqlInjectionSanitization, commandInjectionSanitization, useProperSanitization, validateSanitization, implementContextAware, strategyDefenseInDepth, strategyInputValidation, strategyOutputEncoding — dead metadata, or a report path that was lost.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.

## Why this rule exists

ESLint Rule: no-improper-sanitization
Detects improper sanitization of user input (CWE-94, CWE-79, CWE-116)

Improper sanitization occurs when user input is not properly cleaned
before use in sensitive contexts. This can lead to injection attacks,
XSS, or other security vulnerabilities.

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Known safe sanitization patterns
- Trusted sanitization libraries
- JSDoc annotations (@sanitized, @safe)
- Context-aware validation

## True positives — what it catches

31 asserted case(s). These are the claims; each is only as good as the case.

- `'res.send('<p>' + data[length] + '</p>');'`
- `'res.send('<script>alert(1)</script>');'`
- `'res.send('<div>' + req.query.name + '</div>');'`
- `'res.send(req.query.name || '<p>fallback</p>');'`
- `'res.send(flag ? req.query.name : '<p>x</p>');'`
- `'element.innerHTML = userInput.replace(/</g, "&lt;");'`
- `'const safe = data.replace(/>/g, "&gt;");'`
- `'element.innerHTML = userInput.replace(/</g, "&lt;").replace(/>/g, "&gt;");'`
- `'const s = html.replace(/</g, '&lt;').replace(/>/g, '&gt;')'`
- `'const s = html.replace(/</g, '&lt;')'`
- `'const s = html.replace('<', '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/\\s+/g, ' ')'`
- `'const s = html.replace(/&/g, '&amp;').replace(/</g, '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/"/g, '&quot;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/x/g, fn)'`
- `'const s = html.replace(/</g, '&lt;')[key](x)'`
- `'foo.replace(bar[html.replace(/</g, '&lt;')], z)'`
- `'foo(bar[html.replace(/</g, '&lt;')])'`
- `'foo['bar'](baz[html.replace(/</g, '&lt;')])'`
- `'foo.bar(baz[html.replace(/</g, '&lt;')])'`
- `'element.innerHTML = req.body.content;'`
- `'div.innerHTML = req.query.html;'`
- `'element.innerHTML = "<script>alert(1)</script>";'`
- `'res.send("<img src=x onerror=alert(1)>");'`
- `'res.send(['<li>', req.query.name, '</li>'].join(''))'`
- `'res.send('<p>' + renderBio(req.body.bio) + '</p>')'`
- `'res.send({ error: '<b>' + req.query.msg + '</b>' })'`
- `'res.send('<p>' + sanitizers[kind](req.body.bio) + '</p>')'`
- `'res.send('<p>' + lib.html.escape(req.body.bio) + '</p>')'`
- `'res.send(\'<p>\' + '${req.body.bio}' + \'</p>\')'`
- `'res.send('<b>' + count + '</b>')'`

## False-positive guards — what it must NOT flag

74 asserted case(s). A rule with few of these has not been argued with.

- `'res.send('<p>' + data[length] + '</p>');'`
- `'res.send('<script>alert(1)</script>');'`
- `'res.send('<div>' + req.query.name + '</div>');'`
- `'res.send(req.query.name || '<p>fallback</p>');'`
- `'res.send(flag ? req.query.name : '<p>x</p>');'`
- `'element.innerHTML = userInput.replace(/</g, "&lt;");'`
- `'const safe = data.replace(/>/g, "&gt;");'`
- `'element.innerHTML = userInput.replace(/</g, "&lt;").replace(/>/g, "&gt;");'`
- `'const safeTitle = title.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'`
- `'function escapeHtml(unsafe) {`
- `'template = template`
- `'otherContent = normalizeContent(`
- `'return html.replace(/<\\/head>/, '<script src="x" defer></script></head>')'`
- `'const s = html.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/"/g, '&quot;').trim(`
- `'const s = html.replace(/</g, '&lt;').replace(/>/g, '&gt;')'`
- `'const s = html.replace(/</g, '&lt;')'`
- `'const s = html.replace('<', '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/\\s+/g, ' ')'`
- `'const s = html.replace(/&/g, '&amp;').replace(/</g, '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/"/g, '&quot;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/x/g, fn)'`
- `'const s = html.replace(/</g, '&lt;')[key](x)'`
- `'foo.replace(bar[html.replace(/</g, '&lt;')], z)'`
- `'foo(bar[html.replace(/</g, '&lt;')])'`
- `'foo['bar'](baz[html.replace(/</g, '&lt;')])'`
- `'foo.bar(baz[html.replace(/</g, '&lt;')])'`
- `'element.innerHTML = req.body.content;'`
- `'div.innerHTML = req.query.html;'`
- `'element.innerHTML = "<script>alert(1)</script>";'`
- `'res.send("<img src=x onerror=alert(1)>");'`
- `'res.send('<form method="post"><p>Check to <label>'`
- `'res.send(['<h1>Examples:</h1> <ul>', '<li>GET /users</li>', '</ul>'].join('\\n'));'`
- `'res.send('user ' + escapeHtml(req.params.uid) + "'s pets")'`
- `'res.send({ error: "Sorry, can't find that" })'`
- `'res.json({ message: 'Done <ok>' })'`
- `'res.send('<p>' + DOMPurify.sanitize(req.body.bio) + '</p>')'`
- `'res.send('<p>' + he.encode(req.body.bio) + '</p>')'`
- `'res.send(['<li>', req.query.name, '</li>'].join(''))'`
- `'res.send('<p>' + renderBio(req.body.bio) + '</p>')'`
- `'res.send({ error: '<b>' + req.query.msg + '</b>' })'`
- `'res.send('<p>' + sanitizers[kind](req.body.bio) + '</p>')'`
- `'res.send('<p>' + lib.html.escape(req.body.bio) + '</p>')'`
- `'res.send(\'<p>\' + '${req.body.bio}' + \'</p>\')'`
- `'res.send('<b>' + count + '</b>')'`
- `'const s = html.replace(/</g, '&lt;').replace(/>/g, '&gt;')'`
- `'const s = html.replace(/</g, '&lt;')'`
- `'const s = html.replace('<', '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/\\s+/g, ' ')'`
- `'const s = html.replace(/&/g, '&amp;').replace(/</g, '&lt;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/"/g, '&quot;')'`
- `'const s = html.replace(/</g, '&lt;').replace(/x/g, fn)'`
- `'const s = html.replace(/</g, '&lt;')[key](x)'`
- `'foo.replace(bar[html.replace(/</g, '&lt;')], z)'`
- `'foo(bar[html.replace(/</g, '&lt;')])'`
- `'foo['bar'](baz[html.replace(/</g, '&lt;')])'`
- `'foo.bar(baz[html.replace(/</g, '&lt;')])'`
- `'element.innerHTML = req.body.content;'`
- `'div.innerHTML = req.query.html;'`
- `'element.innerHTML = "<script>alert(1)</script>";'`
- `'res.send("<img src=x onerror=alert(1)>");'`
- `'res.send('<form method="post"><p>Check to <label>'`
- `'res.send(['<h1>Examples:</h1> <ul>', '<li>GET /users</li>', '</ul>'].join('\\n'));'`
- `'res.send('user ' + escapeHtml(req.params.uid) + "'s pets")'`
- `'res.send({ error: "Sorry, can't find that" })'`
- `'res.json({ message: 'Done <ok>' })'`
- `'res.send('<p>' + DOMPurify.sanitize(req.body.bio) + '</p>')'`
- `'res.send('<p>' + he.encode(req.body.bio) + '</p>')'`
- `'res.send(['<li>', req.query.name, '</li>'].join(''))'`
- `'res.send('<p>' + renderBio(req.body.bio) + '</p>')'`
- `'res.send({ error: '<b>' + req.query.msg + '</b>' })'`
- `'res.send('<p>' + sanitizers[kind](req.body.bio) + '</p>')'`
- `'res.send('<p>' + lib.html.escape(req.body.bio) + '</p>')'`
- `'res.send(\'<p>\' + '${req.body.bio}' + \'</p>\')'`
- `'res.send('<b>' + count + '</b>')'`

## Not covered

Corpus: 2 vulnerable, 2 safe fixtures under `CWE-116`.

