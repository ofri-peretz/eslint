# `node-security/no-weak-hash-algorithm`

**CWE-327** · severity CRITICAL · 27 catch cases · 45 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-ecb-mode, no-insecure-rsa-padding, no-weak-cipher-algorithm — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-weak-hash-algorithm
Detects use of weak hash algorithms (MD5, SHA1, MD4)
CWE-327: Use of a Broken or Risky Cryptographic Algorithm

@see https://cwe.mitre.org/data/definitions/327.html

## True positives — what it catches

27 asserted case(s). These are the claims; each is only as good as the case.

- `'crypto.createHash("md5").update(data);'`
- `'crypto.createHash("sha1").update(data);'`
- `'crypto.createHash("md4").update(data);'`
- `'crypto.createHash("MD5").update(data);'`
- `'createHash("md5");'`
- `'crypto.createHash("ripemd").update(data);'`
- `'sha1(data);'`
- `'md5(password);'`
- `'md4(data);'`
- `'crypto.createHash("whirlpool").update(data);'`
- `'const signature = createHash("sha1").update(data).digest("hex");'`
- `'function f() { return createHash("md5").update(pw).digest("hex"); }'`
- `'verify(createHash("sha1").update(x).digest("hex"));'`
- `'obj[key] = createHash("sha1").update(x).digest("hex");'`
- `'const [a] = createHash("md5").update(x).digest("hex");'`
- `'const meta = { 1: createHash("md5").update(x).digest("hex") };'`
- `'const meta = { [k]: createHash("sha1").update(x).digest("hex") };'`
- `'this.sha = createHash("sha1").update(lua).digest("hex");'`
- `'const digest = crypto.createHash('md5').update(password).digest('hex');'`
- `'const signature = crypto.createHash('sha1').update(body).digest('hex');'`
- `'const signRequest = (body) => md5(body);'`
- `'class Signer { authToken(body) { return sha1(body); } }'`
- `'const certFingerprint = createHash('sha1').update(pem).digest('hex');'`
- `'const d = md5(user.password);'`
- `'const api = { signPayload(body) { return md5(body); } };'`
- `'const signature = crypto.createHash(algo).update(body).digest('hex');`
- `'const licence = md5(payload);'`

## False-positive guards — what it must NOT flag

31 asserted case(s). A rule with few of these has not been argued with.

- `'crypto.createHash("md5");'`
- `'crypto.createHash("sha256");'`
- `'crypto.createHash("md5").update(data);'`
- `'const scriptDigest = createHash("sha1").update(lua).digest("hex");'`
- `'crypto.createHash("md5").update(data);'`
- `'crypto.createHash("sha1").update(data);'`
- `'crypto.createHash("md4").update(data);'`
- `'crypto.createHash("MD5").update(data);'`
- `'createHash("md5");'`
- `'crypto.createHash("ripemd").update(data);'`
- `'sha1(data);'`
- `'md5(password);'`
- `'md4(data);'`
- `'crypto.createHash("whirlpool").update(data);'`
- `'const signature = createHash("sha1").update(data).digest("hex");'`
- `'function f() { return createHash("md5").update(pw).digest("hex"); }'`
- `'verify(createHash("sha1").update(x).digest("hex"));'`
- `'obj[key] = createHash("sha1").update(x).digest("hex");'`
- `'const [a] = createHash("md5").update(x).digest("hex");'`
- `'const meta = { 1: createHash("md5").update(x).digest("hex") };'`
- `'const meta = { [k]: createHash("sha1").update(x).digest("hex") };'`
- `'this.sha = createHash("sha1").update(lua).digest("hex");'`
- `'const digest = crypto.createHash('md5').update(password).digest('hex');'`
- `'const signature = crypto.createHash('sha1').update(body).digest('hex');'`
- `'const signRequest = (body) => md5(body);'`
- `'class Signer { authToken(body) { return sha1(body); } }'`
- `'const certFingerprint = createHash('sha1').update(pem).digest('hex');'`
- `'const d = md5(user.password);'`
- `'const api = { signPayload(body) { return md5(body); } };'`
- `'const signature = crypto.createHash(algo).update(body).digest('hex');`
- `'const licence = md5(payload);'`

## Not covered

Corpus: 4 vulnerable, 4 safe fixtures under `CWE-327`.

