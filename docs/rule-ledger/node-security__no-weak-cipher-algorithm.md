# `node-security/no-weak-cipher-algorithm`

**CWE-327** · severity CRITICAL · 13 catch cases · 17 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 1 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-ecb-mode, no-insecure-rsa-padding, no-weak-hash-algorithm — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-weak-cipher-algorithm
Detects use of weak cipher algorithms (DES, 3DES, RC4, Blowfish)
CWE-327: Use of a Broken or Risky Cryptographic Algorithm

@see https://cwe.mitre.org/data/definitions/327.html

## True positives — what it catches

13 asserted case(s). These are the claims; each is only as good as the case.

- `'const cipher = crypto.createCipheriv('des', key, iv);'`
- `'const cipher = crypto.createCipheriv('DES', key, iv);'`
- `'const cipher = crypto.createCipheriv('des-ede3', key, iv);'`
- `'const cipher = crypto.createCipheriv('3des', key, iv);'`
- `'const cipher = crypto.createCipheriv('rc4', key, iv);'`
- `'const cipher = crypto.createCipheriv('blowfish', key, iv);'`
- `'const cipher = crypto.createCipheriv('bf', key, iv);'`
- `'const cipher = crypto.createCipheriv('rc2', key, iv);'`
- `'const cipher = crypto.createCipheriv('idea', key, iv);'`
- `'const cipher = crypto.createCipher('des', password);'`
- `'const decipher = crypto.createDecipheriv('rc4', key, iv);'`
- `'const cipher = createCipheriv('des', key, iv);'`
- `'const cipher = crypto.createCipheriv('cast5', key, iv);'`

## False-positive guards — what it must NOT flag

15 asserted case(s). A rule with few of these has not been argued with.

- `'crypto.createCipheriv("des-cbc", key, iv);'`
- `'crypto.createCipheriv("aes-256-gcm", key, iv);'`
- `'const cipher = crypto.createCipheriv('des', key, iv);'`
- `'const cipher = crypto.createCipheriv('DES', key, iv);'`
- `'const cipher = crypto.createCipheriv('des-ede3', key, iv);'`
- `'const cipher = crypto.createCipheriv('3des', key, iv);'`
- `'const cipher = crypto.createCipheriv('rc4', key, iv);'`
- `'const cipher = crypto.createCipheriv('blowfish', key, iv);'`
- `'const cipher = crypto.createCipheriv('bf', key, iv);'`
- `'const cipher = crypto.createCipheriv('rc2', key, iv);'`
- `'const cipher = crypto.createCipheriv('idea', key, iv);'`
- `'const cipher = crypto.createCipher('des', password);'`
- `'const decipher = crypto.createDecipheriv('rc4', key, iv);'`
- `'const cipher = createCipheriv('des', key, iv);'`
- `'const cipher = crypto.createCipheriv('cast5', key, iv);'`

## Not covered

Corpus: 4 vulnerable, 4 safe fixtures under `CWE-327`.

