# `secure-coding/no-graphql-injection`

**CWE-89** · severity HIGH · 50 catch cases · 117 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

5 defect(s), 4 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Defects — established

- **`unexercised-option`** (test-adequacy) — option(s) never set by any test: validationFunctions, trustedSanitizers, trustedAnnotations, strictMode — those branches ship unexecuted.
- **`orphan-message`** (metadata-contract) — messageId(s) declared but never reported: disableIntrospection, strategyQueryBuilder, strategyInputValidation, strategyIntrospection — dead metadata, or a report path that was lost.
- **`unasserted-message`** (test-adequacy) — messageId(s) the rule emits that no test asserts: useQueryBuilder, limitQueryDepth.
- **`inert-suggestion`** (metadata-contract) — 2 suggestion(s) with `fix: () => null`. ESLint discards these before they reach the user, so the remediation text attached to them is never shown and hasSuggestions overstates what the rule offers.
- **`no-corpus-fixture`** (measurement) — no benchmarks/corpus/CWE-89/ fixture — this rule contributes nothing to the published detection, false-positive or parity figures. Its real-world behaviour is unmeasured.

### Smells — unproven, each with its probe

- **`nominal-inference-suppress`** (detection-soundness) — a name-substring test can silence a finding. Costs recall rather than trust — a false negative is invisible to the user, which is why it is triaged after reports. **Probe:** lint a genuine vulnerability whose identifier hits the allowlist vocabulary.
- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-sql-injection — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.
- **`whole-program-text`** (performance) — sourceCode.getText() with no node prints the whole file. Inside a visitor this is O(file) per node. **Probe:** time the rule against a 5k-line file versus a 500-line one; cost should be linear, not quadratic.

## Why this rule exists

ESLint Rule: no-graphql-injection
Detects GraphQL injection vulnerabilities and DoS attacks (CWE-89, CWE-400)

GraphQL injection occurs when user input is improperly inserted into GraphQL
queries, allowing attackers to:
- Read/modify unauthorized data
- Perform DoS attacks with complex queries
- Extract schema information via introspection

False Positive Reduction:
This rule uses security utilities to reduce false positives by detecting:
- Safe GraphQL libraries (apollo-server, graphql-tools)
- Proper query builders and sanitizers
- JSDoc annotations (@safe, @validated)
- Input validation functions

## True positives — what it catches

42 asserted case(s). These are the claims; each is only as good as the case.

- `'const query = '{ __schema { types { name } } }';'`
- `'const introspectionQuery = 'query { __type(name: "User") { name fields { name } } }';'`
- `'const schemaQuery = '{ __schema { queryType { name } mutationType { name } } }';'`
- `'const query = 'query { user(id: "${userId}") { name } }';'`
- `'const mutation = 'mutation { createUser(name: "${name}", email: "${email}") { id } }';'`
- `'const complexQuery = 'query { users(filter: { name: "${searchTerm}", status: "${status}"`
- `'const query = "query { user(id: \\"" + userId + "\\") { name } }";'`
- `'const deepQuery = \'query {`
- `'const complexQuery = '{ users { posts { comments { author { posts { comments { author { n`
- `'const result = await graphql.execute(query, root, context, variables);'`
- `'const deepQuery = '{ users { friends { name } } }';'`
- `'myGraphql.execute(query, variables);'`
- `'const x = new SomeOtherClass('query { user(id: "${userId}") { name } }');'`
- `'wrapWithNoise('query { user(id: "${userId}") { name } }');'`
- `'myLogFn('query { user(id: "${userId}") { name } }');'`
- `'someObject.someMethod('query { user(id: "${userId}") { name } }');'`
- `'const m = 'mutation(id: "${userId}") { ok }';'`
- `'const q = 'query GetUser { id(x: "${userId}") }';'`
- `'const q = "query GetUser { id { nested } }";'`
- `'const typeDefs = 'type User { id: "${userId}" }';'`
- `'const t = gql'type User { id: "${userId}" }';'`
- `'const m = "mutation(id: 1) { ok { nested } }";'`
- `'const frag = 'fragment UserFields on User { id name ${extra} }';'`
- `'const schema = 'interface Node { ${extraField} }';'`
- `'const frag = "fragment UserFields on User { id name { nested } }";'`
- `'const schema = "enum Color { RED { GREEN } BLUE }";'`
- `'const q = "query { __schema { types { name } } }";'`
- `'const q = "query { __type(name: \\"User\\") { name } }";'`
- `'const q = "{ users { posts { comments { author { name } } } } }";'`
- `'const q = 'query { user(id: "${userId}") { name } }';'`
- `[`
- `'const q = '{ users(name: "${name}") { posts { id } } }';'`
- `'const q = 'query { user(id: "' + userId + \'") { name } }\';'`
- organization_beta_flags.ts:4 — interpolation into a gql query — `[`
- __type is still an introspection root — `'const q = 'query { __type(name: "User") { name } }';'`
- __schema is still an introspection root — `'const q = '{ __schema { types { name } } }';'`
- a real selection set with interpolation — `'function f(userId) { return '{ user(id: "${userId}") { name email } }'; }'`
- attributed by a typeDefs binding — `'const typeDefs = 'interface Node { ${extraField} }';'`
- attributed by a typeDefs property — `'makeSchema({ typeDefs: 'interface Node { ${extraField} }' });'`
- attributed by a member gql tag — `'const t = apollo.gql'interface Node { ${extraField} }';'`
- attributed by a GraphQL library call — `'graphqlClient('interface Node { ${extraField} }');'`
- Nested `+` nodes each report, as they already did — the point here is that the INNER one is attributed by climbing to the outermost concatenation and finding the `typeDefs` binding. attributed concatenation climbs the + chain — `'const typeDefs = "type User {" + fields + "}";'`

## False-positive guards — what it must NOT flag

79 asserted case(s). A rule with few of these has not been argued with.

- `'const query = 'query($id: ID!) { user(id: $id) { name } }';'`
- `'const result = await graphql({ schema, source: query, variableValues });'`
- `'const typeDefs = 'type User { id: ID! name: String }';'`
- `'const message = "Hello World";'`
- `'const query = '{ __schema { types { name } } }';'`
- `'const query = '{ __schema { types { name } } }';'`
- `'const introspectionQuery = 'query { __type(name: "User") { name fields { name } } }';'`
- `'const schemaQuery = '{ __schema { queryType { name } mutationType { name } } }';'`
- `'const query = 'query { user(id: "${userId}") { name } }';'`
- `'const mutation = 'mutation { createUser(name: "${name}", email: "${email}") { id } }';'`
- `'const complexQuery = 'query { users(filter: { name: "${searchTerm}", status: "${status}"`
- `'const query = "query { user(id: \\"" + userId + "\\") { name } }";'`
- `'const deepQuery = \'query {`
- `'const complexQuery = '{ users { posts { comments { author { posts { comments { author { n`
- `'const result = await graphql.execute(query, root, context, variables);'`
- `'const query = '{ users { name email } }';'`
- `'const schema = 'type Query { users: [User] }';'`
- `'const query = '{ __schema { types { name } } }';'`
- `'const shallowQuery = '{ users { name } }';'`
- `'const deepQuery = '{ users { friends { name } } }';'`
- `'myGraphql.execute(query, variables);'`
- `'throw new Error('query { user { name } }');'`
- `'throw new TypeError('{ users { posts { comments { author { name } } } } }');'`
- `'throw new RangeError('{ users { posts { comments { author { name } } } } }');'`
- `'const x = new SomeOtherClass('query { user(id: "${userId}") { name } }');'`
- `'myLog.debug('query { user { name } }');'`
- `'throw new MyCustomError('query { user { name } }');'`
- `'wrapWithNoise('query { user(id: "${userId}") { name } }');'`
- `'myLogFn('query { user(id: "${userId}") { name } }');'`
- `'someObject.someMethod('query { user(id: "${userId}") { name } }');'`
- `'const s = 'myquery queryx myqueryx';'`
- `'const s = 'plain ${value} text {single}';'`
- `'const query = 'query { user(id: "${validate(userId)}") { name } }';'`
- `'const greeting = "Hello, " + name + "!";'`
- `'const s = 'the type';'`
- `'const s = "query foo";'`
- `'const s = "fragment: nope";'`
- `'validate(graphql.execute(cleanId));'`
- `'const m = 'mutation(id: "${userId}") { ok }';'`
- `'const q = 'query GetUser { id(x: "${userId}") }';'`
- `'const q = "query GetUser { id { nested } }";'`
- `'const s = 'query foo';'`
- `'const s = 'fragment Foo bar baz';'`
- `'const typeDefs = 'type User { id: "${userId}" }';'`
- `'const t = gql'type User { id: "${userId}" }';'`
- `'const m = "mutation(id: 1) { ok { nested } }";'`
- `'const s = "fragment Foo bar baz";'`
- `'const s = 'type : nope';'`
- `'const frag = 'fragment UserFields on User { id name ${extra} }';'`
- `'const s = 'fragment: 3';'`
- `'const schema = 'interface Node { ${extraField} }';'`
- `'const frag = "fragment UserFields on User { id name { nested } }";'`
- `'const schema = "enum Color { RED { GREEN } BLUE }";'`
- `'const s = "type:3";'`
- `'const s = "the type ";'`
- `'const q = "query { __schema { types { name } } }";'`
- `'const q = "query { __type(name: \\"User\\") { name } }";'`
- `'const q = "{ users { posts { comments { author { name } } } } }";'`
- `'getClient()(query, variables);'`
- `'const q = 'query { user(id: "${userId}") { name } }';'`
- `[`
- `'const q = '{ users(name: "${name}") { posts { id } } }';'`
- `'const q = 'query { user(id: "' + userId + \'") { name } }\';'`
- __typename is not an introspection root — `'await graphql('query { __typename }', {})'`
- __typename inside a template query — `'const SLO_QUERY = 'query($id: ID!) {\\n sloDefinition(id: $id) {\\n sli { __typename`
- JSON with a Liquid output tag is not a selection set — `'function f(config) { return '{ "type": "display", "value": {{ ${config.snippet} | json }}`
- JSON with a Liquid statement tag is not a selection set — `'function f(config) { return '{ "type": "context", "value": "{% ${config.snippet} %}" }';`
- a bare Liquid output tag is not a selection set — `'function f(config) { return '{{ ${config.snippet} }}'; }'`
- a TypeScript interface template is not SDL — `'function f(types, toolRegistrations) { return '${types}\\ninterface ShopifyTools {\\n${to`
- an unattributed enum template is not SDL — `'const t = 'enum Color { ${values} }';'`
- organization_beta_flags.ts:4 — interpolation into a gql query — `[`
- __type is still an introspection root — `'const q = 'query { __type(name: "User") { name } }';'`
- __schema is still an introspection root — `'const q = '{ __schema { types { name } } }';'`
- a real selection set with interpolation — `'function f(userId) { return '{ user(id: "${userId}") { name email } }'; }'`
- attributed by a typeDefs binding — `'const typeDefs = 'interface Node { ${extraField} }';'`
- attributed by a typeDefs property — `'makeSchema({ typeDefs: 'interface Node { ${extraField} }' });'`
- attributed by a member gql tag — `'const t = apollo.gql'interface Node { ${extraField} }';'`
- attributed by a GraphQL library call — `'graphqlClient('interface Node { ${extraField} }');'`
- Nested `+` nodes each report, as they already did — the point here is that the INNER one is attributed by climbing to the outermost concatenation and finding the `typeDefs` binding. attributed concatenation climbs the + chain — `'const typeDefs = "type User {" + fields + "}";'`

## Not covered

No `benchmarks/corpus/CWE-89/` fixture, so this rule contributes nothing to the published detection or false-positive figures. It is untested by the benchmark.

