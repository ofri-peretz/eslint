# `node-security/no-insecure-rsa-padding`

**CWE-327** · severity HIGH · 6 catch cases · 12 quiet cases

> Generated by `npm run rule-ledger -- --dossier`. Every line below is read from
> the rule and its tests — edit those, not this file.

## Audit

0 defect(s), 2 smell(s). A defect is a fact about this
rule's files. A smell is a pattern that co-occurs with a defect class and **proves
nothing on its own** — each carries the probe that would settle it. Do not add the
two together, and do not report a smell as a finding until its probe has been run.

### Smells — unproven, each with its probe

- **`textual-matching`** (detection-soundness) — a decision is taken on PRINTED SOURCE rather than on the AST. Text matching cannot distinguish a call from the same words appearing in a string, a comment, or a different scope, and it silently follows formatting. **Probe:** put the matched text in a string literal or a comment inside otherwise-clean code. A report proves the check reads text, not structure.
- **`duplicate-coverage`** (placement) — same CWE and overlapping visitor keys as: no-ecb-mode, no-weak-cipher-algorithm, no-weak-hash-algorithm — a single line may produce two findings. **Probe:** lint one vulnerable fixture with both rules on and count the reports.

## Why this rule exists

ESLint Rule: no-insecure-rsa-padding
Detects RSA with PKCS#1 v1.5 padding (Marvin Attack vulnerable)
CVE-2023-46809: Timing side-channel in Node.js privateDecrypt()

@see https://nvd.nist.gov/vuln/detail/CVE-2023-46809
@see https://people.redhat.com/~hkario/marvin/

## True positives — what it catches

6 asserted case(s). These are the claims; each is only as good as the case.

- `'privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buf);'`
- `'crypto.privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.publicDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.privateEncrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.publicEncrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.privateDecrypt({ key, padding: RSA_PKCS1_PADDING }, buffer);'`

## False-positive guards — what it must NOT flag

8 asserted case(s). A rule with few of these has not been argued with.

- `'crypto.privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buf);'`
- `'crypto.privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_OAEP_PADDING }, buf);'`
- `'privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buf);'`
- `'crypto.privateDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.publicDecrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.privateEncrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.publicEncrypt({ key, padding: crypto.constants.RSA_PKCS1_PADDING }, buffer);'`
- `'crypto.privateDecrypt({ key, padding: RSA_PKCS1_PADDING }, buffer);'`

## Not covered

Corpus: 4 vulnerable, 4 safe fixtures under `CWE-327`.

