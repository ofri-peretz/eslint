/** SAFE - ADVERSARIAL. Product copy and an analytics payload that both
 *  contain colon-separated pairs. Neither is CSS, and neither renders. */
export const COPY = {
  tagline: 'Transparency: we show you every fee, up front.',
  ratio: 'Contrast ratio: 4.5:1 minimum',
};

track('checkout:step', { step: 0, visibility: 'public', top: 0 });
