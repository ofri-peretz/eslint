/**
 * SAFE - The consent state read off a preferences object, which is what every
 * consent-management platform actually hands you.
 */
if (preferences.gdpr.analytics) {
  gtag('event', 'page_view');
}
