/** VULNERABLE - Next.js declares headers in config rather than in code. This
 *  block ships two headers and omits Content-Security-Policy entirely. */
module.exports = {
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'X-Content-Type-Options', value: 'nosniff' },
        ],
      },
    ];
  },
};
