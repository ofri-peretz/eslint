/**
 * SAFE - The scheme appears only in a comment.
 */
// Legacy hosts used to be http:// only; everything is HTTPS since the migration.
export const env = { apiBase: 'https://api.acme-corp.io/v1' };
