/**
 * SAFE - ADVERSARIAL. The scheme appears MID-SENTENCE in documentation prose,
 * not at the start of a URL. A rule that searches for the scheme anywhere in a
 * string reports its own error messages.
 */
export const HELP = 'Endpoints must not use http:// — switch them to https:// before release.';
