/**
 * SAFE - English words contain other English words. Neither of these is a secret.
 */
localStorage.setItem('article-author', post.byline);
localStorage.setItem('tokenizer-config', JSON.stringify({ mode: 'bpe' }));
