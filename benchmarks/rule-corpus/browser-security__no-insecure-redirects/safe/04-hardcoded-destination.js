/**
 * SAFE - A hardcoded destination held in a binding. Being an Identifier is not
 * evidence of anything; the OLD rule reported exactly this.
 */
const CHECKOUT = 'https://shop.acme-corp.io/checkout';
window.location.href = CHECKOUT;
