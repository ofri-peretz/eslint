/**
 * SAFE - The same remediation through concatenation, which is the spelling the
 * rule used to be unable to read in EITHER direction.
 */
document.cookie = 'cart=' + cartId + '; Path=/; Secure; SameSite=Lax';
