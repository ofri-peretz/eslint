/**
 * SAFE - `.add` and `.put` are on half the objects in a JavaScript program.
 * A job queue, a Set and a Redux-ish store are not IndexedDB.
 */
jobQueue.add({ credential: c });
uniqueKeys.add({ apiKey: k });
reduxStore.put({ password: p });
