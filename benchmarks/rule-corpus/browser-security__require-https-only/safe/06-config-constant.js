/**
 * SAFE FOR THIS RULE - A cleartext URL in a config object is not a call site.
 * `no-http-urls` owns this line; this rule has no request to point at.
 */
export const settings = {
  legacyProxy: 'http://proxy.acme-corp.io:8080',
};
