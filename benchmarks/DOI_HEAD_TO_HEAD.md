# detect-object-injection, head to head

Generated by `scripts/doi-head-to-head.mts`. Every number on this page was
produced by running both rules over the same string in the same process on
the run that wrote it. Nothing is quoted and nothing is cached.

Ours: `secure-coding/detect-object-injection`. Theirs: `eslint-plugin-security/detect-object-injection` v4.0.1.

## Controls

Before any comparison: ours reports **1** on `o[req.body.k] = 1`, theirs reports **1** on `o[key] = v` — its own documentation example. Both rules are running. A comparison against a rule that is not running measures the harness, not the rule.

## The two contracts

Their documentation states: *"This rule flags any expression in the form of
`object[expression]` no matter where it occurs."* That is a **review aid** —
make dynamic access visible — not a vulnerability detector. Judged that way,
reporting a benign copy loop is the product working.

So the battery is scored twice. A rule cannot be criticised for failing a
contract it never made; it can be criticised for failing its own.

## Every case

`!` marks a wrong answer for that kind: a `defect` that did not fire, or a
`decoy`/`remedy` that did.

| id | kind | ours | theirs | case | why |
|---|---|---|---|---|---|
| `D01` | defect | 1 | 0 **!** | `function f(o, req) { o[req.query.p] = 1; }` | The caller names the key. `__proto__` re-parents the object. |
| `D02` | defect | 1 | 1 | `function f(o, req) { const k = req.body.key; o[k] = req.body.value; }` | Same defect, one binding of indirection. |
| `D03` | defect | 1 | 1 | `function f(o, req) { const { key } = req.body; o[key] = 1; }` | Same defect, destructured. |
| `D04` | defect | 1 | 0 **!** | `function f(o, req) { o[req.params.id] = 1; }` | A path segment is caller-chosen. |
| `D05` | defect | 1 | 0 **!** | `function f(o, s) { const d = JSON.parse(s); o[d.k] = d.v; }` | Parsed input is caller-controlled by definition. |
| `D06` | defect | 1 | 2 | `function merge(dst, src) { for (const k in src) { dst[k] = src[k]; } return dst; }` | The shape behind the published prototype-pollution advisories. |
| `D07` | defect | 1 | 2 | `function f(dst, req) { for (const k in req.body) { dst[k] = req.body[k]; } }` | Same loop, attacker at the root, no parameter to hide behind. |
| `D08` | defect | 1 | 1 | `class Bag { set(k, v) { this[k] = v; } }` | Verified by execution: calling with `__proto__` re-parents the instance. |
| `D09` | defect | 1 | 0 **!** | `function f(o) { o[getKey()] = 1; }` | Unresolvable key on a write. Reported because it cannot be proved safe. |
| `D10` | defect | 1 | 0 **!** | `function f(o, req) { o[`${req.query.p}`] = 1; }` | Interpolation does not launder the value. |
| `D11` | defect | 1 | 0 **!** | `function f(o, req) { o[req.headers["x-key"]] = 1; }` | A header is caller-chosen. |
| `D12` | defect | 1 | 1 | `function f(o, req) { const { a: { b } } = req.body; o[b] = 1; }` | Two levels of destructuring do not launder it. |
| `D13` | defect | 1 | 0 **!** | `function f(o, req) { o[req.body.a][req.body.b] = 1; }` | Both segments are caller-chosen. |
| `D14` | defect | 1 | 2 | `function f(o, req) { for (const k of Object.keys(req.body)) o[k] = req.body[k]; }` | The for-of spelling of D07. |
| `D15` | defect | 1 | 1 | `function f(o, req) { let k; k = req.query.p; o[k] = 1; }` | A late write to the binding is still a caller-chosen key. |
| `D16` | defect | 1 | 1 | `function f(req) { const k = req.query.k; ({}).constructor.prototype[k] = 1; }` | Pollution without an intermediate object at all. |
| `N01` | decoy | 0 | 1 **!** | `function f(paths, i) { return paths[i]; }` | A read. [[Get]] returns the prototype, it does not replace it. |
| `N02` | decoy | 0 | 0 | `function f(current, app) { return current[app.name]; }` | A read. |
| `N03` | decoy | 0 | 1 **!** | `function f(config, name) { send(config[name]); }` | A read. |
| `N04` | decoy | 0 | 0 | `function push(arr, x) { arr[arr.length] = x; }` | `arr.length` is a number; a number cannot name a prototype slot. |
| `N05` | decoy | 0 | 2 **!** | `function f(a, out) { for (let i = 0; i < a.length; i++) { out[i] = a[i]; } }` | The key is provably numeric from its declaration. |
| `N06` | decoy | 0 | 0 | `function f(out, i, v) { out[i + 1] = v; }` | Numeric by construction. |
| `N07` | decoy | 0 | 0 | `const buf = new Uint8Array(8); buf[0] = 1;` | A typed array has no string keys to pollute. |
| `N08` | decoy | 0 | 1 **!** | `const KEYS = ["alpha", "beta"]; const o = {}; for (const k of KEYS) { o[k] = 1; }` | Every value the key can take is written out in the file. |
| `N09` | decoy | 0 | 1 **!** | `const KEYS = Object.freeze(["alpha", "beta"]); const o = {}; for (const k of KEYS) { o[k] = 1; }` | Same, with a runtime guarantee attached. |
| `N10` | decoy | 0 | 2 **!** | `const cfg = { a: 1 }; const out = {}; for (const k in cfg) { out[k] = cfg[k]; }` | Nothing an attacker supplies reaches the loop. |
| `N11` | decoy | 0 | 0 | `function f(o) { return o["name"]; }` | The key is written in the source. |
| `N12` | decoy | 0 | 0 | `function f(o) { o[0] = 1; }` | A number. |
| `N13` | decoy | 0 | 1 **!** | `function f(k, v) { const bag = Object.create(null); bag[k] = v; return bag; }` | No prototype to pollute. |
| `N14` | decoy | 0 | 0 | `function f(m, k, v) { m.set(k, v); }` | Not a computed property access at all. |
| `N15` | decoy | 0 | 1 **!** | `const e = {}; function f(k) { return e[k] ?? null; }` | A read. |
| `N16` | decoy | 0 | 0 | `function f(o, g) { o[Symbol.iterator] = g; }` | A symbol cannot name `__proto__`. |
| `R01` | remedy | 0 | 2 **!** | `function merge(dst, src) { for (const k in src) { if (Object.hasOwn(src, k)) dst[k] = src[k]; } }` | The documented fix for D06. Reporting it leaves the user nothing to do. |
| `R02` | remedy | 0 | 2 **!** | `function f(dst, src) { for (const k in src) { if (k === "__proto__") continue; dst[k] = src[k]; } }` | The other documented fix. |
| `R03` | remedy | 0 | 1 **!** | `function f(map, k) { if (Object.hasOwn(map, k)) { return map[k]; } return undefined; }` | Guarded, and a read besides. |

## Scored as a detector

A `defect` must report; a `decoy` and a `remedy` must not. This is the
contract we hold ourselves to, and it is not the one they published.

| | TP | FN | FP | TN | precision | recall | F1 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **ours** | 16 | 0 | 0 | 19 | 100% | 100% | 100% |
| theirs | 9 | 7 | 11 | 8 | 45% | 56% | 50% |

## Scored against their own contract

Every row below IS `object[expression]`, which their documentation says is
flagged "no matter where it occurs".

| form | theirs | ours |
|---|---|---|
| identifier key (their own doc example) — `o[key] = v;` | 1 | 1 |
| member key — `o[req.query.p] = v;` | **missed** | 1 |
| nested index — `o[parts[i]] = v;` | 1 | 1 |
| call result — `o[f()] = v;` | **missed** | 1 |
| template literal — `o[`${a}`] = v;` | **missed** | 1 |
| concatenation — `o[a + b] = v;` | **missed** | 1 |
| conditional — `o[c ? a : b] = v;` | **missed** | 1 |
| await — `async function g(){ o[await k()] = v; }` | **missed** | 1 |
| assignment in key position — `o[(k = x)] = v;` | **missed** | 1 |
| comma expression — `o[(a, b)] = v;` | **missed** | 1 |
| optional call — `o[f?.()] = v;` | **missed** | 1 |

**9 of 11** documented forms are not reported by their rule.

One cause: the implementation tests `node.property.type === 'Identifier'`,
so a key reached through any other expression never arrives at the check.
The documentation describes something far broader than the code does.

## What this does and does not establish

It does not establish that their rule is bad at its job. Read as a review
aid, its false positives are the product, and its authors say so.

It establishes two things, both executed above:

1. **As a detector, the comparison is not close.** Their recall gap is not a
   tuning difference — the shortest way anyone writes CWE-1321,
   `o[req.query.p] = 1`, is invisible to them, and so is every guarded
   remedy, which means a team that fixes the bug still sees the finding.
2. **Judged on their own published contract, the rule does not do what it
   says.** That is not our standard applied to someone else; it is theirs.

And the direction that matters most for us: two of the disagreements were
OURS, found by running this comparison — `arr[arr.length] = x` and the
const-allowlist loop. Both were our false positives, both are fixed, and
both are in the case registry as `decoy` entries so they cannot come back.

## Read this before quoting the 100%

**We wrote this battery.** A perfect score on a set chosen by the rule’s own
authors measures the authors, and that criticism is the one levelled at every
self-authored corpus in this repository — including ours, repeatedly. The
number is a floor on how we handle cases we thought of, not a measurement of
the rule in the world.

Three things make it worth more than nothing, and none makes it a benchmark:

1. **Six of the sixteen defects (D11–D16) come from an adversarial pass
   written to make OUR rule fail**, not to flatter it — header keys, nested
   destructuring, chained attacker keys, the `Object.keys` merge spelling,
   late assignment, and a direct `constructor.prototype` write.
2. **The decoys are not inventions.** The read shapes, the copy loop and the
   allowlist come from the real-source scan, and two of them were our own
   findings before they were our own fixes.
3. **The comparison has already cost us twice**, which is the only real
   evidence that it is not rigged: it found `arr[arr.length]` and the
   const-allowlist loop, and both were ours to fix.

### What we still get wrong, stated rather than omitted

- **A TS enum used as a key reports.** `function f(o, e: E) { o[e] = 1 }` is
  a closed set, and a numeric one for a numeric enum — deciding that needs
  the type, and these rules are type-unaware by policy. A documented limit,
  not an oversight, and it is a false positive all the same.
- **A bare parameter used as an index reports.** `function f(o, i) { o[i] = 1 }`
  cannot be proved numeric from its declaration. Deliberate, and the reason
  is in the rule: the name-based version of this cleared
  `function put(o, k) { o[k] = 1 }`, which is the defect itself.
- **`o[String(i)] = 1` reports.** `String()` of an arbitrary parameter can
  produce any string, including `__proto__`.

One adversarial case was thrown out rather than counted: `o?.[k] = 1` is
invalid JavaScript — you cannot assign through an optional chain. It read as
a miss until it was run.
