// Kept in sync with ILB-0136's `code`. The registry lints the string, not this
// file — this exists so `./b` resolves and so the pair is readable on its own.
import { b } from './b';
export const a = b;
