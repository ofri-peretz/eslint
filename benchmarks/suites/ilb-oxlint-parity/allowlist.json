{
  "_note": "EMPTY as of 2026-09-04, and empty is the claim: measured across the full corpus with all 25 plugins the ESLint benchmark config loads, every rule reaches the same verdict under oxlint as under ESLint. 3,514 shared findings, zero divergence in either direction. An entry here would say a rule behaves differently under oxlint and we have decided to live with it — there is currently no such rule. Adding one requires a rulePattern, a reason and an introduced date, and it should be argued for rather than pasted in.",
  "eslintOnly": [],
  "oxlintOnly": []
}
