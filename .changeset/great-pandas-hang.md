---
---

Internal: test-only fixes (removed a duplicate RuleTester case, added a
no-op stand-in for external rule names in the component-api reference
self-test) plus a scoped `eslint-disable` comment in the private `@interlace/ui`
package. No published rule behavior change — no release needed.
