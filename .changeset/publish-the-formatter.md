---
'@interlace/eslint-formatter': minor
---

**✨ Feature** — the formatter is published. `eslint -f @interlace/eslint-formatter` emits ~408 tokens where `stylish` emits ~104,483, at the same measured signal score.
