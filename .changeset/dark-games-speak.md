---
---

Docs-only: corrected stale `eslint-plugin-pg` / `eslint-plugin-jwt` references in
four rule `.md` files (both packages were renamed and deprecated on npm), and
moved a "star the repo" footer into the three `.md` sources that already showed
it on the site. No rule behaviour, options, or API changed, so no package
release is needed.
