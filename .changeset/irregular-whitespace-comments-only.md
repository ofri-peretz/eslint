---
---

Comment-only. Three inert zero-width spaces removed and two reworded out of
block comments where they were preventing `*/` from closing early. No rule
behaviour changes, so nothing to release — this changeset exists to say that
deliberately rather than to bump a version for a comment.
