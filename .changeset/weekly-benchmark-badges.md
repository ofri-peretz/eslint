---
---

Add the ILB-Headline weekly benchmark: job-level head-to-head comparison against named community plugins, published as generated badges from GitHub Pages. Replaces the hand-typed README performance table, whose oxlint cache figure contradicted its own cited source and whose numbers were three months stale.

No package behaviour changes — benchmark infrastructure, README, and CI only.
