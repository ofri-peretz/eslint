{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "installCommand": "npm install --legacy-peer-deps --prefix apps/docs",
    "buildCommand": "npm run build --prefix apps/docs",
    "outputDirectory": "apps/docs/.next"
}
