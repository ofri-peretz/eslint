{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "installCommand": "bash -c 'cd apps/docs && npm install --legacy-peer-deps'",
    "buildCommand": "bash -c 'cd apps/docs && npm run build'",
    "outputDirectory": "apps/docs/.next"
}
