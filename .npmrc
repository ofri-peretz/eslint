# Local npm configuration for interlace
# Safe to commit - contains no secrets

# Use public npm registry for @interlace scope
@interlace:registry=https://registry.npmjs.org/

# Ensure we use public registry by default
registry=https://registry.npmjs.org/

# Authentication tokens are inherited from ~/.npmrc
