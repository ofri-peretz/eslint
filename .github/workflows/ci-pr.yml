name: CI Workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "!README.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/PULL_REQUEST_TEMPLATE/**"
  workflow_dispatch:

# Prevent concurrent runs on the same branch to save quota
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "24"

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # CI: Test, Build, Typecheck affected packages
  # ═══════════════════════════════════════════════════════════════════════════
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      # ═══════════════════════════════════════════════════════════════════════
      # EARLY CHECK: DOCS-ONLY CHANGES
      # ═══════════════════════════════════════════════════════════════════════

      - name: Check if changes are docs-only
        id: docs_only
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            CHANGED_FILES=$(git diff --name-only origin/main...HEAD)
          else
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD 2>/dev/null || echo "")
          fi

          echo "Changed files:"
          echo "$CHANGED_FILES"

          DOCS_ONLY=true
          while IFS= read -r file; do
            [[ -z "$file" ]] && continue
            if ! [[ "$file" =~ ^docs/ || "$file" =~ \.md$ || "$file" =~ README || "$file" =~ CHANGELOG ]]; then
              DOCS_ONLY=false
              break
            fi
          done <<< "$CHANGED_FILES"

          if [[ "$DOCS_ONLY" == "true" && -n "$CHANGED_FILES" ]]; then
            echo "✅ Only documentation files changed - skipping full CI"
            echo "docs_only=true" >> $GITHUB_OUTPUT
          else
            echo "⚙️ Non-docs files changed - running full CI pipeline"
            echo "docs_only=false" >> $GITHUB_OUTPUT
          fi

      - name: ✅ Skip CI for docs-only changes
        if: steps.docs_only.outputs.docs_only == 'true'
        run: |
          echo "✅ CI passed automatically - only documentation files were changed"
          exit 0

      # ═══════════════════════════════════════════════════════════════════════
      # SETUP: NODE + DEPENDENCIES
      # ═══════════════════════════════════════════════════════════════════════

      - name: ⚙️ Setup Node.js
        if: steps.docs_only.outputs.docs_only != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: 📦 Install dependencies
        if: steps.docs_only.outputs.docs_only != 'true'
        run: npm ci

      # ═══════════════════════════════════════════════════════════════════════
      # NX CACHE - Persist build artifacts between runs
      # ═══════════════════════════════════════════════════════════════════════

      - name: 🗄️ Restore NX cache
        if: steps.docs_only.outputs.docs_only != 'true'
        uses: actions/cache@v4
        with:
          path: .nx/cache
          key: nx-cache-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-${{ github.sha }}
          restore-keys: |
            nx-cache-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-
            nx-cache-${{ runner.os }}-

      # ═══════════════════════════════════════════════════════════════════════
      # DERIVE AFFECTED PACKAGES
      # ═══════════════════════════════════════════════════════════════════════

      - name: 🔍 Derive affected packages
        if: steps.docs_only.outputs.docs_only != 'true'
        uses: nrwl/nx-set-shas@v4
        with:
          main-branch-name: main

      # ═══════════════════════════════════════════════════════════════════════
      # TEST, BUILD, TYPECHECK (runs in parallel with lint workflow)
      # ═══════════════════════════════════════════════════════════════════════

      - name: 🧪 Test Affected Packages
        if: steps.docs_only.outputs.docs_only != 'true'
        run: npx nx affected -t test -c ci --coverage --verbose
        env:
          NX_SKIP_NX_CLOUD: "true"

      # ═══════════════════════════════════════════════════════════════════════
      # CODECOV UPLOAD
      # ═══════════════════════════════════════════════════════════════════════

      - name: 📥 Install Codecov CLI
        if: steps.docs_only.outputs.docs_only != 'true'
        run: |
          curl -Os https://cli.codecov.io/latest/linux/codecov
          chmod +x codecov

      - name: 📤 Upload coverage to Codecov
        if: steps.docs_only.outputs.docs_only != 'true'
        run: |
          ./codecov upload-process \
            --token ${{ secrets.CODECOV_TOKEN }} \
            --git-service github \
            --exclude .nx/cache \
            --fail-on-error || echo "⚠️ Codecov upload completed with warnings"
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: 🧪 Upload test results to Codecov (Test Analytics)
        if: always() && steps.docs_only.outputs.docs_only != 'true'
        run: |
          # Find all JUnit XML files generated by vitest
          JUNIT_FILES=$(find packages -name "junit.xml" -o -name "test-report.junit.xml" 2>/dev/null | head -50)

          if [ -n "$JUNIT_FILES" ]; then
            echo "📁 Found JUnit files:"
            echo "$JUNIT_FILES"
            
            ./codecov do-upload \
              --token ${{ secrets.CODECOV_TOKEN }} \
              --report-type test_results \
              --git-service github || echo "⚠️ Test results upload completed with warnings"
          else
            echo "⚠️ No JUnit XML files found for Test Analytics"
          fi
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: 🔨 Build Affected Packages
        if: steps.docs_only.outputs.docs_only != 'true'
        run: npx nx affected -t build --verbose
        env:
          NX_SKIP_NX_CLOUD: "true"

      - name: 🔍 Typecheck Affected Packages
        if: steps.docs_only.outputs.docs_only != 'true'
        run: npx nx affected -t typecheck --verbose
        env:
          NX_SKIP_NX_CLOUD: "true"

      # ═══════════════════════════════════════════════════════════════════════
      # RELEASE VALIDATION (Non-blocking early warning)
      # ═══════════════════════════════════════════════════════════════════════

      - name: 🔍 Validate Release (Dry-Run)
        if: steps.docs_only.outputs.docs_only != 'true'
        continue-on-error: true
        run: |
          echo "🔍 Release validation (non-blocking)..."
          npx nx release version --dry-run --projects=eslint-plugin-secure-coding 2>&1 || {
            echo "⚠️ Release dry-run detected potential issues (warning only)"
          }
        env:
          NX_SKIP_NX_CLOUD: "true"

      # ═══════════════════════════════════════════════════════════════════════
      # SUMMARY
      # ═══════════════════════════════════════════════════════════════════════

      - name: 📊 Print CI Summary
        if: always() && steps.docs_only.outputs.docs_only != 'true'
        run: |
          echo "## 📊 CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Node:** $(node --version)" >> $GITHUB_STEP_SUMMARY
          echo "- **npm:** $(npm --version)" >> $GITHUB_STEP_SUMMARY
