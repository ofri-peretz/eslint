# eslint-plugin-modernization

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-modernization` to build the library.

## Running unit tests

Run `nx test eslint-plugin-modernization` to execute the unit tests via [Jest](https://jestjs.io).
