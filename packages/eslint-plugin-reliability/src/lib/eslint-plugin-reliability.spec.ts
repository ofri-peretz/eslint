import { eslintPluginReliability } from './eslint-plugin-reliability';

describe('eslintPluginReliability', () => {
  it('should work', () => {
    expect(eslintPluginReliability()).toEqual('eslint-plugin-reliability');
  });
});
