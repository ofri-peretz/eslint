# eslint-plugin-reliability

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-reliability` to build the library.

## Running unit tests

Run `nx test eslint-plugin-reliability` to execute the unit tests via [Vitest](https://vitest.dev/).
