# eslint-plugin-modularity

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-modularity` to build the library.

## Running unit tests

Run `nx test eslint-plugin-modularity` to execute the unit tests via [Jest](https://jestjs.io).
