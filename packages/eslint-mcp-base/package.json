{
  "name": "@interlace/eslint-mcp-base",
  "version": "0.1.0",
  "description": "Generic MCP server base for any Interlace ESLint plugin. Each plugin-specific MCP package configures this with its plugin name + an optional rule prefix, and inherits the four standard tools: list_rules, audit_file, find_rule_violations, suggest_fix. Roadmap item 3.4.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.mjs",
  "exports": {
    ".": "./src/index.mjs"
  },
  "files": ["src", "README.md"],
  "keywords": ["mcp", "eslint", "static-analysis", "interlace", "agent"],
  "peerDependencies": {
    "eslint": ">=8.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
