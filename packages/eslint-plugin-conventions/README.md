# eslint-plugin-conventions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-conventions` to build the library.

## Running unit tests

Run `nx test eslint-plugin-conventions` to execute the unit tests via [Vitest](https://vitest.dev/).
