# eslint-plugin-operability

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build eslint-plugin-operability` to build the library.

## Running unit tests

Run `nx test eslint-plugin-operability` to execute the unit tests via [Vitest](https://vitest.dev/).
