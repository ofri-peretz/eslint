import { eslintPluginOperability } from './eslint-plugin-operability';

describe('eslintPluginOperability', () => {
  it('should work', () => {
    expect(eslintPluginOperability()).toEqual('eslint-plugin-operability');
  });
});
